---
phase: 05-route-maps
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - src/data/route-track.json
autonomous: false
requirements: [MAP-01, MAP-02]
user_setup:
  - service: stadia-maps
    why: "Production tile serving — localhost works unauthenticated, but the Netlify domain is rate-limited (HTTP 429) without a registered free account"
    env_vars: []
    dashboard_config:
      - task: "Create free Stadia Maps account and add the site domain under Authentication Configuration (domain-based auth, no API key)"
        location: "Stadia Maps client dashboard -> Authentication Configuration -> Domains"
  - service: nebo-gpx
    why: "Route polyline (MAP-02) — the Nebo app GPX export is the only source of actual GPS track coordinates (D-11); Claude cannot access the iPhone/iPad app"
    env_vars: []
    dashboard_config:
      - task: "Export GPX per trip (Nebo app -> Settings -> Trips -> Export GPX) and place all .gpx files in .planning/data/gpx/"
        location: "Nebo mobile app"

must_haves:
  truths:
    - "Once Nebo GPX files are placed in .planning/data/gpx/, a build regenerates src/data/route-track.json with the real Great Loop polyline under 500 KB"
    - "The full-route map draws the complete Great Loop track as a polyline on the deployed site"
    - "Stadia Maps tiles load on the production Netlify URL with no 429 errors"
    - "The voyage index map is usable on a mobile device (touch pan, pinch zoom, popup tap) with no layout overflow"
  artifacts:
    - path: "src/data/route-track.json"
      provides: "Real simplified Great Loop route (regenerated from Nebo GPX)"
      contains: "LineString"
  key_links:
    - from: ".planning/data/gpx/*.gpx"
      to: "src/data/route-track.json"
      via: "scripts/simplify-gpx.mjs run via astro:build:start hook"
      pattern: "coordinates"
---

<objective>
Complete the human-gated prerequisites that fully satisfy MAP-02 (real route polyline) and MAP-01 (production tiles), then verify the maps on a real device.

Purpose: The map components (05-02, 05-03) and the GPX pipeline (05-01) work with a stub route and localhost tiles. Three things require a human and cannot be automated: (1) exporting the Nebo GPX track from the mobile app and placing it in `.planning/data/gpx/` (D-10, D-11), (2) creating a free Stadia Maps account and registering the site domain so production tiles are not rate-limited, and (3) confirming the interactive map works on a mobile device. This plan gates and verifies those three.

Output: a regenerated real `src/data/route-track.json`, a configured Stadia account, and human sign-off on mobile behavior.

This plan is checkpoint-only (autonomous: false). It runs after the components exist (05-03) and the pipeline exists (05-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-route-maps/05-CONTEXT.md
@.planning/phases/05-route-maps/05-RESEARCH.md
@.planning/phases/05-route-maps/05-UI-SPEC.md
@.planning/phases/05-route-maps/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Export Nebo GPX and regenerate the real route polyline (D-10)</name>
  <what-built>05-01 produced scripts/simplify-gpx.mjs and an astro:build:start hook that converts any .gpx files in .planning/data/gpx/ into src/data/route-track.json. A committed stub is currently in place (empty LineString). The real GPS track can only come from the Nebo app export.</what-built>
  <read_first>
    - .planning/phases/05-route-maps/05-CONTEXT.md (D-09, D-10, D-11, D-12)
    - .planning/phases/05-route-maps/05-RESEARCH.md (Open Question 1: Nebo GPX format/count; Pitfall 4: file too large)
    - scripts/simplify-gpx.mjs (the converter and its 500 KB size gate + TOLERANCE constant)
  </read_first>
  <action>Pause for the human to export the Great Loop GPX from the Nebo app (Settings -> Trips -> Export GPX), create the `.planning/data/gpx/` directory, and place all exported `.gpx` files there. Claude cannot access the mobile app, so this step is human-only. After files are present, run `node scripts/simplify-gpx.mjs` then `npm run build` to regenerate `src/data/route-track.json` from real coordinates. If the size gate fails, raise `TOLERANCE` in `scripts/simplify-gpx.mjs` and rerun. If no GPX is available, the human may defer MAP-02 and ship markers-only.</action>
  <how-to-verify>
    1. In the Nebo app: Settings -> Trips -> Export GPX. Export the Great Loop trip(s).
    2. Create the directory `.planning/data/gpx/` and copy all exported `.gpx` files into it.
    3. Run `node scripts/simplify-gpx.mjs` and confirm it prints a non-zero simplified point count and an output size under 500 KB (if it exits with a size-gate error, raise `TOLERANCE` in the script and rerun).
    4. Run `npm run build` and confirm it exits 0 and the build log shows "Running GPX simplification...".
    5. Run `npm run preview`, open `/voyages/great-loop/`, and confirm the polyline traces the full Great Loop route.
  </how-to-verify>
  <verify>
    <automated>test -d .planning/data/gpx && node scripts/simplify-gpx.mjs && node -e "const d=require('./src/data/route-track.json'); if(d.geometry.coordinates.length===0) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - After GPX files are placed, `src/data/route-track.json` has a non-empty `geometry.coordinates` array and is under 500000 bytes
    - `npm run build` exits 0 with the GPX hook running (not the "skipped" warning)
    - Human confirms the rendered polyline visually covers the Great Loop route from New Bern NC around the loop and back
    - If no GPX is available yet, human explicitly defers MAP-02 polyline (map ships with markers only) — documented in the SUMMARY as a known gap
  </acceptance_criteria>
  <resume-signal>Type "approved" once route-track.json is regenerated and the polyline renders, or "defer" to ship markers-only with a documented MAP-02 gap.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Register the site domain with Stadia Maps (production tiles)</name>
  <what-built>Both map components use Stadia Maps stadia_outdoors tiles. Stadia serves localhost unauthenticated but rate-limits (HTTP 429) unregistered production domains. Domain-based auth requires no code change — only dashboard configuration.</what-built>
  <read_first>
    - .planning/phases/05-route-maps/05-RESEARCH.md (Pitfall 6: Stadia 429 in production; Security Domain: domain-based auth, no API key in code)
    - astro.config.mjs (the `site:` URL — the current Netlify domain to register)
  </read_first>
  <action>Pause for the human to create a free Stadia Maps account and register the production domain (the Netlify URL from astro.config.mjs `site:`, plus any custom domain) under Authentication Configuration. This is a dashboard-only step with no code change (domain-based auth). No API key or secret is added to the repository. After the site is deployed, confirm tile requests to tiles.stadiamaps.com return 200 (not 429) on the production URL.</action>
  <how-to-verify>
    1. Create a free Stadia Maps account at stadiamaps.com (no credit card).
    2. In the client dashboard, under Authentication Configuration, add the production domain (the Netlify URL from astro.config.mjs `site:`, currently incomparable-cranachan-979404.netlify.app, plus any custom domain).
    3. After the site is deployed, open the production URL, open DevTools Network, and confirm tile requests to tiles.stadiamaps.com return 200 (not 429).
  </how-to-verify>
  <verify>
    <automated>MISSING — production tile 200/429 check is a manual DevTools verification on the deployed Netlify URL; no local automated proxy</automated>
  </verify>
  <acceptance_criteria>
    - A free Stadia Maps account exists with the production Netlify domain registered under Authentication Configuration
    - On the deployed production URL, Stadia tile requests return HTTP 200 (no 429) in DevTools
    - No API key or secret is committed to git (domain-based auth only)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the domain is registered and production tiles load without 429, or "defer" if not yet deployed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify maps on a mobile device</name>
  <what-built>VoyageMap (full route, 400px, 68 markers with photo+excerpt+link popups) on /voyages/great-loop/ and PostMiniMap (single static pin) on individual posts. Success criteria require these to work on mobile with no layout overflow.</what-built>
  <read_first>
    - .planning/phases/05-route-maps/05-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/05-route-maps/05-UI-SPEC.md (Interaction Contracts; Layout Constraints; Z-Index Stack)
  </read_first>
  <action>Pause for the human to open the deployed site on a real phone and verify both maps behave per the UI-SPEC Interaction Contracts. Device-specific touch behavior (pan, pinch, popup auto-pan, scroll hijack) cannot be verified in an automated build, so a human must confirm on hardware.</action>
  <how-to-verify>
    1. On a phone (iOS or Android), open `/voyages/great-loop/`.
    2. Confirm: the map is 400px tall and does not overflow the page width; touch-pan and pinch-zoom work; tapping a stop marker opens a popup that auto-pans into view showing cover photo (where present), title, excerpt, and a working "Read more →" link that navigates to the post.
    3. Open an individual post that has lat/lon; confirm the single-pin mini map appears above the body, does not hijack scroll (dragging disabled), and shows the correct location.
    4. Open a post without lat/lon; confirm no mini map and no empty placeholder.
    5. Confirm map popups never render above the site header (z-index).
  </how-to-verify>
  <verify>
    <automated>MISSING — device-specific touch/gesture verification; confirmed by human on real mobile hardware</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms VoyageMap touch pan, pinch zoom, and marker popup tap all work on a real mobile device with no horizontal overflow or collapsed map height
    - Human confirms a marker popup shows photo + title + excerpt + working Read more link (MAP-03)
    - Human confirms PostMiniMap renders only on posts with lat/lon, above the body, with no scroll hijack
    - Human confirms popups stay below the site header
  </acceptance_criteria>
  <resume-signal>Type "approved" if all mobile checks pass, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nebo app -> .planning/data/gpx/ -> build | Author-provided GPX files enter the build pipeline |
| browser -> Stadia Maps CDN (production domain) | Tile requests authenticated by domain (Origin/Referer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information disclosure | Stadia Maps credentials / domain auth | mitigate | Domain-based auth only — no API key committed to git; if a key is ever used it stays in Netlify env, never in source |
| T-05-01 | Denial of Service | oversized regenerated route-track.json | mitigate | simplify-gpx.mjs 500 KB size gate blocks (process.exit 1) until TOLERANCE is raised |
| T-05-SC | Tampering | GPX file provenance | accept | GPX files are the site owner's own Nebo app exports (author-controlled) |
</threat_model>

<verification>
- Regenerated `src/data/route-track.json` has real coordinates and is under 500 KB (or MAP-02 explicitly deferred).
- Production Stadia tiles return 200, not 429.
- Human sign-off on mobile interaction for both map components.
</verification>

<success_criteria>
- Full Great Loop polyline renders on the deployed voyage index map (MAP-02), or is explicitly deferred with a documented gap.
- Production tiles load without rate-limiting (MAP-01).
- Both maps verified interactive on mobile.
</success_criteria>

<output>
Create `.planning/phases/05-route-maps/05-04-SUMMARY.md` when done.
</output>
