---
phase: 02-blogger-migration
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - MIG-04
  - MIG-05

must_haves:
  truths:
    - "All Phase 2 files are committed and pushed to origin main"
    - "10 old Blogger URLs (/YYYY/MM/slug.html) each redirect with HTTP 301 to the correct new Astro URL"
    - "jackiebiiigoingloopy.blogspot.com is still live and accessible (no DNS changes occurred)"
  artifacts:
    - path: "dist/_redirects"
      provides: "Netlify-readable 301 redirect rules — copied from public/_redirects during Astro build"
  key_links:
    - from: "public/_redirects"
      to: "dist/_redirects"
      via: "Astro copies public/ to dist/ at build time"
      pattern: "dist/_redirects"
    - from: "git push origin main"
      to: "Netlify auto-deploy"
      via: "netlify.toml publish = dist — Netlify picks up dist/_redirects from the deploy"
---

<objective>
Run pre-deploy gate checks, commit all Phase 2 output files, push to origin main to trigger Netlify auto-deploy, and verify the redirect rules are live with a human spot-check of 10 old Blogger URLs.

Purpose: Confirm Phase 2 is complete and the live Netlify site correctly serves 301 redirects from old Blogger URL patterns. The existing Blogger blog remains live (MIG-05) — no DNS changes.

Output: Git commit with all Phase 2 files; Netlify deploy live with 72 posts and redirect rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-blogger-migration/02-CONTEXT.md
@.planning/phases/02-blogger-migration/02-VALIDATION.md

<interfaces>
Netlify deploy setup (from Phase 1 — already configured):
  netlify.toml: command = "npm run build", publish = "dist", NODE_VERSION = "22"
  Netlify reads dist/_redirects after build; rules activate immediately on deploy.
  git push to origin main triggers automatic Netlify build+deploy (no manual trigger needed).

Redirect format in public/_redirects (generated by import-blogger.mjs):
  # comment header line
  /YYYY/MM/slug.html /blog/YYYY-MM-DD-slug/ 301
  — 72 rules total; source path has no domain; Netlify matches path only

Old Blogger URL structure confirmed (RESEARCH.md):
  Base: https://jackiebiiigoingloopy.blogspot.com/YYYY/MM/slug.html
  All 72 posts follow /YYYY/MM/slug.html exactly — no custom domains, no alternate structures

Phase gate checklist (02-VALIDATION.md):
  - npm run build exits 0 with zero errors and zero Zod warnings
  - ls src/content/blog/great-loop/*.mdx | wc -l equals 72
  - grep -v '^#' public/_redirects | grep -c '^/' equals 72
  - dist/_redirects exists after build
  - jackiebiiigoingloopy.blogspot.com is still live

Files to commit from Phase 2:
  - scripts/import-blogger.mjs (new)
  - src/content/blog/great-loop/ (72 new MDX files; 2024-01-01-test-scaffold.mdx deleted)
  - public/_redirects (new)
  - src/components/Gallery.astro (new)
  - src/components/VideoEmbed.astro (new)
  - .planning/data/blog-posts-full.json (new cache)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy gate checks</name>
  <files></files>

  <read_first>
    - public/_redirects — verify file structure and count before pushing; confirm no domain in source paths
    - .planning/phases/02-blogger-migration/02-VALIDATION.md — phase gate checklist; run all items before commit
  </read_first>

  <action>
    Run the full phase gate checklist from 02-VALIDATION.md before any git operations. These checks must all pass; fix any failures before proceeding to Task 2.

    Check 1 — MDX file count: ls src/content/blog/great-loop/*.mdx | wc -l must output 72. If lower than 72, re-run node scripts/import-blogger.mjs --force.

    Check 2 — Redirect count: grep -v '^#' public/_redirects | grep -c '^/' must output 72. If lower, re-run the import script.

    Check 3 — No test scaffold: ls src/content/blog/great-loop/ | grep test-scaffold | wc -l must output 0.

    Check 4 — Build passes: npm run build must exit 0 with zero Zod errors and zero MDX compile errors.

    Check 5 — dist/_redirects exists: ls dist/_redirects must succeed after the build in Check 4.

    Check 6 — Spot-check 3 MDX files: open and read 2022-04-16-getting-ready-to-go.mdx (or the earliest post), one 2023 post, and one 2024 post. Each must have double-quoted title, bare YYYY-MM-DD date, voyage: "great-loop", location string, excerpt under 200 chars, migrated: true, and a non-empty HTML body with sanitized img tags (no bare <br>, no <script> blocks, no unclosed img tags).

    Check 7 — Spot-check redirect file: open public/_redirects. Confirm first line is the comment header. Confirm line 2 starts with /2022/ (earliest posts). Confirm a few lines at random match the pattern /YYYY/MM/slug.html /blog/YYYY-MM-DD-slug/ 301.

    If any check fails, do not proceed. Fix the underlying issue and re-run checks from the beginning.
  </action>

  <acceptance_criteria>
    - ls src/content/blog/great-loop/*.mdx | wc -l outputs 72
    - grep -v '^#' public/_redirects | grep -c '^/' outputs 72
    - ls src/content/blog/great-loop/ | grep test-scaffold | wc -l outputs 0
    - npm run build exits 0
    - ls dist/_redirects succeeds (file exists)
    - Manual review of 3 MDX files confirms correct frontmatter format and sanitized HTML body
  </acceptance_criteria>

  <verify>
    <automated>
      ls src/content/blog/great-loop/*.mdx | wc -l
      grep -v '^#' public/_redirects | grep -c '^/'
      ls src/content/blog/great-loop/ | grep test-scaffold | wc -l
      npm run build
      ls dist/_redirects
    </automated>
  </verify>

  <done>All 7 gate checks pass: 72 MDX files, 72 redirects, no scaffold, clean build, dist/_redirects present, 3-post spot-check clean</done>
</task>

<task type="auto">
  <name>Task 2: Stage, commit, and push Phase 2 output</name>
  <files></files>

  <read_first>
    - public/_redirects — confirm file exists and is non-empty before staging
  </read_first>

  <action>
    Stage all Phase 2 output files using specific paths (do not use git add -A or git add .). Stage in this order:

    (1) git add src/content/blog/great-loop/ — stages all 72 new MDX files AND the deletion of 2024-01-01-test-scaffold.mdx (git add on a directory picks up both additions and deletions tracked by git).
    (2) git add scripts/import-blogger.mjs
    (3) git add public/_redirects
    (4) git add src/components/Gallery.astro
    (5) git add src/components/VideoEmbed.astro
    (6) git add .planning/data/blog-posts-full.json

    Run git status to confirm: 72 new .mdx files staged, 1 deleted .mdx staged, and the 5 other files staged. Expected staging summary: ~78 changes.

    Create a commit with a message that summarizes Phase 2. The message should reference the 72 posts, redirects, and media components. Use the HEREDOC pattern to avoid shell escaping issues.

    After the commit succeeds, run git push origin main. Netlify will detect the push and trigger an automatic build+deploy (netlify.toml is configured). The deploy typically completes in 1-3 minutes.

    Observe the Netlify deploy status in the terminal or at app.netlify.com. Wait for the deploy to complete (status: Published) before the human checkpoint in Task 3.
  </action>

  <acceptance_criteria>
    - git status after staging shows approximately 78 staged changes (72 new MDX + 1 deleted + 5 other files)
    - git commit exits 0 with a non-empty message referencing Phase 2 migration
    - git push origin main exits 0 (no authentication error, no rejected push)
    - Netlify deploy triggered (visible in app.netlify.com or via Netlify CLI if installed)
  </acceptance_criteria>

  <verify>
    <automated>
      git log --oneline -1
      git status
    </automated>
  </verify>

  <done>All Phase 2 files committed and pushed; Netlify deploy triggered and completes successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies redirects and Blogger blog status</name>

  <read_first>
    - public/_redirects — read 10 sample redirect rules to know exactly which old URLs to test; pick a spread across years (2022, 2023, 2024)
  </read_first>

  <what-built>
    Claude has: (1) imported 72 Blogger posts as MDX files into src/content/blog/great-loop/, (2) generated 72 Netlify 301 redirect rules in public/_redirects, (3) written Gallery.astro and VideoEmbed.astro components, and (4) pushed everything to origin main triggering a Netlify auto-deploy.
  </what-built>

  <how-to-verify>
    Wait for Netlify to finish deploying (check app.netlify.com or the Netlify CLI — deploy status should show "Published").

    Step 1 — Verify 10 old Blogger URLs redirect correctly. Open a browser and visit each of these 10 old Blogger URL patterns (substitute your actual Netlify site URL as the expected destination):

      From public/_redirects, pick 10 rules spanning multiple years (e.g., first 3 from 2022, 3 from 2023, 4 from 2024). For each rule, visit the source URL on the Netlify site: https://your-netlify-domain.netlify.app/{source-path} — e.g., https://your-netlify-domain.netlify.app/2022/04/getting-ready-to-go.html

      Confirm: you are redirected to the new Astro URL (/blog/YYYY-MM-DD-slug/) and the post renders correctly. The redirect status should be 301 (verify in browser DevTools Network tab — look for the 301 response followed by the 200 on the Astro post page).

    Step 2 — Confirm Blogger blog is still live. Visit https://jackiebiiigoingloopy.blogspot.com in a browser. Confirm the blog loads with the original Blogger posts intact. No DNS changes have been made; this is expected to still work.

    Step 3 — Spot-check 5 post pages on the Astro site. From the new Astro site (/blog/... URLs), visit 5 posts with photos. Confirm the photos display without broken image icons (they load from blogger.googleusercontent.com CDN). Confirm the page layout renders correctly.

    Step 4 — Check a post with a YouTube video. From blog-inventory.json or by browsing posts, find a post that had a YouTube embed. Confirm the YouTube iframe is visible and playable on the Astro post page (ROADMAP Phase 2 success criterion #5 — mandatory, not optional).
  </how-to-verify>

  <acceptance_criteria>
    - 10 old Blogger URL paths on the Netlify site return 301 redirects to the correct Astro post URLs
    - All 10 redirected Astro post pages load and render blog content (no 404 or build error pages)
    - https://jackiebiiigoingloopy.blogspot.com loads the original Blogger blog (site is still live)
    - 5 spot-checked posts have visible photos (no broken image icons)
    - At least one YouTube embed is visible and playable on an Astro post page (iframe renders, no 404)
  </acceptance_criteria>

  <resume-signal>Type "approved" if all checks pass. Describe any failures (e.g., "redirect for /2022/07/day-72.html went to wrong slug" or "Blogger blog returned 404").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/_redirects → Netlify CDN edge | Redirect rules processed at edge; malformed rules silently fail |
| git push → Netlify auto-deploy | Git remote is the deployment trigger; push should only go to origin main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | public/_redirects — redirect rules could misdirect users | mitigate | Pre-deploy gate check verifies `grep -v '^#' public/_redirects | grep -c '^/'` == 72 and npm run build exits 0 before pushing; spot-check of 10 rules in Task 3 checkpoint confirms correctness on live site |
| T-02-08 | Information Disclosure | git push — publishes full Blogger HTML content (travel blog posts) | accept | All content is intentionally public (migrating from public Blogger blog); no new privacy exposure; no credentials or personal data beyond what was already publicly accessible on blogspot.com |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; plan is git/verification operations only |
</threat_model>

<verification>
After Task 3 human checkpoint is approved:
- git log --oneline -1 shows the Phase 2 commit
- Netlify deploy status is Published
- 10 old Blogger paths return 301 on the live site
- jackiebiiigoingloopy.blogspot.com is still live
- 5 post spot-checks show photos rendering from blogger.googleusercontent.com
</verification>

<success_criteria>
All Phase 2 files committed and live on Netlify. Human has verified 10 redirects (301 status, correct destination). Blogger blog confirmed still live. Phase 2 success criteria from ROADMAP.md are met.
</success_criteria>

<output>
Create .planning/phases/02-blogger-migration/02-03-SUMMARY.md when done
</output>
