---
phase: 2
slug: blogger-migration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-07
---

# Phase 2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — Astro build-time Zod validation (no jest/vitest installed) |
| **Config file** | none |
| **Quick run command** | `npm run build` |
| **Full suite command** | `npm run build` + file count checks + visual spot-checks |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm run build`
- **After every plan wave:** Run `npm run build` + file count check + visual spot-check of 5 posts
- **Before `/gsd:verify-work`:** Full build green + `wc -l public/_redirects` == 72 + visual check of 10 redirects

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| Delete test scaffold | 01 | 1 | MIG-01 | — | — | File check | `ls src/content/blog/great-loop/ \| grep test-scaffold \| wc -l` == 0 | ✅ file exists pre-task | ○ |
| import-blogger.mjs | 01 | 1 | MIG-01, MIG-02, MIG-04 | Script/Tampering | `<script>`/`<style>` stripped; void elements self-closed; braces escaped | Build (Zod) + count | `npm run build && ls src/content/blog/great-loop/*.mdx \| wc -l` == 72 | ❌ Wave 1: create | ○ |
| Redirect generation | 01 | 1 | MIG-04 | — | — | File count | `grep -c "^/" public/_redirects` == 72 | ❌ Wave 1: generated by import script | ○ |
| Photo URL smoke-check | 01 | 1 | MIG-03 | — | — | HTTP check | `curl -o /dev/null -w "%{http_code}" [10 sample URLs]` all 200 | ❌ Wave 1: add to import script output | ○ |
| Gallery.astro | 02 | 2 | MEDIA-01, MEDIA-03 | — | — | Build + visual | `npm run build && grep -r 'loading="lazy"' dist/` matches | ❌ Wave 2: create | ○ |
| VideoEmbed.astro | 02 | 2 | MEDIA-02 | — | — | Build + visual | `npm run build` + visit post with YouTube embed | ❌ Wave 2: create | ○ |
| Deploy + redirect verify | 03 | 3 | MIG-04, MIG-05 | — | — | Manual | Visit 10 old Blogger URLs; confirm 301 → new Astro URLs | ❌ Wave 3: Netlify deploy | ○ |

---

## Phase Gate Checklist

Before `/gsd:verify-work`:

- [ ] `npm run build` exits 0 with zero errors and zero Zod warnings
- [ ] `ls src/content/blog/great-loop/*.mdx | wc -l` equals 72
- [ ] `grep -c "^/" public/_redirects` equals 72
- [ ] `dist/_redirects` exists after build
- [ ] Visual spot-check: 5 posts render correctly in browser (photos visible, no broken layout)
- [ ] Visual spot-check: 10 old Blogger URLs redirect to correct Astro URLs
- [ ] `jackiebiiigoingloopy.blogspot.com` is still live and accessible (MIG-05)
