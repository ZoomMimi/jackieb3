---
phase: 03-quality-lift
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/07-quality-lift.mjs
  - package.json
  - src/content/blog/great-loop/*.mdx
autonomous: true
requirements: [QLFT-01, QLFT-02, QLFT-03, QLFT-04]
user_setup:
  - service: anthropic
    why: "Claude API drives the HTML→Markdown quality lift of 72 posts"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → Settings → API Keys"
must_haves:
  truths:
    - "Running `node scripts/07-quality-lift.mjs` rewrites raw migrated posts into clean Markdown with H2 sections and no Blogger HTML soup"
    - "Every lifted post gains `lifted: true`, a `coverPhoto`, and a `<VoyageStats />` call at the end of its body"
    - "Re-running the script skips posts already marked `lifted: true` and does not overwrite them"
    - "An API failure on one post logs the slug and continues to the next post"
  artifacts:
    - path: "scripts/07-quality-lift.mjs"
      provides: "Batch quality-lift script using @anthropic-ai/sdk"
      min_lines: 80
    - path: "package.json"
      provides: "quality-lift npm script entry"
      contains: "quality-lift"
  key_links:
    - from: "scripts/07-quality-lift.mjs"
      to: "process.env.ANTHROPIC_API_KEY"
      via: "SDK client init from env var (never hardcoded)"
      pattern: "process\\.env\\.ANTHROPIC_API_KEY"
    - from: "scripts/07-quality-lift.mjs"
      to: "src/content/blog/great-loop/*.mdx"
      via: "read/rewrite each MDX file, inject VoyageStats import + call"
      pattern: "VoyageStats"
---

<objective>
Write `scripts/07-quality-lift.mjs` (D-01, D-02): a standalone ESM Node script that batch-processes all 72 migrated MDX posts through the Claude API, converting Blogger HTML soup to clean Markdown with consistent H2 headings and normalized prose (QLFT-01, QLFT-02), sets `lifted: true` and auto-selects `coverPhoto` (D-04, D-08, D-09), and appends a `<VoyageStats />` footer using matched Nebo log stats (QLFT-03, D-06). Idempotent, skip-and-log, re-runnable (D-03, D-04).

Purpose: Core engine of the quality lift. Enrichment (location, early-post galleries, QLFT-05) is layered on in plan 03-03.
Output: `scripts/07-quality-lift.mjs`, `package.json` script entry, rewritten `*.mdx` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-quality-lift/03-CONTEXT.md
@.planning/phases/03-quality-lift/03-01-SUMMARY.md

<interfaces>
Established pipeline pattern: ESM Node scripts in `scripts/` (package.json `"type": "module"`), read JSON from `.planning/data/`, write to `src/content/blog/great-loop/`. `@anthropic-ai/sdk` v0.52.0 already installed.

SDK usage:
```
import Anthropic from '@anthropic-ai/sdk';
const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
const msg = await client.messages.create({ model, max_tokens, system, messages: [{ role:'user', content }] });
```

nebo-logs.json — array of 171 entries. Parsed fields present on 156 entries:
`date` (YYYY-MM-DD), `distanceNm`, `underwayHours`, `underwayFormatted`, `maxSpeedKts`, `durationHours`, `avgSpeedKts`, `voyageCount`. Map to VoyageStats: miles=distanceNm, hours=underwayHours.

Post MDX shape (raw, from Phase 2): YAML frontmatter (title, date, voyage, location, excerpt, migrated: true) then a body of Blogger HTML (`<div class="separator">`, `<img src="https://blogger.googleusercontent.com/...">`, `<br />`, `&nbsp;`).

MDX component rendering: post pages render `<Content />` with NO components prop, so any MDX using `<VoyageStats>` must include `import VoyageStats from '../../../components/VoyageStats.astro'` at the top of the MDX body (after frontmatter). VoyageStats props: miles, hours, stops, startLocation, endLocation (all optional).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the batch quality-lift script core</name>
  <read_first>scripts/06-inventory-blog.mjs, scripts/03-correlate.mjs, src/content/blog/great-loop/2022-04-16-getting-ready-to-go.mdx, .planning/data/nebo-logs.json</read_first>
  <files>scripts/07-quality-lift.mjs</files>
  <action>Create `scripts/07-quality-lift.mjs` as an ESM script. Behavior:
1. Init the Anthropic client from `process.env.ANTHROPIC_API_KEY` (D-02). If the env var is missing, print a clear error naming the variable and `process.exit(1)` — never hardcode the key.
2. Load `nebo-logs.json` into a Map keyed by `date`.
3. Glob all `src/content/blog/great-loop/*.mdx`. For each: parse YAML frontmatter and body (simple front-matter split on the first two `---` lines; do not add a heavy dependency — hand-parse or reuse the pattern in an existing 0*.mjs script).
4. Idempotency (D-04): if frontmatter `lifted` is already `true`, skip and log `SKIP <slug>`.
5. Auto-select coverPhoto (D-09): first `<img ... src="URL">` URL in the raw body; set `coverPhoto` in frontmatter if found.
6. Call the Claude API (model `claude-sonnet-4-6`, e.g. max_tokens 8000). System prompt instructs: convert Blogger HTML to clean Markdown ONLY — preserve all factual content and Barbara's first-person voice, do not rewrite or summarize prose (specifics); output `## Heading` for section headings, plain paragraphs, and inline photos as `![](url)` keeping them where Blogger placed them (D-05, D-07 blank alt); strip `<div>`, `<span>`, stray `<br>`, and `&nbsp;`/`&amp;` entities; return Markdown body only, no frontmatter, no code fences.
7. Append VoyageStats (D-06, QLFT-03): look up the nebo-logs entry for the post `date` (Claude's discretion for multi-day/date-gap: use the first matching entry, else nearest within the post's date span, else omit). Append at the end of the lifted body a `<VoyageStats miles={<distanceNm>} hours={<underwayHours>} />` call, only including props that exist. Inject `import VoyageStats from '../../../components/VoyageStats.astro'` as the first line of the body (before the first paragraph) so the MDX resolves the component.
8. Set frontmatter `lifted: true`; keep `migrated: true` (D-08). Re-serialize frontmatter + new body and overwrite the .mdx file.
9. Error handling (D-03): wrap each post in try/catch; on failure log `FAIL <slug>: <message>`, push slug to a failures array, continue. At the end write `.planning/data/quality-lift-report.json` with processed/skipped/failed slugs and print a summary line.
Do NOT perform location/lat/lon enrichment or early-post Gallery injection here — that is plan 03-03.</action>
  <verify>
    <automated>node --check scripts/07-quality-lift.mjs && grep -q "process.env.ANTHROPIC_API_KEY" scripts/07-quality-lift.mjs && grep -vi "hardcod" scripts/07-quality-lift.mjs | grep -qE "sk-ant-" && echo FAIL_HARDCODED_KEY || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/07-quality-lift.mjs` passes (valid syntax)
    - `grep -c "process.env.ANTHROPIC_API_KEY" scripts/07-quality-lift.mjs` >= 1 and no literal `sk-ant-` key string appears in the file
    - Script contains an idempotency skip on `lifted === true`
    - Script contains try/catch per-post and writes a report file listing failed slugs
    - Script injects a `VoyageStats` import and a `<VoyageStats .../>` call into lifted bodies
  </acceptance_criteria>
  <done>Script exists, syntactically valid, reads API key from env, idempotent, skip-and-log, writes VoyageStats footer.</done>
</task>

<task type="auto">
  <name>Task 2: Run the lift on all posts and verify a clean build</name>
  <read_first>package.json, scripts/07-quality-lift.mjs</read_first>
  <files>package.json, src/content/blog/great-loop/*.mdx</files>
  <action>Add a `"quality-lift": "node scripts/07-quality-lift.mjs"` entry to package.json `scripts`. Execute `ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npm run quality-lift` to process all 72 posts (the executor supplies the key from env per user_setup; if the key is absent the execute-plan flow surfaces an auth checkpoint). After the run, inspect `.planning/data/quality-lift-report.json`: if any slugs failed, re-run the script (idempotent — only failed posts reprocess) up to 2 more times. Then run `npm run build` and confirm it exits 0. Spot-check 3 lifted posts to confirm: `lifted: true` in frontmatter, `coverPhoto` set, no `<div class="separator">` / `&nbsp;` artifacts remain, and a `<VoyageStats />` call plus its import are present.</action>
  <verify>
    <automated>npm run build 2>&1 | grep -qiE "error" && echo FAIL || echo PASS; grep -rl "lifted: true" src/content/blog/great-loop/*.mdx | wc -l; grep -rlc 'class="separator"' src/content/blog/great-loop/*.mdx | grep -c ':[1-9]' </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/data/quality-lift-report.json` exists with a processed count and a (possibly empty) failed list
    - `grep -rl "lifted: true" src/content/blog/great-loop/*.mdx | wc -l` equals the number of successfully processed posts (target 72 minus any permanently-failing slugs, which are documented in the report)
    - No lifted post body contains `class="separator"` or literal `&nbsp;` (grep returns 0 matches across lifted files)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All processable posts are lifted with clean Markdown, cover photos, and VoyageStats footers; build is clean; failures (if any) are logged for retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → Anthropic API | API key crosses to a third-party service; must come from env, never source |
| Claude API response → MDX files | LLM output is written into site content that later renders in browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | scripts/07-quality-lift.mjs | mitigate | API key read only from `process.env.ANTHROPIC_API_KEY`; exit(1) if missing; no literal key in source (grep gate in verify). Script is git-tracked, key is not. |
| T-03-04 | Tampering | Claude API response → MDX | mitigate | System prompt forbids fact rewriting; output written to git-tracked files reviewed on the live site by Barbara (plan 03-04). Astro escapes Markdown at build; raw HTML is stripped by the lift prompt. |
| T-03-05 | Denial of Service | Anthropic API rate limits | accept | Skip-and-log (D-03) + re-runnable idempotent design absorbs rate-limit failures without data loss. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — `@anthropic-ai/sdk` v0.52.0 already in package.json. No install task; no supply-chain surface added. |
</threat_model>

<verification>
- `node --check` passes; no hardcoded key.
- Report file lists processed/skipped/failed.
- Lifted posts carry `lifted: true`, coverPhoto, VoyageStats; no Blogger HTML artifacts.
- `npm run build` exits 0.
</verification>

<success_criteria>
All 72 processable posts converted to clean Markdown with consistent H2 headings, normalized prose, cover photos, `lifted: true`, and a VoyageStats footer. Script is idempotent and re-runnable. Build clean.
</success_criteria>

<output>
Create `.planning/phases/03-quality-lift/03-02-SUMMARY.md` when done.
</output>
