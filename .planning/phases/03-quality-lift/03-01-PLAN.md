---
phase: 03-quality-lift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/components/VoyageStats.astro
autonomous: true
requirements: [QLFT-03, QLFT-04]
must_haves:
  truths:
    - "The blog schema accepts a `lifted` boolean field and optional voyage-stats fields without validation errors"
    - "A post that calls <VoyageStats /> with no props renders no visible box"
    - "A post that calls <VoyageStats miles={28.7} hours={2.5} /> renders a stats footer showing those values"
  artifacts:
    - path: "src/content.config.ts"
      provides: "lifted flag + optional miles/hours/stops/startLocation/endLocation fields"
      contains: "lifted"
    - path: "src/components/VoyageStats.astro"
      provides: "Voyage stats footer component with optional props"
      min_lines: 20
  key_links:
    - from: "src/components/VoyageStats.astro"
      to: "Astro.props"
      via: "optional prop destructuring with empty-state guard"
      pattern: "Astro\\.props"
---

<objective>
Add the `lifted` frontmatter flag (D-08) and the optional VoyageStats prop fields to the Zod schema, and create the `VoyageStats.astro` component (D-06) that renders a voyage-stats footer from optional props.

Purpose: Establishes the schema contract and the footer component that the quality-lift script (plan 03-02) writes `<VoyageStats ... />` calls against. Building this first prevents downstream build failures.
Output: Updated `src/content.config.ts`, new `src/components/VoyageStats.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-quality-lift/03-CONTEXT.md

<interfaces>
Current schema (src/content.config.ts) blog collection already has:
title, date, voyage, location, excerpt, migrated (required);
lat, lon, coverPhoto, anchorage, marina (optional).

Existing component prop pattern (src/components/Gallery.astro):
```
const { images, caption = '' } = Astro.props;
```
BlogPost layout (.post-body) styles h2/h3/p/hr; VoyageStats renders inside the MDX body via <slot />. Post pages render <Content /> WITHOUT a components prop, so MDX files must import components themselves — VoyageStats must be importable from `../../../components/VoyageStats.astro` relative to a post file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend blog schema with lifted flag and voyage-stats fields</name>
  <read_first>src/content.config.ts</read_first>
  <files>src/content.config.ts</files>
  <action>In the `blog` collection `schema` object, add per D-08 `lifted: z.boolean().default(false)`. Add optional voyage-stats fields (D-06) so frontmatter can carry them without validation errors: `miles: z.number().optional()`, `hours: z.number().optional()`, `stops: z.number().optional()`, `startLocation: z.string().optional()`, `endLocation: z.string().optional()`. Do NOT remove or alter `migrated` — it stays permanently (D-08). Do not touch the `voyages` collection.</action>
  <verify>
    <automated>npm run build 2>&1 | grep -qiE "error|warning" && echo FAIL || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lifted: z.boolean().default(false)" src/content.config.ts` returns 1
    - All five optional fields (miles, hours, stops, startLocation, endLocation) present as `.optional()` in the blog schema
    - `migrated` field still present unchanged
    - `npm run build` exits 0 with zero schema validation warnings
  </acceptance_criteria>
  <done>Schema compiles; existing 72 posts (which lack `lifted`) still validate because `lifted` has a default and new fields are optional.</done>
</task>

<task type="auto">
  <name>Task 2: Create VoyageStats.astro footer component</name>
  <read_first>src/components/Gallery.astro, src/layouts/BlogPost.astro</read_first>
  <files>src/components/VoyageStats.astro</files>
  <action>Create `src/components/VoyageStats.astro` per D-06. Props interface (all optional): `miles?: number`, `hours?: number`, `stops?: number`, `startLocation?: string`, `endLocation?: string`. Destructure from `Astro.props`. Compute a `hasData` guard: true if any prop is defined. If `!hasData`, render nothing (return null equivalent — emit no markup). When data exists, render a footer block: a labeled row for each defined stat only (skip undefined ones) — e.g. "Distance: {miles} nm", "Underway: {hours} hrs", "Stops: {stops}", and a route line "{startLocation} → {endLocation}" when both locations exist. Use a scoped `<style>` consistent with the nautical theme (border-top using var(--color-navy), font-family var(--font-inter), muted labels). Do NOT show placeholder text like "No stats" when empty (D-06, specifics).</action>
  <verify>
    <automated>test -f src/components/VoyageStats.astro && grep -qi "Astro.props" src/components/VoyageStats.astro && npm run build 2>&1 | grep -qiE "error" && echo FAIL || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/VoyageStats.astro` exists
    - Component reads all five props from `Astro.props` with optional typing
    - Contains an explicit empty-state guard that emits no markup when no props are provided
    - Only defined stats render (undefined props produce no row)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Component renders a stats footer when given props and nothing when given none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time frontmatter → component props | Post frontmatter/MDX values flow into VoyageStats; all authored locally, no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | src/content.config.ts | accept | Schema is local static config; only trusted local authors edit it. No external input. |
| T-03-02 | Information Disclosure | VoyageStats.astro | accept | Component renders only voyage stat numbers/place names — no secrets or PII. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (`@anthropic-ai/sdk` already present). No install task, so no supply-chain surface added. |
</threat_model>

<verification>
- `npm run build` exits 0 with zero schema warnings after both tasks.
- Grep confirms `lifted` and the five optional fields exist in the schema.
- VoyageStats file exists and guards the empty state.
</verification>

<success_criteria>
Schema accepts `lifted` + optional stat fields; VoyageStats component renders conditionally on props; build is clean. Contract is ready for plan 03-02 to write `<VoyageStats />` calls.
</success_criteria>

<output>
Create `.planning/phases/03-quality-lift/03-01-SUMMARY.md` when done.
</output>
