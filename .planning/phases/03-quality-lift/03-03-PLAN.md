---
phase: 03-quality-lift
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/07-quality-lift.mjs
  - src/content/blog/great-loop/*.mdx
autonomous: true
requirements: [QLFT-05, QLFT-03]
must_haves:
  truths:
    - "Each post's `location` is updated from the voyage timeline to a real place name when a timeline entry matches its date (D-10)"
    - "Posts with a matching timeline day gain `lat`/`lon` frontmatter"
    - "Early posts (Days 1–111, Apr–Aug 2022) with correlated iCloud photos gain a <Gallery images={[...localPaths]} /> placeholder at the end of the body (D-12)"
    - "Posts with no timeline match keep their existing location unchanged"
  artifacts:
    - path: "scripts/07-quality-lift.mjs"
      provides: "Enrichment pass added: location/lat/lon + early-post Gallery placeholder"
      contains: "voyage-timeline-enriched"
  key_links:
    - from: "scripts/07-quality-lift.mjs"
      to: ".planning/data/voyage-timeline-enriched.json"
      via: "date-matched lookup into days[] for location/lat/lon/photos"
      pattern: "voyage-timeline-enriched"
    - from: "scripts/07-quality-lift.mjs"
      to: "src/content/blog/great-loop/*.mdx (early posts)"
      via: "append Gallery import + <Gallery images={[...]} /> call"
      pattern: "Gallery"
---

<objective>
Extend `scripts/07-quality-lift.mjs` with a frontmatter-enrichment pass (D-10, D-11, QLFT-05): update `location` and add `lat`/`lon` from `voyage-timeline-enriched.json` matched by post date, and for early posts (Days 1–111, Apr–Aug 2022) with correlated iCloud photos append a `<Gallery images={[...localPaths]} />` placeholder (D-12). Also enrich the VoyageStats footer with startLocation/endLocation/stops where the timeline provides them (QLFT-03).

Purpose: Brings early posts toward the richness of later posts using already-built pipeline output. Phase 6 later resolves real photo hosting; this establishes the structure with local-path placeholders.
Output: Updated `scripts/07-quality-lift.mjs`, re-lifted `*.mdx` files with enriched frontmatter and early-post galleries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-quality-lift/03-CONTEXT.md
@.planning/phases/03-quality-lift/03-02-SUMMARY.md

<interfaces>
voyage-timeline-enriched.json shape:
```
{ generated, stats, days: [ {
    date: "2022-04-01", location: "New Bern NC",
    centroidLat: 35.103, centroidLon: -77.0391,
    milesFromPrev, photoCount, withGps,
    photos: [ { uuid, filename, directory, kind, ts, lat, lon }, ... ]
} ], segments }
```
Match a post to a day via `date`. location → frontmatter `location` (D-10); centroidLat/centroidLon → `lat`/`lon`. Photo local placeholder path (Claude's discretion, D-12) e.g. `file:///Users/bruhnhome/Pictures/Photos Library.photoslibrary/originals/{directory}/{filename}`.

Early-post window (QLFT-05): post `date` between 2022-04-16 and 2022-08-04 (Days 1–111). Gallery component (src/components/Gallery.astro) props: `images: string[]`, `caption?`. Import path from a post: `../../../components/Gallery.astro`.

D-04 idempotency already skips `lifted: true` posts. Because this pass must run over already-lifted posts, gate the enrichment on a separate `enriched` frontmatter flag (add `enriched: true` after enrichment) so re-runs skip completed enrichment without un-skipping the whole post. Keep changes additive; never remove Barbara's manual edits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enrichment pass to the lift script</name>
  <read_first>scripts/07-quality-lift.mjs, .planning/data/voyage-timeline-enriched.json, src/content.config.ts</read_first>
  <files>scripts/07-quality-lift.mjs</files>
  <action>Add an enrichment function to `scripts/07-quality-lift.mjs` that runs on each post (invoked in the same per-post loop, after the structural lift; safe for already-lifted posts):
1. Load `.planning/data/voyage-timeline-enriched.json` once; build a Map of `days` keyed by `date`.
2. Idempotency: if frontmatter `enriched` is already `true`, skip enrichment for that post (log `ENRICH-SKIP <slug>`).
3. Date match (D-10, D-11): if a timeline day matches the post `date`: set frontmatter `location` to `day.location` (only when day.location is a non-empty place name — otherwise leave existing location, per D-10); set `lat = day.centroidLat`, `lon = day.centroidLon`.
4. VoyageStats enrichment (QLFT-03): where the timeline/nebo data yields them, pass `stops`, `startLocation`, `endLocation` into the existing `<VoyageStats ... />` call in the body (update the existing call rather than adding a second). Only include props that have values.
5. Early-post Gallery (D-12, QLFT-05): if post `date` is between 2022-04-16 and 2022-08-04 AND the matched day has photos, build an array of local placeholder path strings (one per photo, Claude's discretion on format using `directory`+`filename`). Append at the end of the body a `<Gallery images={[...]} />` call and inject `import Gallery from '../../../components/Gallery.astro'` at the top of the body if not already imported. Cap the array to a reasonable number (e.g. first 20 photos) to keep files manageable.
6. Set frontmatter `enriched: true`; overwrite the file.
Reuse the existing skip-and-log + report mechanics (D-03); add enriched/enrich-skipped counts to `quality-lift-report.json`.</action>
  <verify>
    <automated>node --check scripts/07-quality-lift.mjs && grep -q "voyage-timeline-enriched" scripts/07-quality-lift.mjs && grep -q "enriched" scripts/07-quality-lift.mjs && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/07-quality-lift.mjs` passes
    - Script reads `.planning/data/voyage-timeline-enriched.json`
    - Script gates enrichment on an `enriched` flag for idempotency
    - Script sets location/lat/lon from date-matched timeline days and only overrides location when a place name exists (D-10)
    - Script appends a `<Gallery images={[...]} />` placeholder + import only for posts dated 2022-04-16..2022-08-04 with photos
  </acceptance_criteria>
  <done>Enrichment logic added, idempotent via `enriched` flag, additive to lifted output.</done>
</task>

<task type="auto">
  <name>Task 2: Run enrichment and verify early posts + build</name>
  <read_first>scripts/07-quality-lift.mjs</read_first>
  <files>src/content/blog/great-loop/*.mdx</files>
  <action>Execute `ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npm run quality-lift` again (already-lifted posts skip the API call; enrichment runs). Inspect `.planning/data/quality-lift-report.json` for enriched counts and failures. Run `npm run build` and confirm exit 0. Spot-check: (a) at least 3 posts across the voyage now have real `location` place names and `lat`/`lon`; (b) at least 3 early posts (Apr–Aug 2022) contain a `<Gallery images={[...]} />` call and a Gallery import; (c) VoyageStats calls include start/end locations where available.</action>
  <verify>
    <automated>npm run build 2>&1 | grep -qiE "error" && echo FAIL || echo PASS; grep -rl "enriched: true" src/content/blog/great-loop/*.mdx | wc -l; grep -rl "<Gallery images" src/content/blog/great-loop/2022-0[4-8]*.mdx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "enriched: true" src/content/blog/great-loop/*.mdx | wc -l` > 0 (posts with timeline matches enriched)
    - `grep -rl "<Gallery images" src/content/blog/great-loop/2022-0[4-8]*.mdx | wc -l` > 0 (early posts with photos get placeholder galleries)
    - At least 3 posts have `lat:`/`lon:` frontmatter added
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Frontmatter enriched with location/lat/lon; early posts carry Gallery placeholders; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline JSON → MDX frontmatter | Timeline data (locally generated) flows into post frontmatter and Gallery placeholder paths |
| local file paths → Gallery src | `file:///` placeholder paths are non-web and render nothing useful; replaced in Phase 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | file:/// paths in Gallery placeholders | accept | Placeholder paths expose only local library structure in git; no secrets. Phase 6 replaces them with hosted URLs before public relevance. |
| T-03-07 | Tampering | timeline JSON → location/lat/lon | mitigate | Data is locally generated pipeline output; enrichment only overrides location when a non-empty place name exists (D-10), preventing blanking. Barbara reviews live site (plan 03-04). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed. No install task; no supply-chain surface added. |
</threat_model>

<verification>
- `node --check` passes; script reads timeline JSON and gates on `enriched`.
- Enriched posts have place-name location + lat/lon; early posts have Gallery placeholders.
- `npm run build` exits 0.
</verification>

<success_criteria>
Early posts (Days 1–111) and all date-matched posts are enriched with real locations, GPS coordinates, richer VoyageStats, and (for early posts with photos) Gallery placeholders — using existing pipeline output. Idempotent and build-clean. QLFT-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-quality-lift/03-03-SUMMARY.md` when done.
</output>
