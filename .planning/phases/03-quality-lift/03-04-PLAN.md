---
phase: 03-quality-lift
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - .planning/data/quality-lift-verification.json
autonomous: false
requirements: [QLFT-01, QLFT-02, QLFT-03, QLFT-04, QLFT-05]
must_haves:
  truths:
    - "Every post has both `migrated: true` and `lifted: true` in frontmatter (D-08)"
    - "No lifted post body contains Blogger HTML artifacts (<div>, <span>, stray <br>, &nbsp;, &amp;)"
    - "Every post renders a consistent layout: H1 title, H2 sections, and a VoyageStats footer where stats exist"
    - "Early posts (Days 1–111) show enriched location/GPS and Gallery placeholders where photo data exists"
    - "Barbara has reviewed the live site and reported issues or approved"
  artifacts:
    - path: ".planning/data/quality-lift-verification.json"
      provides: "Automated verification results across all 72 posts"
      min_lines: 5
  key_links:
    - from: "verification script/checks"
      to: "src/content/blog/great-loop/*.mdx"
      via: "grep-based artifact + flag audit across all posts"
      pattern: "lifted: true"
---

<objective>
Verify Phase 3 output across all 72 posts and secure Barbara's human review (D-01). Automated checks confirm: every post is lifted (QLFT-04), no Blogger HTML artifacts remain (QLFT-01/QLFT-02), consistent heading + VoyageStats layout (QLFT-03), and early posts are enriched (QLFT-05). Then a human-verify checkpoint has Barbara review the live/preview site and report issues.

Purpose: Goal-backward verification of the phase success criteria plus the mandatory human review pass.
Output: `.planning/data/quality-lift-verification.json`, Barbara's approval or issue list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-quality-lift/03-CONTEXT.md
@.planning/phases/03-quality-lift/03-02-SUMMARY.md
@.planning/phases/03-quality-lift/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification audit across all posts</name>
  <read_first>.planning/data/quality-lift-report.json, .planning/ROADMAP.md</read_first>
  <files>.planning/data/quality-lift-verification.json</files>
  <action>Produce an automated audit and write results to `.planning/data/quality-lift-verification.json`. Run these checks against `src/content/blog/great-loop/*.mdx` (use grep/node one-liners, no new dependency):
1. Flag audit (QLFT-04): count posts with `lifted: true` and count posts with `migrated: true`. Any post missing `lifted: true` is a failure (list the slugs) — reconcile against the failed list in `quality-lift-report.json`.
2. Artifact audit (QLFT-01, QLFT-02): grep all lifted bodies for Blogger HTML soup — `class="separator"`, `<span style`, `&nbsp;`, `&amp;`, and stray `<br`. Any match is a failure; record slug + pattern.
3. Layout audit (QLFT-03): count posts containing a `<VoyageStats` call and (where the post has a matched nebo log) at least one `##` H2 heading. Record posts with stats data but no VoyageStats call.
4. Enrichment audit (QLFT-05): count early posts (2022-04-16..2022-08-04) that have `lat:`/`lon:` and a `<Gallery images` placeholder where the timeline day has photos.
5. `npm run build` must exit 0.
Write a JSON summary with pass/fail per check and any failing slugs. Print a concise pass/fail table.</action>
  <verify>
    <automated>npm run build 2>&1 | grep -qiE "error" && echo FAIL || echo PASS; test -f .planning/data/quality-lift-verification.json && node -e "const v=require('./.planning/data/quality-lift-verification.json'); process.exit(v.artifactFailures&&v.artifactFailures.length?1:0)" && echo CLEAN || echo ARTIFACTS_REMAIN