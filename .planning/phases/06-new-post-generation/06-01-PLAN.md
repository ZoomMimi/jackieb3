---
phase: 06-new-post-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/content.config.ts
  - scripts/verify-phase6.mjs
  - scripts/04-generate-stubs.mjs
  - src/content/blog/great-loop/*.mdx
autonomous: true
requirements: [POST-01, POST-02]

descoped_requirements:
  - id: POST-04
    reason: "Out of scope per 06-CONTEXT.md D-05 — the 'undocumented middle' (Days 112-124, Holland MI Aug 2022, rest of Jan 2023-Apr 2024) was dropped in the 2026-07-25 rescoping session. No tasks in this phase address it. Plan 06-06 Task 3 flags it in REQUIREMENTS.md for movement to Out of Scope at the next /gsd:transition."
  - id: QLFT-05
    reason: "Effectively reversed per 06-CONTEXT.md D-00 — the 72 original Blogger posts are frozen and their Phase-4 Gallery blocks were removed in commit fe69136. Flagged alongside POST-04 in Plan 06-06 Task 3."

user_setup: []

must_haves:
  truths:
    - "Running `npm run verify-phase6` prints an accurate in-scope coverage report for exactly the two in-scope date ranges, never counting the ~187 out-of-scope draft stubs"
    - "Every in-scope day that has correlated photo data has a draft MDX post file (40 previously-missing days now have one)"
    - "The content collection schema accepts the r2Uploaded and narrativeDrafted pipeline flags without stripping them"
    - "`npm run build` completes with zero errors after the 40 new stubs are generated"
  artifacts:
    - path: "scripts/verify-phase6.mjs"
      provides: "Scoped Wave-0 verification: in-scope date coverage, remaining file:// URLs, remaining draft:true, r2Uploaded/narrativeDrafted counts, exit-code gates"
      min_lines: 120
    - path: ".env.example"
      provides: "Documented names (no values) of the R2 + Anthropic env vars this phase needs"
      contains: "R2_PUBLIC_BASE"
    - path: "src/content.config.ts"
      provides: "r2Uploaded and narrativeDrafted optional boolean flags in the blog collection schema"
      contains: "r2Uploaded"
    - path: "scripts/04-generate-stubs.mjs"
      provides: "FORCE_STUB_DATES allow-list bypassing the photoCount<10 filter for exactly the 40 named dates"
      contains: "FORCE_STUB_DATES"
  key_links:
    - from: "scripts/verify-phase6.mjs"
      to: "src/data/daily-routes.json"
      via: "in-scope date derivation (keys within the two ranges)"
      pattern: "daily-routes\\.json"
    - from: "scripts/04-generate-stubs.mjs"
      to: "voyage-timeline-enriched.json day.photoCount filter"
      via: "FORCE_STUB_DATES.has(day.date) override"
      pattern: "FORCE_STUB_DATES\\.has"
    - from: "package.json"
      to: "scripts/verify-phase6.mjs"
      via: "npm run verify-phase6"
      pattern: "verify-phase6"
---

<objective>
Lay the Phase 6 foundation: the verification tooling that every later plan gates on, the schema/env/npm contracts the two new pipeline scripts will use, and the 40 missing draft stubs that unblock all downstream work.

Purpose: Without a *scoped* verification script, a repo-wide `grep 'draft: true'` false-positives against the ~187 explicitly out-of-scope stubs (D-05) and can never report "done". Without the 40 missing stubs, 40% of the in-scope days have nothing for the R2 upload or narrative scripts to operate on.

Output: `scripts/verify-phase6.mjs`, `.env.example`, three npm scripts, two new schema flags, one `@aws-sdk/client-s3` dependency, and 40 new `draft: true` MDX stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-new-post-generation/06-CONTEXT.md
@.planning/phases/06-new-post-generation/06-PATTERNS.md
@.planning/phases/06-new-post-generation/06-VALIDATION.md
@scripts/04-generate-stubs.mjs
@src/content.config.ts

<interfaces>
<!-- Contracts established by this plan and consumed by plans 06-02 through 06-06. -->
<!-- Executor: these are the exact identifiers downstream plans expect. Do not rename. -->

Two in-scope date ranges (used identically by every Phase 6 script):
- keys-to-new-bern: 2024-04-13 .. 2024-05-17 (inclusive)
- canada-side-trip: 2023-06-05 .. 2023-08-31 (inclusive)

In-scope date set (authoritative definition, matches 06-CONTEXT.md's 101 candidate days):
- the keys of `src/data/daily-routes.json` that fall within either range == 101 dates
- of those, `2023-08-09` is a KNOWN GAP: it has a GPS track in daily-routes.json but
  photoCount 0 in voyage-timeline-enriched.json, so no Gallery is possible and no stub
  can be generated. It is deliberately absent from 06-CONTEXT.md's 40-date list.
  Expected post coverage is therefore 100 of 101 dates.

New frontmatter flags (added to src/content.config.ts by this plan):
- r2Uploaded: boolean, default false      — set true by scripts/10-upload-r2.mjs (plan 06-02/06-04)
- narrativeDrafted: boolean, default false — set true by scripts/11-draft-narratives.mjs (plan 06-05)

New npm scripts (added by this plan):
- verify-phase6    -> node scripts/verify-phase6.mjs
- upload-r2        -> node --env-file-if-exists=.env scripts/10-upload-r2.mjs
- draft-narratives -> node --env-file-if-exists=.env scripts/11-draft-narratives.mjs

Environment variables (documented in .env.example by this plan, values supplied in plan 06-02):
- R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, R2_PUBLIC_BASE, ANTHROPIC_API_KEY

verify-phase6 output contract (stdout, one key=value per line — downstream plans grep these):
- IN_SCOPE_DATES=<n>
- KNOWN_GAPS=<n>
- MISSING_POSTS=<n>
- FILE_URL_POSTS=<n>
- DRAFT_POSTS=<n>
- R2_UPLOADED_POSTS=<n>
- NARRATIVE_DRAFTED_POSTS=<n>
Gate names accepted by --gate (repeatable): posts | no-file-urls | no-drafts
Gate output: GATE_PASS <name> or GATE_FAIL <name>; process exits 1 if any named gate fails.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish dependency, env, npm-script, and schema contracts</name>
  <files>package.json, package-lock.json, .env.example, src/content.config.ts</files>
  <read_first>
    - package.json (current dependency list and scripts block)
    - src/content.config.ts (current blog collection Zod schema — note the `lifted` flag placement)
    - .gitignore (confirm `.env` and `.env.production` are already excluded)
    - .planning/phases/06-new-post-generation/06-RESEARCH.md § "Package Legitimacy Audit" and § "Environment Availability"
  </read_first>
  <action>
Install the one new dependency and record the contracts every later Phase 6 plan depends on.

1. Run `npm install @aws-sdk/client-s3`. This package was already legitimacy-audited in 06-RESEARCH.md § Package Legitimacy Audit with verdict `[OK]` (official AWS SDK v3, github.com/aws/aws-sdk-js-v3). Do NOT run `slopcheck install` against it — 06-RESEARCH.md documents that `slopcheck install` performs a real install and mutates package.json/package-lock.json.

2. Add three scripts to the `scripts` block in package.json, keeping the existing entries untouched:
   - `"verify-phase6": "node scripts/verify-phase6.mjs"`
   - `"upload-r2": "node --env-file-if-exists=.env scripts/10-upload-r2.mjs"`
   - `"draft-narratives": "node --env-file-if-exists=.env scripts/11-draft-narratives.mjs"`
   `--env-file-if-exists` is used (not `--env-file`) so the scripts still run when `.env` is absent; verified working on this machine's Node v24.14.0.

3. Create `.env.example` at the repo root containing ONLY variable names with empty or placeholder values and short comments — never real secrets. Variables: `ANTHROPIC_API_KEY`, `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_PUBLIC_BASE`. Note in a comment that `R2_PUBLIC_BASE` is the bucket's public base URL with no trailing slash (the `https://pub-<hash>.r2.dev` form), and that `.env` itself is gitignored and must never be committed.

4. In `src/content.config.ts`, add two optional boolean flags to the `blog` collection schema immediately after the existing `lifted` field, following the same `z.boolean().default(false)` style and comment convention already used there:
   - `r2Uploaded` — comment it as "Phase 6 D-01: set true by scripts/10-upload-r2.mjs once every media file for the post has uploaded to R2"
   - `narrativeDrafted` — comment it as "Phase 6: set true by scripts/11-draft-narratives.mjs after an AI narrative is written into the body"
   Do not reorder or modify any existing field. Both flags are needed explicitly because 06-PATTERNS.md flags the risk that Zod's non-strict object parsing silently drops unknown keys.
  </action>
  <verify>
    <automated>node -e "import('@aws-sdk/client-s3').then(m=>{if(!m.S3Client||!m.PutObjectCommand)process.exit(1);console.log('sdk ok')})" && node -e "const p=require('./package.json');for(const s of ['verify-phase6','upload-r2','draft-narratives'])if(!p.scripts[s]){console.error('missing script '+s);process.exit(1)};if(!p.dependencies['@aws-sdk/client-s3']){console.error('missing dep');process.exit(1)};console.log('package.json ok')" && grep -q 'R2_PUBLIC_BASE' .env.example && grep -q 'r2Uploaded' src/content.config.ts && grep -q 'narrativeDrafted' src/content.config.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('@aws-sdk/client-s3')..."` prints `sdk ok` (S3Client and PutObjectCommand both resolvable)
    - `package.json` `dependencies` contains `@aws-sdk/client-s3`; `scripts` contains all three of `verify-phase6`, `upload-r2`, `draft-narratives`
    - `.env.example` exists and contains all six variable names: ANTHROPIC_API_KEY, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, R2_PUBLIC_BASE
    - `.env.example` contains no value longer than a placeholder — `git diff --cached` / the file body contains no string matching a real key shape (no 32+ char alphanumeric secrets)
    - `src/content.config.ts` blog schema contains `r2Uploaded: z.boolean().default(false)` and `narrativeDrafted: z.boolean().default(false)`
    - `git check-ignore .env` exits 0 (still gitignored)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The dependency, env-var names, npm entry points, and two pipeline frontmatter flags exist and the site still builds.</done>
</task>

<task type="auto">
  <name>Task 2: Create the scoped Phase-6 verification script</name>
  <files>scripts/verify-phase6.mjs</files>
  <read_first>
    - scripts/04-generate-stubs.mjs lines 43-101 (the splitFrontmatter/parseFrontmatter/serializeFrontmatter trio to copy verbatim — this project duplicates these per-script rather than sharing a module; follow that convention)
    - scripts/04-generate-stubs.mjs lines 23-41 (header/const/flag layout to mirror)
    - .planning/phases/06-new-post-generation/06-VALIDATION.md § "Wave 0 Requirements"
    - src/data/daily-routes.json (structure: top-level object keyed by YYYY-MM-DD)
  </read_first>
  <action>
Create `scripts/verify-phase6.mjs`, an ESM Node script that reports Phase-6 completion state for exactly the two in-scope date ranges and nothing else.

Structure and conventions: copy the header doc-comment style, `__dirname`/`ROOT`/`DATA_DIR`/`POSTS_DIR` const block, and the `splitFrontmatter`/`parseFrontmatter` functions verbatim from `scripts/04-generate-stubs.mjs`. `serializeFrontmatter` is not needed (this script never writes MDX).

Constants:
- `RANGES` = two entries: `{ name: 'keys-to-new-bern', start: '2024-04-13', end: '2024-05-17' }` and `{ name: 'canada-side-trip', start: '2023-06-05', end: '2023-08-31' }`.
- `KNOWN_GAP_DATES` = a Map with a single entry `'2023-08-09'` -> reason string "GPS track present in daily-routes.json but photoCount 0 in voyage-timeline-enriched.json — no photos exist, so no Gallery/stub is possible; excluded from 06-CONTEXT.md D-04's 40-date list".

Derivation:
- Read `src/data/daily-routes.json`; the in-scope date set is its keys falling inside either range (string comparison is safe for ISO dates). Expect 101.
- Read the filenames in `src/content/blog/great-loop/`; a date is "covered" if any `.mdx` filename starts with that date string.

Checks to compute:
- `missingPosts`: in-scope dates, excluding KNOWN_GAP_DATES, with no covering `.mdx` file.
- `fileUrlPosts`: in-scope `.mdx` files whose full text contains the substring `file://`.
- `draftPosts`: in-scope `.mdx` files whose parsed frontmatter has `draft === true`.
- `r2UploadedPosts`: in-scope `.mdx` files with `r2Uploaded === true`.
- `narrativeDraftedPosts`: in-scope `.mdx` files with `narrativeDrafted === true`.

Output (stdout), one `KEY=<n>` per line in this exact order and spelling, because later plans grep for them: `IN_SCOPE_DATES`, `KNOWN_GAPS`, `IN_SCOPE_POSTS`, `MISSING_POSTS`, `FILE_URL_POSTS`, `DRAFT_POSTS`, `R2_UPLOADED_POSTS`, `NARRATIVE_DRAFTED_POSTS`. Follow with a per-range breakdown and, when non-empty, the actual date/slug lists for `missingPosts`, `fileUrlPosts`, and `draftPosts` (cap each printed list at 20 entries with a "... and N more" suffix). Always print the known-gap dates with their reason.

Also write the full result object to `.planning/data/phase6-verify.json` with shape `{ generated, ranges, inScopeDates, knownGaps, counts: {...}, missingPosts, fileUrlPosts, draftPosts, r2UploadedPosts, narrativeDraftedPosts }`.

CLI: accept `--gate <name>` repeatably. Gate names and their pass conditions: `posts` -> `missingPosts.length === 0`; `no-file-urls` -> `fileUrlPosts.length === 0`; `no-drafts` -> `draftPosts.length === 0`. For each requested gate print `GATE_PASS <name>` or `GATE_FAIL <name>`. Exit 1 if any requested gate failed, otherwise 0. With no `--gate` argument the script is report-only and always exits 0. Reject an unknown gate name with a clear error and exit 2.

Do not use a repo-wide grep anywhere in this script — every check must be filtered to the in-scope date set first, or it will false-positive against the ~187 out-of-scope draft stubs (06-CONTEXT.md D-05).
  </action>
  <verify>
    <automated>node scripts/verify-phase6.mjs | grep -E '^IN_SCOPE_DATES=101$' && node scripts/verify-phase6.mjs | grep -E '^KNOWN_GAPS=1$' && node scripts/verify-phase6.mjs | grep -E '^MISSING_POSTS=40$' && node scripts/verify-phase6.mjs --gate posts; test $? -eq 1 && echo "gate correctly fails pre-stub-generation" && test -f .planning/data/phase6-verify.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase6.mjs` exits 0 and prints `IN_SCOPE_DATES=101`
    - Output includes `KNOWN_GAPS=1` and names `2023-08-09` with its documented reason
    - Before Task 3 runs, output includes `MISSING_POSTS=40` (the exact count from 06-CONTEXT.md D-04)
    - Before Task 3 runs, output includes `FILE_URL_POSTS=60` and `DRAFT_POSTS=60` (the 60 in-scope posts that already exist)
    - `node scripts/verify-phase6.mjs --gate posts` prints `GATE_FAIL posts` and exits 1 (correct pre-Task-3 state)
    - `node scripts/verify-phase6.mjs --gate bogus` exits 2 with an error naming the valid gate names
    - `.planning/data/phase6-verify.json` exists and its `counts.inScopeDates` equals 101
    - `grep -v '^#' scripts/verify-phase6.mjs | grep -c 'daily-routes.json'` is at least 1 (derivation reads the authoritative in-scope source, not a hardcoded date list)
  </acceptance_criteria>
  <done>A single command reports Phase-6 progress scoped to the two in-scope ranges, with machine-greppable counters and exit-code gates that later plans use as their automated verification.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the 40 missing in-scope stubs</name>
  <files>scripts/04-generate-stubs.mjs, src/content/blog/great-loop/*.mdx</files>
  <read_first>
    - scripts/04-generate-stubs.mjs (whole file — Task 3 changes exactly one guard on line 271, and everything downstream is reused unmodified)
    - .planning/phases/06-new-post-generation/06-PATTERNS.md § "scripts/04-generate-stubs.mjs (utility, CRUD) — EXTEND" (contains the verbatim 40-date list and exact insertion point)
    - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx (the exact stub output shape the 40 new files must match)
  </read_first>
  <action>
Add a date allow-list to `scripts/04-generate-stubs.mjs` that bypasses the photo-count filter for exactly 40 named dates, then run the generator.

Root cause (confirmed in 06-RESEARCH.md, not inferred): line 270-271 of the script reads `// D-01/D-02: skip if fewer than 10 photos` / `if (day.photoCount < 10) continue;`. All 40 target dates have `photoCount` between 1 and 9 in `voyage-timeline-enriched.json`, so they were silently skipped.

1. Declare a `const FORCE_STUB_DATES = new Set([...])` near the top of the file, immediately after the `DRY` flag block (around line 41), with a comment referencing 06-CONTEXT.md D-04. The set contains exactly these 40 date strings and no others:
   Keys->New Bern (9): 2024-04-14, 2024-04-15, 2024-04-23, 2024-04-29, 2024-05-03, 2024-05-08, 2024-05-09, 2024-05-10, 2024-05-14
   Canada side trip (31): 2023-06-06, 2023-06-08, 2023-06-11, 2023-06-12, 2023-06-13, 2023-06-15, 2023-06-17, 2023-06-20, 2023-06-22, 2023-06-25, 2023-06-26, 2023-06-28, 2023-06-29, 2023-07-05, 2023-07-11, 2023-07-15, 2023-07-23, 2023-07-30, 2023-08-01, 2023-08-04, 2023-08-06, 2023-08-10, 2023-08-12, 2023-08-13, 2023-08-16, 2023-08-19, 2023-08-21, 2023-08-22, 2023-08-23, 2023-08-28, 2023-08-29

2. Change the guard on line 271 so the general-purpose threshold survives untouched for any future full-timeline re-run, and only the 40 named dates bypass it: skip the day when `day.photoCount < 10` AND `!FORCE_STUB_DATES.has(day.date)`. Do NOT lower or remove the global threshold — 06-RESEARCH.md Pitfall 2 documents that lowering it globally would generate roughly 187 additional out-of-scope stubs, directly violating D-05's dropped-scope decision.

3. Change nothing else. Slug/filename construction (lines 276-301), the frontmatter field order `title, date, voyage, location, excerpt, migrated, draft` then conditional `lat, lon, miles, hours` (lines 303-319), the body/Gallery build (lines 321-343), and the collision guards must all be reused byte-identically so the 40 new stubs come out in the same `file://.../originals/{dir}/{UUID}.{ext}` Gallery shape that `scripts/10-upload-r2.mjs` will parse in plan 06-02.

4. Run `node scripts/04-generate-stubs.mjs --dry-run` and confirm exactly 40 `WOULD STUB` lines before running for real. Job 1 (backfill) also executes on every invocation but was confirmed to be a no-op on the current tree (0 `WOULD BACKFILL` lines) — if the dry run shows any backfill activity, stop and report it rather than proceeding.

5. Run `node scripts/04-generate-stubs.mjs` for real.
  </action>
  <verify>
    <automated>node scripts/04-generate-stubs.mjs --dry-run 2>&1 | grep -c '^WOULD STUB' && node scripts/verify-phase6.mjs --gate posts && node scripts/verify-phase6.mjs | grep -E '^MISSING_POSTS=0$' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' scripts/04-generate-stubs.mjs | grep -c 'FORCE_STUB_DATES'` is at least 2 (declaration plus the guard usage)
    - The `FORCE_STUB_DATES` set contains exactly 40 entries — verifiable with `node -e "..."` importing nothing, or by counting date literals; no date outside the two in-scope ranges appears in it
    - The literal string `day.photoCount < 10` still exists in the file (global threshold preserved, not lowered)
    - Re-running `node scripts/04-generate-stubs.mjs --dry-run` after the real run prints 0 `WOULD STUB` lines (idempotent — the date-prefix collision guard now covers all 40)
    - `node scripts/verify-phase6.mjs` prints `MISSING_POSTS=0` and `IN_SCOPE_POSTS=100`
    - `node scripts/verify-phase6.mjs --gate posts` prints `GATE_PASS posts` and exits 0
    - `git status --porcelain src/content/blog/great-loop | grep -c '^??'` equals 40 (exactly 40 new files, no modifications to existing posts)
    - Each new stub's frontmatter contains `draft: true` and `migrated: false`, and its body contains both the `VoyageStats` and `Gallery` imports plus a `<Gallery images={[` block
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 100 in-scope days with photo data have a draft MDX post; the only uncovered in-scope date is the documented 2023-08-09 gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script -> git repository | Secrets or personal file paths could be committed by accident |
| npm registry -> local node_modules | A new dependency executes install scripts with developer privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `.env.example`, `src/content.config.ts`, `package.json` | mitigate | `.env.example` carries variable NAMES only with placeholder values; real values live in `.env`, which `.gitignore` already excludes (verified by `git check-ignore .env` in Task 1 acceptance criteria) |
| T-06-SC | Tampering (supply chain) | `npm install @aws-sdk/client-s3` | mitigate | Package already audited in 06-RESEARCH.md § Package Legitimacy Audit: `[OK]`, official AWS SDK v3, github.com/aws/aws-sdk-js-v3, tens of millions of weekly downloads. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages introduced, so no blocking human legitimacy checkpoint is required. Do NOT re-run `slopcheck install` — it performs a real install and mutates package.json/package-lock.json |
| T-06-02 | Information Disclosure | 40 generated MDX stubs | accept | Stubs contain `file://` paths under the developer's own home directory and are committed to a private-authoring repo; they are rewritten to R2 URLs in plan 06-04 before any of these posts is published (`draft: true` blocks rendering until Barbara flips it) |
</threat_model>

<verification>
1. `npm run build` exits 0.
2. `node scripts/verify-phase6.mjs --gate posts` exits 0 with `GATE_PASS posts`.
3. `node scripts/verify-phase6.mjs` reports `IN_SCOPE_DATES=101`, `KNOWN_GAPS=1`, `MISSING_POSTS=0`, `IN_SCOPE_POSTS=100`.
4. `git check-ignore .env` exits 0; `git status --porcelain` shows no `.env` file staged or untracked-but-committed.
</verification>

<success_criteria>
- `scripts/verify-phase6.mjs` exists, reports the eight named counters, and supports the three exit-code gates.
- `@aws-sdk/client-s3` installed; `verify-phase6`, `upload-r2`, `draft-narratives` npm scripts present.
- `.env.example` documents all six env-var names with no real secret values.
- `r2Uploaded` and `narrativeDrafted` present in the blog collection Zod schema.
- `scripts/04-generate-stubs.mjs` contains `FORCE_STUB_DATES` with exactly the 40 named dates and still contains the unmodified `day.photoCount < 10` global threshold.
- Exactly 40 new MDX stub files created; zero existing posts modified.
- `npm run build` green.
</success_criteria>

<output>
Create `.planning/phases/06-new-post-generation/06-01-SUMMARY.md` when done.
</output>
