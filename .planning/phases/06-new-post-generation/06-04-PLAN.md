---
phase: 06-new-post-generation
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/pages/blog/[...id].astro
  - src/pages/blog/index.astro
  - src/components/VoyageMap.astro
  - package.json
  - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx
  - src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx
  - .planning/data/r2-upload-report.json
autonomous: false
requirements: [POST-02, POST-03]

must_haves:
  truths:
    - "A draft post can be previewed in the local dev server without flipping its draft flag, so narrative and gallery quality can be reviewed before publication"
    - "A production `npm run build` still emits zero draft posts — the preview path is opt-in via an env var that is never set in CI or netlify.toml"
    - "Two pilot voyage days (one all-photo, one containing videos) have real R2-hosted galleries whose URLs return HTTP 200 and render in the browser"
    - "The measured per-photo iCloud download and upload cost of the pilot is known, so the full 1846-file run can be planned rather than guessed"
  artifacts:
    - path: "src/pages/blog/[...id].astro"
      provides: "Draft preview gated on PREVIEW_DRAFTS env var, default off"
      contains: "PREVIEW_DRAFTS"
    - path: ".planning/data/r2-upload-report.json"
      provides: "Pilot run results: files uploaded, bytes, failures, derivative fallbacks"
      contains: "bytesUploaded"
  key_links:
    - from: "src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx"
      to: "R2 public bucket"
      via: "Gallery array rewritten to https URLs"
      pattern: "https://pub-.*\\.r2\\.dev/great-loop/2024-04-13/"
    - from: "package.json"
      to: "astro dev with drafts visible"
      via: "npm run dev:drafts"
      pattern: "PREVIEW_DRAFTS=1"
---

<objective>
Make draft posts previewable locally, then upload two pilot voyage days to R2 end to end and confirm with human eyes that the photos actually render.

Purpose: Every in-scope post is `draft: true`, and the site's `getCollection` filters exclude drafts from `getStaticPaths` entirely — meaning right now there is literally no way to look at any of the 100 in-scope posts, in dev or in build. POST-03's "reviewed before publishing" gate is unexecutable until that is fixed. Separately, a two-day pilot is the only honest way to learn what the full 1846-file upload will cost, given that 99.6% of the in-scope originals are iCloud-only on this machine.

Output: an env-gated draft preview path, and two fully R2-hosted pilot posts with human confirmation that galleries and the lightbox work against R2 URLs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-new-post-generation/06-CONTEXT.md
@.planning/phases/06-new-post-generation/06-PATTERNS.md
@.planning/phases/06-new-post-generation/06-02-SUMMARY.md
@scripts/10-upload-r2.mjs
@src/components/Gallery.astro
@src/components/Lightbox.astro

<interfaces>
<!-- Existing draft filters this plan modifies (exact current source, read before editing). -->

src/pages/blog/[...id].astro line 6:
  const posts = await getCollection('blog', ({ data }) => !data.draft);

src/pages/blog/index.astro line 5:
  const posts = (await getCollection('blog', ({ data }) => !data.draft)).sort(

src/components/VoyageMap.astro (around lines 20-23): filters to non-draft great-loop posts with lat/lon.

Current published-post count: 72 (the migrated Blogger posts). 250 stubs + the 40 new ones are all draft.

Lightbox behavior against R2 URLs (confirmed in 06-PATTERNS.md, no component change needed):
`Lightbox.astro`'s `largerSrc()` only rewrites Blogger `/w400/`-style size segments to `/w1600/`; it
silently no-ops on R2 URLs, which is correct because R2 images are already served at the 1600px target.

Pilot days chosen for coverage of both media paths:
- 2024-04-13-day-723-florida-keys  — 19 images, 0 videos (Keys->New Bern range, the Loop's finish)
- 2023-07-16-day-451-north-channel-on — 12 images, 2 videos (Canada side trip, exercises the .mov path)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an env-gated draft preview path</name>
  <files>src/pages/blog/[...id].astro, src/pages/blog/index.astro, src/components/VoyageMap.astro, package.json</files>
  <read_first>
    - src/pages/blog/[...id].astro (whole file — the getStaticPaths draft filter on line 6)
    - src/pages/blog/index.astro (whole file — the draft filter on line 5)
    - src/components/VoyageMap.astro (the non-draft + lat/lon filter around lines 20-23)
    - netlify.toml (confirm the production build command is plain `npm run build` with no extra env)
    - package.json (current scripts block)
  </read_first>
  <action>
Make draft posts visible in the local dev server only, behind an explicit opt-in env var that production never sets.

1. In all three files, replace the `!data.draft` predicate with one that also passes when the env var is set: the post is included when `!data.draft || import.meta.env.PREVIEW_DRAFTS === '1'`. Use `import.meta.env` (Astro's env access), not `process.env`. Keep every other part of each predicate intact — in particular `VoyageMap.astro` must still require `voyage === 'great-loop'` and present lat/lon.

2. Add an npm script `"dev:drafts": "PREVIEW_DRAFTS=1 astro dev"` to package.json, leaving the existing `dev` script untouched.

3. Do NOT add `PREVIEW_DRAFTS` to `netlify.toml`, to `.env.example`, or to any CI configuration. The variable exists only to be typed on a local command line. Add a short comment above each modified filter stating that this is a local-preview-only escape hatch and that a production build must never set it.

4. In `[...id].astro`, if the page renders anything that distinguishes post state, add a visible draft banner is NOT required — do not add UI. The only change is the filter predicate.
  </action>
  <verify>
    <automated>npm run build && test $(find dist/blog -name 'index.html' | wc -l | tr -d ' ') -eq 72 && grep -c 'PREVIEW_DRAFTS' src/pages/blog/'[...id]'.astro src/pages/blog/index.astro src/components/VoyageMap.astro && node -e "const p=require('./package.json');if(!p.scripts['dev:drafts'])process.exit(1);console.log('dev:drafts ok')" && ! grep -q 'PREVIEW_DRAFTS' netlify.toml && echo 'production safe'</automated>
  </verify>
  <acceptance_criteria>
    - A plain `npm run build` (no env vars) produces exactly 72 blog post pages under `dist/blog/` — the same count as before this task, proving no draft leaked into production
    - `PREVIEW_DRAFTS=1 npm run build` produces 322 blog post pages (72 published + 250 drafts), proving the gate works
    - After confirming the above, the `dist/` directory is regenerated with a plain `npm run build` so the working tree does not carry a draft-inclusive build
    - All three of `src/pages/blog/[...id].astro`, `src/pages/blog/index.astro`, `src/components/VoyageMap.astro` reference `import.meta.env.PREVIEW_DRAFTS` and none references `process.env.PREVIEW_DRAFTS`
    - `package.json` scripts contains `"dev:drafts": "PREVIEW_DRAFTS=1 astro dev"`
    - `grep -c PREVIEW_DRAFTS netlify.toml` is 0 and `grep -c PREVIEW_DRAFTS .env.example` is 0
    - No component markup or styling changed — `git diff` for the three files shows only predicate/comment lines
  </acceptance_criteria>
  <done>Draft posts can be previewed with `npm run dev:drafts` while production builds remain draft-free.</done>
</task>

<task type="auto">
  <name>Task 2: Run the R2 upload pilot on two voyage days</name>
  <files>src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx, src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx, .planning/data/r2-upload-report.json</files>
  <read_first>
    - scripts/10-upload-r2.mjs (whole file — the script being exercised for the first time against real media)
    - .planning/phases/06-new-post-generation/06-02-SUMMARY.md (bucket name and public base URL host recorded there)
    - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx (pre-run Gallery state)
    - .planning/phases/06-new-post-generation/06-RESEARCH.md § "Pitfall 4" (partial-upload symptom to watch for)
  </read_first>
  <action>
Upload two pilot days and measure what the full run will cost.

1. Confirm the Photos library is reachable and the Photos app can service iCloud downloads. 99.6% of in-scope originals are iCloud-only on this machine, so `osxphotos export --download-missing --use-photokit` will drive Photos.app to fetch each file; this is expected to be the slowest part of the run.

2. Time the first pilot: run `npm run upload-r2 -- --date 2024-04-13` under `time`. This day has 19 images and 0 videos. Record wall-clock seconds, files uploaded, and bytes uploaded from `.planning/data/r2-upload-report.json`.

3. Run the second pilot: `npm run upload-r2 -- --date 2023-07-16`. This day has 12 images and 2 videos and exercises the `.mov` passthrough path (no transcoding — D-02).

4. Verify each rewritten Gallery: every entry must be an `https://` URL under `<R2_PUBLIC_BASE>/great-loop/<date>/`, with zero `file://` entries remaining in either file, and each file's frontmatter must now carry `r2Uploaded: true`. Fetch every URL in both posts with `curl -sS -o /dev/null -w '%{http_code}'` and require 200 for all of them.

5. Confirm idempotency: re-run `npm run upload-r2 -- --date 2024-04-13` and confirm it logs a skip and makes zero changes to the file.

6. Read the report's `missingExports` and `derivativeFallbacks` fields and compute the export success rate. Compute and report the projection for the remaining run: (remaining media count) x (measured seconds per file) and (remaining media count) x (measured average bytes per file), against the 10 GB R2 free-tier ceiling. Roughly 1846 media files exist across all 100 in-scope posts, of which 137 are videos.

7. If any post fails, confirm the failure was clean: the MDX file must be byte-identical to its pre-run state (no mixed `file://` + `https://` Gallery). Report the failure and its cause rather than working around it.
  </action>
  <verify>
    <automated>grep -c 'file://' src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx | grep -qv ':[1-9]' && grep -q 'r2Uploaded: true' src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx && grep -q 'r2Uploaded: true' src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx && node -e "const fs=require('fs');const {execSync}=require('child_process');let bad=0;for(const f of ['2024-04-13-day-723-florida-keys','2023-07-16-day-451-north-channel-on']){const t=fs.readFileSync('src/content/blog/great-loop/'+f+'.mdx','utf8');const urls=[...t.matchAll(/\"(https:\/\/[^\"]+)\"/g)].map(m=>m[1]);if(!urls.length){console.error('no urls in '+f);process.exit(1)}for(const u of urls){const code=execSync('curl -sS -o /dev/null -w \'%{http_code}\' \"'+u+'\"').toString();if(code!=='200'){console.error('HTTP '+code+' '+u);bad++}}}if(bad)process.exit(1);console.log('all pilot media 200 OK')" && node scripts/verify-phase6.mjs | grep -E '^R2_UPLOADED_POSTS=2$' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'file://'` returns 0 for both pilot MDX files
    - Both pilot files' frontmatter contains `r2Uploaded: true`
    - Every `https://` URL in both pilot Galleries returns HTTP 200 (31 URLs total: 19 + 12 images, plus the 2 videos)
    - The video entries in `2023-07-16-day-451-north-channel-on.mdx` end in `.mov` and their objects were uploaded with ContentType `video/quicktime`
    - `node scripts/verify-phase6.mjs` prints `R2_UPLOADED_POSTS=2` and `FILE_URL_POSTS=98`
    - Re-running `npm run upload-r2 -- --date 2024-04-13` logs a skip and leaves `git diff` empty for that file
    - `.planning/data/r2-upload-report.json` contains non-zero `summary.filesUploaded` and `summary.bytesUploaded`, and the SUMMARY records measured seconds-per-file and bytes-per-file plus the projected total for the remaining ~1815 files
    - `summary.failed` is empty; if it is not, each failed post's MDX file is byte-identical to its pre-run state (`git diff` clean for that file)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Two voyage days are fully R2-hosted with verified-live URLs, and the cost of the remaining run is measured rather than assumed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm R2 galleries render, and decide on the derivative fallback</name>
  <files>src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx, src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx</files>
  <read_first>
    - .planning/data/r2-upload-report.json (pilot results: files uploaded, bytes, missingExports, derivativeFallbacks)
    - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx (rewritten Gallery)
    - .planning/phases/06-new-post-generation/06-RESEARCH.md § "Pitfall 3" (r2.dev rate-limit tradeoff already accepted)
  </read_first>
  <what-built>
    Two voyage days now have real, public R2-hosted photo galleries: 2024-04-13 (19 photos, Florida Keys) and 2023-07-16 (12 photos + 2 videos, North Channel Ontario). Every URL has been machine-verified to return HTTP 200, and `npm run dev:drafts` now makes draft posts previewable locally. What a script cannot verify is whether the images actually look right — correct photos on the correct day, acceptable quality after the 1600px/80% resize, working lightbox.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev:drafts` and open http://localhost:4321/blog/2024-04-13-day-723-florida-keys
    2. Confirm all 19 photos load (no broken-image icons), and that they are Florida Keys photos from that day — not photos from some other trip or a different date.
    3. Click a photo. Confirm the lightbox opens and the full-size image displays. Press Escape and confirm it closes.
    4. Check image quality: these were resized to 1600px wide at JPEG quality 80. Confirm they look acceptable at full-screen width on this display.
    5. Open http://localhost:4321/blog/2023-07-16-day-451-north-channel-on and confirm the 12 photos load AND the 2 video entries behave acceptably (they render through the same `<Gallery>` `<img>` path, so a `.mov` may show as a broken thumbnail — note whether that is acceptable or whether videos should be handled differently before the full run).
    6. Review the pilot report Claude just presented: files uploaded, bytes uploaded, seconds per file, projected total for the remaining ~1815 files, and the export success rate.
    7. Decide the derivative-fallback question: if any photos failed to download from iCloud, the script can optionally fall back to the locally cached 1022x768 derivative (95.3% of in-scope media have one) via `--allow-derivative-fallback`. Lower resolution, but a real image instead of a gap. Confirm whether the full run should use it.
    8. Reply `approved` to proceed to the full upload, or describe what looks wrong.
  </how-to-verify>
  <action>
    Before pausing, present to the user: (a) the two preview URLs, (b) the pilot report figures — files uploaded, total bytes, measured seconds per file, projected wall-clock and projected total bytes for the remaining ~1815 files against the 10 GB free-tier ceiling, (c) the export success rate and any `missingExports`, and (d) an explicit recommendation on `--allow-derivative-fallback` based on the measured failure rate.

    After the user replies, record their derivative-fallback decision in the SUMMARY so plan 06-05 runs with the right flag. If the user reports that `.mov` entries render badly enough to matter, record that as a finding for the SUMMARY — do not silently redesign the video path here.
  </action>
  <verify>
    <automated>node scripts/verify-phase6.mjs | grep -E '^R2_UPLOADED_POSTS=2$' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The user confirmed both pilot posts render with all photos loading and correct-for-the-day content
    - The user confirmed lightbox open/close works against R2 URLs
    - The user's decision on `--allow-derivative-fallback` (use / do not use) is recorded verbatim in `06-04-SUMMARY.md`
    - The pilot's measured seconds-per-file, bytes-per-file, and the projection for the remaining ~1815 files are recorded in `06-04-SUMMARY.md`
    - Any `.mov` rendering concern is recorded as a finding in `06-04-SUMMARY.md` rather than being fixed ad hoc in this task
    - `npm run build` exits 0 and `node scripts/verify-phase6.mjs` still reports `R2_UPLOADED_POSTS=2`
  </acceptance_criteria>
  <resume-signal>Type "approved" (optionally with "use derivative fallback") to proceed to the full upload, or describe what is wrong.</resume-signal>
  <done>A human has confirmed R2-hosted galleries look right, and the full-run parameters are decided from measured data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft content -> production build output | An env-gated filter now controls whether unreviewed posts reach `dist/` |
| local Photos library -> public R2 bucket | First real media crosses this boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Information Disclosure | `PREVIEW_DRAFTS` env gate in `src/pages/blog/*` and `VoyageMap.astro` | mitigate | Gate defaults off and is never set in `netlify.toml`, `.env.example`, or CI. Asserted by an acceptance criterion that a plain `npm run build` still emits exactly 72 post pages — the pre-change count — so no unreviewed draft can reach the deployed site |
| T-06-03 | Information Disclosure | First real photo upload to a public bucket | mitigate | The pilot uploads only the UUIDs listed in two specific posts' Gallery arrays; the human-verify checkpoint explicitly asks the user to confirm the rendered photos belong to that voyage day and are not unrelated personal photos, which is the direct human check on the per-UUID export scoping |
| T-06-06 | Denial of Service | `r2.dev` public URL rate limits during the pilot's 31-URL curl sweep | accept | 31 sequential requests is far below any documented r2.dev rate limit; the production-traffic version of this risk was already accepted in plan 06-02 (T-06-06) with the custom-domain upgrade path documented |
| T-06-11 | Tampering | Partial Gallery rewrite leaving mixed `file://` and `https://` entries | mitigate | The script commits per post all-or-nothing (06-02). Verified here by an acceptance criterion requiring that any failed post's MDX file be byte-identical to its pre-run state |
</threat_model>

<verification>
1. Plain `npm run build` emits exactly 72 blog post pages; `PREVIEW_DRAFTS=1 npm run build` emits 322.
2. `grep -c 'file://'` is 0 in both pilot posts; both carry `r2Uploaded: true`.
3. All 33 pilot media URLs return HTTP 200.
4. `node scripts/verify-phase6.mjs` reports `R2_UPLOADED_POSTS=2`, `FILE_URL_POSTS=98`.
5. Human confirmed rendering and lightbox behavior.
</verification>

<success_criteria>
- Draft posts previewable via `npm run dev:drafts`; production build unchanged at 72 pages.
- Two pilot days fully R2-hosted, all URLs live, idempotent re-run confirmed.
- Measured per-file time and size, plus a projection for the remaining ~1815 files, recorded in the SUMMARY.
- Derivative-fallback decision recorded for plan 06-05.
</success_criteria>

<output>
Create `.planning/phases/06-new-post-generation/06-04-SUMMARY.md` when done. It must record the measured per-file cost, the projection for the full run, and the user's `--allow-derivative-fallback` decision.
</output>
