---
phase: 06-new-post-generation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/11-draft-narratives.mjs
  - .planning/data/narrative-triage.json
autonomous: false
requirements: [POST-02, POST-03]

must_haves:
  truths:
    - "Every in-scope post is classified full-narrative, transit, or sparse using photo volume and Nebo detail richness as the proxy (D-06)"
    - "The complete triage list is presented to the user for adjustment before any AI draft is generated (D-07)"
    - "Moving a day between categories requires only editing narrative-triage.json — no code change and no re-run of the classifier"
    - "The triage file records, per day, the exact signals that drove its classification so a reclassification decision can be made without re-deriving them"
  artifacts:
    - path: "scripts/11-draft-narratives.mjs"
      provides: "--triage mode: classifies all in-scope posts and writes narrative-triage.json; --generate mode is stubbed and exits with a not-yet-implemented message (filled in by plan 06-05)"
      min_lines: 150
      contains: "narrative-triage.json"
    - path: ".planning/data/narrative-triage.json"
      provides: "Per-day classification with the photoCount / neboRichness / distanceNm signals behind it, plus range and locked fields"
      contains: "classification"
  key_links:
    - from: "scripts/11-draft-narratives.mjs"
      to: ".planning/data/nebo-logs.json legs field"
      via: "date-keyed Map lookup for neboRichness scoring"
      pattern: "nebo-logs\\.json"
    - from: "scripts/11-draft-narratives.mjs"
      to: ".planning/data/voyage-timeline-enriched.json photoCount"
      via: "date-keyed Map lookup for photo-volume scoring"
      pattern: "voyage-timeline-enriched\\.json"
---

<objective>
Classify every in-scope voyage day into full-narrative vs. transit-day treatment using photo/data volume as the proxy (D-06), and put the complete list in front of the user for adjustment before a single AI draft is generated (D-07).

Purpose: D-07 is an explicit gate — the user must be able to move specific days between categories before any Claude API spend or any narrative lands in an MDX file. Making the classification a reviewable, hand-editable JSON artifact is what makes that gate real rather than ceremonial.

Output: `scripts/11-draft-narratives.mjs` with its `--triage` half implemented, and a reviewed, possibly hand-adjusted `.planning/data/narrative-triage.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-new-post-generation/06-CONTEXT.md
@.planning/phases/06-new-post-generation/06-PATTERNS.md
@.planning/phases/06-new-post-generation/06-01-SUMMARY.md
@scripts/07-quality-lift.mjs
@scripts/09-parse-nebo-details.mjs

<interfaces>
<!-- Contract produced by this plan and consumed by plan 06-05. -->

.planning/data/narrative-triage.json shape (plan 06-05 reads this file and must not re-derive it):
{
  "generated": "<ISO timestamp>",
  "thresholds": { "fullPhotoCount": 10, "fullPhotoCountWithNebo": 5, "fullNeboRichness": 3 },
  "summary": { "total": 100, "full": 64, "transit": 36, "sparse": 0 },
  "days": [
    {
      "date": "2024-04-13",
      "slug": "2024-04-13-day-723-florida-keys",
      "range": "keys-to-new-bern",          // or "canada-side-trip"
      "location": "Florida Keys",
      "photoCount": 19,
      "videoCount": 0,
      "distanceNm": 12.4,                    // or null
      "hasNeboLog": true,
      "neboRichness": 5,
      "routeName": "Marathon to Islamorada",  // or null
      "classification": "full",              // "full" | "transit" | "sparse"
      "classificationReason": "photoCount>=10",
      "locked": false                        // set true by hand to protect a manual reclassification
    }
  ]
}

Classification rules (deterministic, no AI involved):
- neboRichness = sum over the day's nebo legs of: routeName present (1) + weatherDeparture present (1)
  + weatherArrival present (1) + icwMarkers non-empty (1) + landmarks non-empty (1) + namedStops.arrived non-empty (1)
- "full"    when photoCount >= 10, OR (photoCount >= 5 AND neboRichness >= 3)
- "transit" when not full AND (photoCount >= 1 OR the day has at least one nebo leg)
- "sparse"  otherwise

Expected result against the current data (verified during planning — treat a material deviation as a red flag worth reporting, not as something to silently accept):
- 100 in-scope posts total: 64 full, 36 transit, 0 sparse
- keys-to-new-bern (27 posts): 20 full, 7 transit
- canada-side-trip (73 posts): 44 full, 29 transit

Range boundaries: keys-to-new-bern = 2024-04-13..2024-05-17; canada-side-trip = 2023-06-05..2023-08-31.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the triage classifier in scripts/11-draft-narratives.mjs</name>
  <files>scripts/11-draft-narratives.mjs, .planning/data/narrative-triage.json</files>
  <read_first>
    - scripts/07-quality-lift.mjs lines 1-95 (header doc-comment style, const block, and the `neboByDate` / `timelineByDate` date-keyed Map construction to reuse)
    - scripts/07-quality-lift.mjs lines 100-152 and scripts/04-generate-stubs.mjs lines 43-101 (frontmatter trio — copy the 04 variant verbatim per 06-PATTERNS.md)
    - scripts/09-parse-nebo-details.mjs (the exact `legs` field names this classifier scores: routeName, weatherDeparture, weatherArrival, icwMarkers, landmarks, namedStops)
    - .planning/phases/06-new-post-generation/06-CONTEXT.md D-06 and D-07
    - scripts/verify-phase6.mjs (the in-scope date derivation to mirror — same two ranges, same daily-routes.json source)
  </read_first>
  <action>
Create `scripts/11-draft-narratives.mjs` and implement only its `--triage` mode in this task. The `--generate` mode is added in plan 06-05; for now, invoking `--generate` must print a clear "narrative generation is implemented in plan 06-05" message and exit 0 without doing anything.

Structure: mirror `scripts/07-quality-lift.mjs`'s header doc-comment, `__dirname`/`ROOT`/`DATA_DIR`/`POSTS_DIR` const block, and its date-keyed `Map` construction from `.planning/data/nebo-logs.json` and `.planning/data/voyage-timeline-enriched.json` (keep only the first entry per date, as that script does). Copy `splitFrontmatter`/`parseFrontmatter`/`serializeFrontmatter` verbatim from `scripts/04-generate-stubs.mjs` — this project duplicates the trio per script rather than sharing a module.

CLI: `--triage` (write the triage file), `--generate` (stub, see above), `--dry-run` (print the classification table but do not write the JSON file). Running with no mode flag prints usage and exits 2.

Triage mode:
1. Derive the in-scope date set exactly the way `scripts/verify-phase6.mjs` does: the keys of `src/data/daily-routes.json` inside 2024-04-13..2024-05-17 or 2023-06-05..2023-08-31. Then map each date to the `.mdx` file in `src/content/blog/great-loop/` whose filename starts with that date. Dates with no post file (the documented 2023-08-09 gap) are excluded from the triage list — record them in a `skippedDates` array in the output with a short reason instead.
2. For each post, gather signals: `photoCount` and `location` from the enriched-timeline entry; `videoCount` by counting `.mov` entries in the post's `<Gallery images={[...]} />` array; `distanceNm` from the nebo entry (or null); `hasNeboLog`; `routeName` from the first leg with one (or null); and `neboRichness` computed with the exact 6-signal sum defined in the interfaces block above, summed across all of the day's legs.
3. Classify with the exact rules in the interfaces block. Record `classificationReason` as a short machine-ish string naming the rule that fired: `photoCount>=10`, `photoCount>=5 && neboRichness>=3`, `has photos or nebo leg`, or `no photos, no nebo`.
4. Preserve manual edits across re-runs: if `.planning/data/narrative-triage.json` already exists, load it first, and for any day whose existing entry has `locked: true`, keep that entry's `classification` value instead of the freshly computed one (recompute all other fields). New days always get `locked: false`. This is what makes D-07's "move specific days between categories" durable.
5. Write `.planning/data/narrative-triage.json` in the exact shape given in the interfaces block, with `days` sorted by date ascending.
6. Print to stdout: a per-range summary table (range, total, full, transit, sparse), machine-greppable counters `TRIAGE_TOTAL=<n>`, `TRIAGE_FULL=<n>`, `TRIAGE_TRANSIT=<n>`, `TRIAGE_SPARSE=<n>`, and then the complete day-by-day list — one line per day formatted `<date>  <classification>  photos=<n> videos=<n> nebo=<neboRichness> <location>` — because D-07 requires the full list to be presentable, not just a summary.

This script must not call the Anthropic API, must not read `ANTHROPIC_API_KEY`, and must not modify any MDX file in triage mode.
  </action>
  <verify>
    <automated>node scripts/11-draft-narratives.mjs --triage | grep -E '^TRIAGE_TOTAL=100$' && node scripts/11-draft-narratives.mjs --triage | grep -E '^TRIAGE_FULL=64$' && node scripts/11-draft-narratives.mjs --triage | grep -E '^TRIAGE_TRANSIT=36$' && node -e "const t=require('./.planning/data/narrative-triage.json');if(t.days.length!==100)process.exit(1);if(t.days.some(d=>!['full','transit','sparse'].includes(d.classification)))process.exit(1);if(t.days.some(d=>typeof d.neboRichness!=='number'))process.exit(1);console.log('triage json ok')" && git diff --quiet src/content/blog/great-loop && echo "no mdx modified"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/11-draft-narratives.mjs --triage` exits 0 and prints `TRIAGE_TOTAL=100`, `TRIAGE_FULL=64`, `TRIAGE_TRANSIT=36`, `TRIAGE_SPARSE=0`
    - Per-range breakdown printed: keys-to-new-bern 27 posts (20 full / 7 transit), canada-side-trip 73 posts (44 full / 29 transit)
    - stdout contains exactly 100 per-day lines (one per in-scope post), each showing date, classification, photo count, video count, nebo richness, and location
    - `.planning/data/narrative-triage.json` parses, has `days.length === 100`, and every entry has all of: date, slug, range, location, photoCount, videoCount, distanceNm, hasNeboLog, neboRichness, classification, classificationReason, locked
    - The output records `2023-08-09` in `skippedDates` with a documented reason, not silently omitted
    - Hand-editing one day's `classification` to `transit` and setting `locked: true`, then re-running `--triage`, leaves that day's classification as `transit` while all other fields are refreshed
    - `node scripts/11-draft-narratives.mjs --generate` exits 0 with a message pointing at plan 06-05 and makes no file changes
    - `node scripts/11-draft-narratives.mjs` with no flags exits 2 and prints usage
    - `git diff --quiet src/content/blog/great-loop` succeeds (triage never touches MDX)
    - `grep -c 'ANTHROPIC_API_KEY' scripts/11-draft-narratives.mjs` is 0 at this stage
  </acceptance_criteria>
  <done>Every in-scope post has a deterministic, signal-backed classification stored in a hand-editable JSON file.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Present the triage list and take reclassification decisions</name>
  <files>.planning/data/narrative-triage.json</files>
  <read_first>
    - .planning/data/narrative-triage.json (the full generated list)
    - .planning/phases/06-new-post-generation/06-CONTEXT.md D-06, D-07, D-08 and the Canada side-trip framing note
  </read_first>
  <decision>
    Which in-scope days get a full AI-drafted narrative, and which get stats + gallery only.
  </decision>
  <context>
    D-06 makes photo/data volume the proxy for "eventful day", and D-07 requires the complete triage list to be presented before any AI draft is generated so specific days can be moved between categories first. This is the last cheap moment to change the split: after plan 06-05 runs, moving a day from transit to full means another Claude API call, and moving a day from full to transit means deleting narrative text that was already written into an MDX file.

    Current classification (deterministic, from Task 1):
    - 100 in-scope posts: 64 full-narrative, 36 transit, 0 sparse
    - keys-to-new-bern (27 posts, the main Loop finishing at New Bern on 2024-05-17): 20 full / 7 transit
    - canada-side-trip (73 posts, 2023-06-05..2023-08-31): 44 full / 29 transit

    Note that the two ranges are NOT the same kind of thing (06-CONTEXT.md domain section): the Keys-to-New-Bern days are the main day-numbered Loop story running straight through to the finish and must NOT be framed as a side trip, while the Canada days ARE framed as a return excursion to Canadian waters, distinct from the already-published 2022 Georgian Bay leg. That framing difference is handled in the prompt in plan 06-05, but it may influence which days you consider worth a full narrative.
  </context>
  <action>
    Present the complete triage list grouped by range and classification (all 100 days, not a sample), with each day's photo count, video count, nebo richness, and location. Highlight the borderline cases explicitly: days classified `transit` with photoCount 7-9, and days classified `full` solely because photoCount crossed 10 with neboRichness 0. These are the ones most likely to be worth moving.

    Apply the user's requested moves by editing `.planning/data/narrative-triage.json` directly: change the `classification` value and set `locked: true` on every day the user moves, so a later `--triage` re-run cannot revert the decision. Do not change any other field. Do not modify the classifier's thresholds in code — the whole point of the `locked` flag is that per-day judgment lives in data, not in the script.
  </action>
  <options>
    <option id="accept-as-is">
      <name>Accept the computed split (64 full / 36 transit)</name>
      <pros>No further work; the thresholds already track photo volume plus Nebo detail as D-06 specifies; ~64 Claude calls</pros>
      <cons>Some genuinely memorable low-photo days (a rough crossing, an unphotographed lock) get stats+gallery only</cons>
    </option>
    <option id="promote-days">
      <name>Promote specific days from transit to full</name>
      <pros>Days the user actually remembers as eventful get a real narrative regardless of how many photos were taken</pros>
      <cons>Each promoted day is another Claude API call and another post for Barbara to review</cons>
    </option>
    <option id="demote-days">
      <name>Demote specific days from full to transit</name>
      <pros>Fewer AI drafts to review; avoids generating narrative for photo-heavy but uneventful days (e.g. a marina day with 30 photos of the same sunset)</pros>
      <cons>A demoted day's post stays at "Photos from &lt;location&gt;" quality</cons>
    </option>
    <option id="promote-all-keys">
      <name>Promote all 27 Keys-to-New-Bern days to full</name>
      <pros>The Loop's finishing sequence reads as an unbroken day-by-day narrative to New Bern, matching POST-05's intent</pros>
      <cons>7 extra Claude calls on genuinely thin days, which may produce padded prose Barbara has to cut</cons>
    </option>
  </options>
  <verify>
    <automated>node -e "const t=require('./.planning/data/narrative-triage.json');const c=t.days.reduce((a,d)=>{a[d.classification]=(a[d.classification]||0)+1;return a},{});console.log('FINAL_TRIAGE='+JSON.stringify(c));if(t.days.length!==100)process.exit(1);if(t.days.some(d=>d.locked&&!['full','transit','sparse'].includes(d.classification)))process.exit(1)" && node scripts/11-draft-narratives.mjs --triage | grep -E '^TRIAGE_TOTAL=100$'</automated>
  </verify>
  <acceptance_criteria>
    - The user was shown all 100 days grouped by range and classification, including the borderline sets called out above
    - Every day the user moved has both an updated `classification` and `locked: true` in `.planning/data/narrative-triage.json`
    - Re-running `node scripts/11-draft-narratives.mjs --triage` after the edits preserves every locked classification (`FINAL_TRIAGE` counts are unchanged by the re-run)
    - `days.length` is still 100 and no day's `slug` or `date` was altered
    - `scripts/11-draft-narratives.mjs` classification thresholds are unchanged (`git diff scripts/11-draft-narratives.mjs` is empty for this task)
  </acceptance_criteria>
  <resume-signal>Type "approved" to accept the current split, or list the dates to move and their target category (e.g. "move 2024-05-14 and 2023-07-15 to full").</resume-signal>
  <done>The full-narrative vs. transit split is user-approved and locked in data before any Claude API spend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local pipeline data -> triage artifact | Only project-internal JSON crosses; nothing leaves the machine in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | `.planning/data/narrative-triage.json` (committed to the repo) | accept | Contains only dates, slugs, place names, photo counts and Nebo-derived route/weather signals — all of which already appear in committed `.planning/data/*.json` pipeline artifacts and in the site's own published posts. No credentials, no file paths outside the repo, no photo content |
| T-06-08 | Spoofing / Repudiation | Silent reclassification drift between the approved triage and what plan 06-05 actually generates | mitigate | The `locked: true` flag makes every user decision durable across `--triage` re-runs, and plan 06-05 reads `classification` from this file rather than recomputing it. An acceptance criterion asserts locked classifications survive a re-run |
| T-06-09 | Information Disclosure | Accidental API-key handling in this script | mitigate | Triage mode is asserted to contain no `ANTHROPIC_API_KEY` reference at all; the credential guard is introduced only in plan 06-05, following the lazy `getClient()` pattern from `scripts/07-quality-lift.mjs` |
</threat_model>

<verification>
1. `node scripts/11-draft-narratives.mjs --triage` exits 0 and prints `TRIAGE_TOTAL=100`.
2. `.planning/data/narrative-triage.json` parses with 100 day entries, each carrying its classification signals.
3. Locked classifications survive a `--triage` re-run.
4. `git diff --quiet src/content/blog/great-loop` succeeds — no MDX file was touched by this plan.
5. `npm run build` exits 0.
</verification>

<success_criteria>
- `scripts/11-draft-narratives.mjs` exists with a working `--triage` mode and a stubbed `--generate` mode.
- All 100 in-scope posts classified using the D-06 photo/data-volume proxy with the exact documented thresholds.
- The complete triage list was presented to the user and any requested moves are applied and locked (D-07 satisfied).
- Zero MDX modifications, zero Claude API calls in this plan.
</success_criteria>

<output>
Create `.planning/phases/06-new-post-generation/06-03-SUMMARY.md` when done. Record the final full/transit counts after any user reclassification.
</output>
