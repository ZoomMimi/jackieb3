---
phase: 06-new-post-generation
plan: 06
type: execute
wave: 5
depends_on: ["06-03", "06-05"]
files_modified:
  - scripts/11-draft-narratives.mjs
  - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx
  - src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx
  - .planning/data/narrative-draft-report.json
autonomous: false
requirements: [POST-03, POST-05]

user_setup:
  - service: anthropic
    why: "Claude API narrative drafting for the 64 full-narrative in-scope days (06-CONTEXT.md AI narrative generation approach)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com -> API Keys (same key already used by scripts/07-quality-lift.mjs; add it to .env alongside the R2 values)"

must_haves:
  truths:
    - "A full-narrative day's post body contains first-person prose in Barbara's established voice, an attempted closing Bible verse, the R2 photo gallery, and the voyage stats footer"
    - "Keys-to-New-Bern days read as the main Loop continuing to its finish; Canada days read as a return excursion to Canadian waters distinct from the 2022 Georgian Bay leg"
    - "Generation never flips draft:false and never overwrites a post Barbara has already started editing"
    - "The R2-hosted photos themselves inform the narrative — the model sees the actual images, not just metadata"
  artifacts:
    - path: "scripts/11-draft-narratives.mjs"
      provides: "--generate mode: multimodal Claude call with R2 photo URLs plus Nebo legs detail, body rewrite, narrativeDrafted flag, report"
      contains: "narrativeDrafted"
    - path: ".planning/data/narrative-draft-report.json"
      provides: "Per-day generation results in the 07-quality-lift report shape"
      contains: "summary"
  key_links:
    - from: "scripts/11-draft-narratives.mjs"
      to: "Anthropic Messages API"
      via: "image content blocks with source type url pointing at R2"
      pattern: "type: 'url'"
    - from: "scripts/11-draft-narratives.mjs"
      to: ".planning/data/narrative-triage.json"
      via: "reads classification, never recomputes it"
      pattern: "narrative-triage\\.json"
    - from: "scripts/11-draft-narratives.mjs"
      to: ".planning/data/nebo-logs.json legs"
      via: "date-keyed lookup feeding weather/route/ICW detail into the prompt"
      pattern: "legs"
---

<objective>
Implement the `--generate` half of `scripts/11-draft-narratives.mjs` and prove it on two pilot days before spending API budget on the other 62.

Purpose: This is the step that turns a stats-and-gallery stub into something that reads like Barbara wrote it. It is also the step with the highest chance of producing subtly wrong output — wrong voice, fabricated events, misquoted scripture — so it gets a mandatory human read of two real drafts before the bulk run.

Output: a working `--generate` mode, two AI-drafted pilot posts, and a human verdict on voice quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-new-post-generation/06-CONTEXT.md
@.planning/phases/06-new-post-generation/06-RESEARCH.md
@.planning/phases/06-new-post-generation/06-PATTERNS.md
@.planning/phases/06-new-post-generation/06-03-SUMMARY.md
@.planning/phases/06-new-post-generation/06-05-SUMMARY.md
@scripts/07-quality-lift.mjs
@scripts/11-draft-narratives.mjs

<interfaces>
<!-- Contracts consumed and produced by --generate mode. -->

Reads (never recomputes): .planning/data/narrative-triage.json — the user-approved classification
from plan 06-03. Only days with classification "full" are generated. Days marked "transit" keep
stats + gallery only (D-06) and must not be touched.

Post body shape BEFORE generation (produced by 04-generate-stubs, Gallery rewritten by 10-upload-r2):
  import VoyageStats from '../../../components/VoyageStats.astro'
  import Gallery from '../../../components/Gallery.astro'
  <VoyageStats miles={51.3} hours={5.2} />
  <Gallery images={[ "https://<r2-host>/great-loop/<date>/<UUID>.jpg", ... ]} />

Post body shape AFTER generation (both existing blocks reused byte-for-byte, only reordered):
  the two imports, unchanged
  the AI-drafted narrative prose
  the closing verse (quoted line, then "Book Chapter:verse TRANSLATION" on the next line, no blank line between)
  the existing <Gallery ...> block, byte-identical
  the existing <VoyageStats ... /> block, byte-identical, now last (matching the published-post footer shape)

Established voice/verse anchors (real published posts, migrated: true):
- src/content/blog/great-loop/2024-02-23-boca-chica-vogelzaangs-days-254-258.mdx — first-person, warm,
  conversational, specific place and people names, light self-deprecating humor
- src/content/blog/great-loop/2022-04-22-the-adventure-begins.mdx — closing verse format:
  a quoted verse line followed immediately by  Psalms 19:1 NIV

Nebo legs fields available per date in .planning/data/nebo-logs.json (from scripts/09-parse-nebo-details.mjs):
  routeName, commenced, completed, weatherDeparture {temp,unit,windKn,windDir}, weatherArrival,
  icwMarkers [Number], landmarks [String], namedStops {started,departed,arrived[],stopped},
  weatherReadings [], positions [[lat,lon]]
Every field degrades to null/[] when OCR missed it. 50 of the 101 in-scope dates have a Nebo log.

Model and limits (verified in 06-RESEARCH.md): model claude-sonnet-4-6, max_tokens 4000, image blocks
use source type "url" pointing straight at the public R2 URLs (no base64 download), at most 20 image
blocks per request, images before text in the content array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement --generate mode</name>
  <files>scripts/11-draft-narratives.mjs</files>
  <read_first>
    - scripts/11-draft-narratives.mjs (whole file — the triage half built in plan 06-03, whose helpers and Map lookups are reused)
    - scripts/07-quality-lift.mjs lines 39-63 (the lazy getClient ANTHROPIC_API_KEY guard to copy verbatim), lines 216-236 (messages.create call shape and text-block extraction), lines 415-445 (report shape)
    - src/content/blog/great-loop/2024-02-23-boca-chica-vogelzaangs-days-254-258.mdx (voice anchor — read the actual prose)
    - src/content/blog/great-loop/2022-04-22-the-adventure-begins.mdx (closing-verse format anchor)
    - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx (a target post's current body, post-R2-upload)
    - .planning/phases/06-new-post-generation/06-RESEARCH.md § "Pattern 5" and § "Pitfall 5"
    - .planning/data/narrative-triage.json (the approved classifications)
  </read_first>
  <action>
Add `--generate` mode to `scripts/11-draft-narratives.mjs`, reusing the file's existing in-scope date derivation, frontmatter trio, and date-keyed Nebo/timeline Maps from the triage half.

Credential guard: copy `getClient()` from `scripts/07-quality-lift.mjs` verbatim — lazy, checks `ANTHROPIC_API_KEY`, prints the "Export it before running" guidance, `process.exit(1)`. It must stay lazy so `--triage` continues to work with no key set.

CLI additions: `--generate`; `--date YYYY-MM-DD` (repeatable, restricts the run); `--limit N`; `--force` (re-draft a post that already has `narrativeDrafted: true`); `--dry-run` (build and print the exact prompt text and the image-URL list for each selected post, make no API call, write nothing).

Selection rules:
1. Load `.planning/data/narrative-triage.json`. Select only days whose `classification` is `full`. Never recompute classification in generate mode — the file is the user-approved contract from plan 06-03.
2. Skip a post with `SKIP <slug> (already drafted)` when `fm.narrativeDrafted === true` and `--force` was not passed.
3. Refuse outright, with a loud error and no write, when `fm.draft === false` — that means Barbara has already reviewed and published it, and re-drafting would destroy her work. This refusal applies even with `--force`.
4. Skip with a clear message when the post's Gallery still contains `file://` entries — generation requires public R2 URLs (this should never happen after plan 06-05, so treat it as an error condition worth reporting).

Prompt construction, per post:
- System prompt: instruct the model to write a first-person journal entry as Barbara, the voice established in her published posts. Include 2-3 style excerpts drawn programmatically from posts with `migrated: true` in `src/content/blog/great-loop/` — pick the ones nearest in date to the target day, cap each excerpt at roughly 1200 characters, and strip their import lines and component tags before embedding. Always additionally include the closing-verse format anchor from `2022-04-22-the-adventure-begins.mdx`.
- Verse instruction (D-08): require an attempted closing Bible verse relevant to the day, formatted as a quoted verse line followed immediately on the next line by `Book Chapter:verse TRANSLATION` with no blank line between them. State explicitly in the prompt that this is an attempt Barbara will verify and freely edit, and that the model must not claim the citation is guaranteed accurate — 06-RESEARCH.md Pitfall 5 documents that LLMs misquote scripture references, and the human review gate is the mitigation.
- Range framing, chosen by the post's date:
  - 2024-04-13..2024-05-17: this is the main Great Loop continuing straight through to its finish at New Bern NC on 2024-05-17 — the day-numbered sequence running to the end. Explicitly forbid "side trip" or "excursion" framing here.
  - 2023-06-05..2023-08-31: this is a return visit to Canadian waters and the Great Lakes (North Channel, Ontario, roughly Jul 14-22 2023) — a side excursion, and distinctly NOT the already-published 2022 Georgian Bay/Canada leg. The prose should acknowledge it as a return.
- Grounding instruction: the model must write only from what the photos and the supplied Nebo/GPS data actually show. It must not invent named people, restaurants, marinas, or events that are not evidenced. Where data is thin, a shorter entry is correct; padding is not.
- Output contract: the model returns the excerpt line `EXCERPT: <one sentence, under 160 characters>`, then a blank line, then the narrative markdown ending with the verse. No frontmatter, no code fences, no headings above H2, no commentary about being an AI.
- User message content array (image blocks first, then one text block, per Anthropic's ordering guidance): up to 20 `{ type: 'image', source: { type: 'url', url } }` blocks built from the post's Gallery URLs, images only — skip `.mov` entries, since video URLs are not valid image blocks. When a post has more than 20 images, take an evenly spaced sample across the day's ordered gallery so morning through evening is represented, and record the sampled count in the report. Then a text block carrying: the date, the computed voyage day number, the location, `miles`/`hours` from frontmatter when present, and every non-null Nebo `legs` detail — route name, commenced/completed times, departure and arrival weather, ICW mile markers, landmarks, named stops. Omit any field that is null or empty rather than emitting the word "null".

API call: `messages.create({ model: 'claude-sonnet-4-6', max_tokens: 4000, system, messages: [{ role: 'user', content }] })`, then extract the first `text` block exactly as `scripts/07-quality-lift.mjs` does and throw when there is none.

Body rewrite, per post:
- Parse the response: if the first line starts with `EXCERPT:`, take the remainder of that line as the new `fm.excerpt` and everything after the following blank line as the narrative. If it does not, leave `fm.excerpt` unchanged and treat the whole response as the narrative — do not fail the post over a missing excerpt line.
- Extract the existing `<Gallery images={[ ... ]} />` block and the existing `<VoyageStats ... />` block from the current body as exact substrings. Reuse them byte-for-byte — never regenerate the Gallery URLs or the VoyageStats props.
- Compose the new body as: the two import lines, blank line, narrative prose (which ends with the verse), blank line, the Gallery block, blank line, the VoyageStats block. Write the file as `'---\n' + serializeFrontmatter(fm) + '\n---\n\n' + newBody + '\n'`.
- Set `fm.narrativeDrafted = true`. Never touch `fm.draft` — flipping it is Barbara's action alone (D-10). Never touch `fm.title`, `fm.date`, `fm.voyage`, `fm.lat`, `fm.lon`, `fm.miles`, `fm.hours`, `fm.r2Uploaded`.
- On any error, log `FAIL <slug>: <message>`, push to the failed list, and leave the file completely unmodified.

Report: write `.planning/data/narrative-draft-report.json` in the `scripts/07-quality-lift.mjs` shape — `{ generated, durationSeconds, summary: { total, processed, skipped, failed, imagesSent, imagesSampledDown }, processed, skipped, failed }`. Never log or serialize the API key.
  </action>
  <verify>
    <automated>node scripts/11-draft-narratives.mjs --generate --dry-run --date 2024-04-13 | grep -qi 'new bern' && node scripts/11-draft-narratives.mjs --generate --dry-run --date 2024-04-13 | grep -vi 'forbid\|not a side trip\|no side' | grep -ci 'side trip' | grep -q '^0$'; node scripts/11-draft-narratives.mjs --generate --dry-run --date 2023-07-16 | grep -qi 'return' && node scripts/11-draft-narratives.mjs --generate --dry-run --date 2024-04-13 | grep -cE 'https://.*/great-loop/2024-04-13/' | grep -qE '^(19|20)$' && git diff --quiet src/content/blog/great-loop && echo 'dry-run wrote nothing' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/11-draft-narratives.mjs --generate --dry-run --date 2024-04-13` prints the assembled prompt, mentions the New Bern finish framing, and contains no instruction to frame the day as a side trip or excursion
    - `node scripts/11-draft-narratives.mjs --generate --dry-run --date 2023-07-16` prints prompt text framing the day as a return visit to Canadian waters and distinguishing it from the 2022 Georgian Bay leg
    - The dry-run image list for 2024-04-13 contains 19 R2 image URLs and zero `.mov` URLs
    - A dry run against a post with more than 20 images lists exactly 20 image URLs and reports that it sampled down
    - The prompt text includes at least two style excerpts sourced from `migrated: true` posts and the literal verse-format instruction (quoted line, then `Book Chapter:verse TRANSLATION` on the next line)
    - `git diff --quiet src/content/blog/great-loop` succeeds after all dry runs — dry run writes nothing
    - The script contains a guard refusing to write when `fm.draft === false`, and the string `fm.draft =` appears nowhere (generation never sets the draft flag)
    - `grep -c "narrative-triage.json" scripts/11-draft-narratives.mjs` is at least 1 in the generate path, and generate mode contains no call to the classification function (it reads, never recomputes)
    - `node scripts/11-draft-narratives.mjs --generate` with no `ANTHROPIC_API_KEY` set exits 1 with the guard message and writes nothing
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The narrative generator is implemented and provably correct in dry run, with zero API spend and zero file changes so far.</done>
</task>

<task type="auto">
  <name>Task 2: Generate two pilot narratives</name>
  <files>src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx, src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx, .planning/data/narrative-draft-report.json</files>
  <read_first>
    - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx (pre-generation body)
    - src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx (pre-generation body)
    - scripts/11-draft-narratives.mjs (the generate path being exercised for real)
    - .planning/data/narrative-triage.json (confirm both pilot days are classified full)
  </read_first>
  <action>
Generate real narratives for the two days already validated end to end in plan 06-04: `2024-04-13` (Florida Keys, 19 photos, the Loop's finishing sequence) and `2023-07-16` (North Channel Ontario, 12 photos + 2 videos, the Canada return excursion). One from each range, so the pilot exercises both framings.

First confirm both are classified `full` in `.planning/data/narrative-triage.json`. If either was moved to `transit` by the user in plan 06-03, pick the nearest `full` day in the same range instead and record the substitution.

Run `npm run draft-narratives -- --generate --date 2024-04-13 --date 2023-07-16`.

Then verify structurally, before any human reads them:
- Each file still contains its original `<Gallery images={[...]} />` block byte-for-byte (compare against `git show HEAD:<path>` — the URL list must be identical).
- Each file still contains its original `<VoyageStats ... />` block with identical props, now positioned last in the body.
- Each file's frontmatter has `narrativeDrafted: true`, still has `draft: true`, still has `r2Uploaded: true`, and has unchanged `title`, `date`, `lat`, `lon`, `miles`, `hours`.
- Each body contains prose above the Gallery block, and a closing verse in the two-line quoted-then-citation format.
- The 2024-04-13 body contains no "side trip" or "excursion" framing; the 2023-07-16 body acknowledges a return to Canada.
- `npm run build` passes (MDX is valid and the Zod schema accepts the new flag).

Report the two drafts' word counts, the number of images sent to the API, and the verse each draft chose.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const {execSync}=require('child_process');const paths=['src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx','src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx'];for(const p of paths){const now=fs.readFileSync(p,'utf8');const before=execSync('git show HEAD:'+p).toString();const g=s=>{const m=s.match(/<Gallery images=\{\[[\s\S]*?\]\}\s*\/>/);return m?m[0]:null};const v=s=>{const m=s.match(/<VoyageStats[^>]*\/>/);return m?m[0]:null};if(g(now)!==g(before)){console.error('gallery changed in '+p);process.exit(1)}if(v(now)!==v(before)){console.error('voyagestats props changed in '+p);process.exit(1)}if(!/^narrativeDrafted: true$/m.test(now)){console.error('no narrativeDrafted in '+p);process.exit(1)}if(!/^draft: true$/m.test(now)){console.error('draft flag changed in '+p);process.exit(1)}const body=now.split(/\n---\n/)[1]||'';const gi=body.indexOf('<Gallery');const prose=body.slice(body.indexOf('Gallery.astro')+20,gi).trim();if(prose.length<400){console.error('prose too short in '+p+': '+prose.length);process.exit(1)}console.log(p+' ok, prose chars='+prose.length)}" && node scripts/verify-phase6.mjs | grep -E "^NARRATIVE_DRAFTED_POSTS=2$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Both pilot files' `<Gallery images={[...]} />` blocks are byte-identical to their pre-generation state (verified against `git show HEAD:<path>`)
    - Both files' `<VoyageStats ... />` tags are byte-identical and now appear last in the body
    - Both files have `narrativeDrafted: true`, `draft: true`, `r2Uploaded: true`, and unchanged `title`/`date`/`lat`/`lon`/`miles`/`hours`
    - Each body contains at least 400 characters of prose before the Gallery block
    - Each body contains a closing verse matching the two-line format: a quoted line followed on the very next line by a `Book Chapter:verse TRANSLATION` citation
    - `2024-04-13`'s prose contains neither "side trip" nor "excursion"; `2023-07-16`'s prose refers to returning to Canada
    - `node scripts/verify-phase6.mjs` prints `NARRATIVE_DRAFTED_POSTS=2` and still prints `DRAFT_POSTS=100`
    - `.planning/data/narrative-draft-report.json` records `summary.processed = 2`, `summary.failed = 0`, and a non-zero `imagesSent`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Two real AI drafts exist and are structurally correct — galleries and stats preserved, drafts still unpublished.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Barbara reads the two pilot drafts and rules on the voice</name>
  <files>src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx, src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx</files>
  <read_first>
    - src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx (the generated draft)
    - src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx (the generated draft)
    - src/content/blog/great-loop/2024-02-23-boca-chica-vogelzaangs-days-254-258.mdx (the voice these should resemble)
  </read_first>
  <what-built>
    Two real AI-drafted posts, generated from the actual R2-hosted photos plus Nebo GPS, weather, and route data: 2024-04-13 (Florida Keys, the Loop running toward its New Bern finish) and 2023-07-16 (North Channel Ontario, the Canada return excursion). Structural checks already passed — the photo galleries and voyage stats were preserved exactly, both posts are still `draft: true`, and each ends with an attempted Bible verse. What no script can judge is whether they sound like Barbara.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev:drafts` and open http://localhost:4321/blog/2024-04-13-day-723-florida-keys and http://localhost:4321/blog/2023-07-16-day-451-north-channel-on
    2. Read both. Ask specifically:
       - Does this sound like Barbara — first-person, warm, conversational, specific — or does it sound like an AI writing about boating?
       - Are there invented facts? Named people, restaurants, marinas, or events that did not happen? The model was told to write only from the photos and Nebo data, but this is the check that matters.
       - Is the closing Bible verse quoted accurately and does the citation match the text? The model was explicitly told this is an attempt for you to verify — LLMs misquote scripture references.
       - Is the length right? Should thin days get shorter entries?
    3. Confirm the framing: the 2024-04-13 post should read as the main Loop continuing to the finish (not a side trip), and the 2023-07-16 post should read as a return to Canadian waters, distinct from the 2022 Georgian Bay leg.
    4. Decide: approve the prompt as-is for the remaining 62 full-narrative days, or describe specific prompt changes (tone, length, how much Nebo detail to weave in, verse handling).
    5. You do NOT need to edit these two posts now. Editing every draft happens in plan 06-08. This checkpoint is only about whether the generator is good enough to run at scale.
  </how-to-verify>
  <action>
    Present both drafts' full text inline as well as the preview URLs, plus the verse each one chose and its citation, so the accuracy question can be answered without hunting. State plainly that the verse citations are unverified model output.

    If the user requests prompt changes, apply them to the system prompt in `scripts/11-draft-narratives.mjs`, re-run generation for both pilot days with `--force`, and present the new drafts. Repeat until approved. Record the final approved prompt characteristics and the number of revision rounds in the SUMMARY.

    If the user rejects the approach entirely, stop and report — do not proceed to the bulk run in plan 06-07.
  </action>
  <verify>
    <automated>node scripts/verify-phase6.mjs | grep -E "^NARRATIVE_DRAFTED_POSTS=2$" && node scripts/verify-phase6.mjs | grep -E "^DRAFT_POSTS=100$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The user read both drafts and gave an explicit verdict (approve / revise / reject)
    - Any requested prompt changes were applied to `scripts/11-draft-narratives.mjs`, both pilots regenerated with `--force`, and re-presented
    - The user's verdict on verse accuracy is recorded — specifically whether the generated citations were correct, since that determines how carefully plan 06-08's review must treat them
    - Both posts remain `draft: true` (`DRAFT_POSTS=100` unchanged) — this checkpoint publishes nothing
    - The approved prompt characteristics and revision-round count are recorded in `06-06-SUMMARY.md`
    - `npm run build` exits 0
  </acceptance_criteria>
  <resume-signal>Type "approved" to run the generator across the remaining full-narrative days, or describe the prompt changes you want.</resume-signal>
  <done>The narrative generator has a human verdict on voice, invented facts, and verse handling before it runs at scale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script -> Anthropic API | An API key and public photo URLs cross this boundary |
| model output -> published blog content | Generated text becomes content attributed to a real person |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information Disclosure | `ANTHROPIC_API_KEY` in `scripts/11-draft-narratives.mjs` and `narrative-draft-report.json` | mitigate | Key read only via `process.env.ANTHROPIC_API_KEY` inside the lazy `getClient()` copied verbatim from `scripts/07-quality-lift.mjs`; supplied from gitignored `.env` via `--env-file-if-exists`; never logged and never written to the report |
| T-06-14 | Spoofing | Model-fabricated events, people, or misquoted scripture published under Barbara's name | mitigate | The prompt forbids inventing unevidenced people/places/events and states the verse is an attempt for human verification. The structural gate is that generation never sets `draft: false` (asserted: the string `fm.draft =` appears nowhere), so nothing reaches the site without Barbara's manual flip. This checkpoint puts a human on the two pilot drafts specifically to judge fabrication and verse accuracy before the bulk run |
| T-06-15 | Tampering | Regeneration destroying Barbara's in-progress edits | mitigate | Generation skips posts with `narrativeDrafted: true` unless `--force`, and refuses outright — even with `--force` — when `draft: false`, which is the marker that Barbara has finished with a post. Documented as a hard rule, asserted in acceptance criteria |
| T-06-16 | Information Disclosure | Photo content sent to a third-party API | accept | The images are already publicly readable at their R2 URLs by design (they are about to be published on a public blog), and only URLs are transmitted — no base64 payload. Scope is limited to the same curated per-day voyage UUIDs already vetted under T-06-03 |
| T-06-12 | Denial of Service | Unbounded API spend | mitigate | Generation is restricted to `full`-classified days from the user-approved triage file (64 of 100), capped at 20 image blocks and 4000 max_tokens per request, and this plan runs only 2 posts before a human gate |
</threat_model>

<verification>
1. `node scripts/11-draft-narratives.mjs --generate --dry-run` produces correct per-range framing and image lists with zero writes.
2. Both pilot posts' Gallery and VoyageStats blocks are byte-identical to their pre-generation state.
3. `NARRATIVE_DRAFTED_POSTS=2` and `DRAFT_POSTS=100`.
4. Human verdict recorded on voice, fabrication, and verse accuracy.
5. `npm run build` exits 0.
</verification>

<success_criteria>
- `--generate` implemented with lazy API-key guard, triage-driven selection, per-range framing, ≤20 url-source image blocks, and byte-preserving Gallery/VoyageStats reuse.
- Generation cannot flip `draft`, cannot overwrite a published post, and skips already-drafted posts without `--force`.
- Two pilot narratives generated and structurally verified.
- Human verdict on voice quality, fabricated facts, and verse accuracy recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/06-new-post-generation/06-06-SUMMARY.md` when done. Record the approved prompt characteristics, revision rounds, and the user's verdict on verse-citation accuracy.
</output>
