---
context: phase
phase: 06-new-post-generation
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-19T21:05:35.732Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Plan 06-07 (bulk narrative generation) is **already fully executed**, inside the same worktree as 06-06 — do not re-run it. All 57 remaining full-classified days were generated in 8 batches with 0 failures. `NARRATIVE_DRAFTED_POSTS=59`, `DRAFT_POSTS=100` (nothing published), `npm run build` passes. Your job is documentation and merge bookkeeping (write both SUMMARY.md files, merge to main, update STATE.md/ROADMAP.md), not more generation.
- [ ] CONSTRAINT: A real generation defect was found and fixed mid-run — the model hallucinated placeholder markdown images (`![](photo1)`, `![](rocky shoreline with green water)`) in 4 of the 59 posts, which broke the Astro/Rolldown build (it tried to resolve them as JS imports). Fixed three ways: stripped the bad lines from the 4 affected posts (2023-06-18, 07-09, 07-18, 08-20), added an explicit prohibition to the system prompt, and added a defensive regex strip inside the generator itself (commit `751fe79`) so this can't break a build again even on a future `--force` regenerate from the review tool. Verified 0/59 posts contain markdown image syntax after the fix.
- [ ] CONSTRAINT: The plan 06-06 Task 3 human-verify checkpoint never got one clean "approved" sentence from the user — it was approved *through action*, not words. Sequence: user flagged an invented name ("Mike") → grounding rule fixed → user asked for a full editing tool with keep/discard + real names/memories → tool built (`scripts/narrative-viewer.mjs`) → user said "draft the text as planned for all of these days" → that was treated as the go-ahead to run all of plan 06-07's bulk generation immediately, skipping a separate pause-and-ask-again step. When writing `06-06-SUMMARY.md`, record this sequence as the de facto approval rather than inventing a verdict that was never stated in those words.

**Do not proceed until all three boxes above are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting a plan's hardcoded acceptance-criteria numbers | 06-06-PLAN.md's Task 1 verify block expected exactly 19 images on the 2024-04-13 pilot; the real, correct, already-uploaded count was 17 | advisory | When a plan's acceptance criteria cite a specific data-derived count for content that could have changed since planning time, verify against the live file, not the plan's number |
| Unverified model output can break the build, not just be "wrong" | The markdown-image hallucination wasn't just a content-quality issue — it was a literal build-breaking defect that `npm run build` didn't surface until the FULL bulk run finished (it builds all pages in one pass, so the first bad file aborts everything) | blocking (see constraint above) | Defensive stripping now lives in the generator itself, not just the prompt. If extending the generator further, keep this pattern: prompt instructions are necessary but not sufficient — add a code-level guard for any output shape that would break the build if the model ignores the instruction. |

<current_state>
Phase 06, Wave 5+6 (plans 06-06 and 06-07), both functionally complete, working in an isolated worktree: `.claude/worktrees/wave5-06-06` (branch `worktree-wave5-06-06`), 16 commits ahead of `main`, HEAD at `ac9926a`. Nothing has been merged to `main` yet. No SUMMARY.md files have been written for either plan.

What's done:
- `scripts/11-draft-narratives.mjs`'s `--generate` mode: fully implemented, hardened against the markdown-image defect, and now reads `.planning/data/narrative-notes.json` for site-wide "known people" facts and per-day memories.
- `scripts/narrative-viewer.mjs`: a new local review tool (matches the existing `photo-viewer.mjs`/`blog-viewer.mjs` pattern) — real-time text editing, per-day keep/discard (discard deletes the post file entirely), family/memory notes editor, "regenerate with notes" button, and per-photo rotate/delete/zoom (rotate re-uploads to the same R2 key; delete removes the URL from the Gallery array only).
- All 59 full-classified days (2 pilots from 06-06 + 57 bulk from 06-07) have AI-drafted narratives. All still `draft: true`. `npm run build` passes.
- `06-CONTEXT.md`'s D-09 ("direct MDX editing, no new review UI") is marked superseded in place, not silently overridden.
</current_state>

<completed_work>

Plan 06-06 (all 3 tasks functionally done, SUMMARY.md not yet written):
- Task 1: `--generate` mode implemented (commit `59a9a59`)
- Task 2: Two pilot narratives generated (commit `0065322`, later regenerated multiple times during checkpoint iteration)
- Task 3: Checkpoint — see the "approved through action, not words" constraint above. Also produced two real feature additions beyond the plan's original scope: the review tool (`101abb3`, `46ef734`) and the grounding-rule fix (`42c51c9`).

Plan 06-07 (both tasks done, SUMMARY.md not yet written):
- Task 1: All 19 remaining Keys-to-New-Bern days generated across 3 batches (commits `65aae18`, `65f1876`, `85d3ca5`). Spot-checked 3 posts (2024-04-21, 05-02, 05-17) — zero "side trip"/"excursion" language. Read the New Bern finish (05-17) in full — genuinely strong closing narrative.
- Task 2: All 38 remaining Canada-side-trip days generated across 5 batches (commits `ff34f97`, `1c45f74`, `9a9ce7a`, `072bc93`, `430961b`). Spot-checked 2023-07-14 (first North Channel day) — correctly reads "We are back in Canada... our first visit during the Loop," not conflated with the 2022 Georgian Bay leg.
- Task 3 (transit days untouched, phase state consistent) — NOT yet explicitly verified. Should be quick: `git diff` on a few transit-classified posts should show no changes, and `node scripts/verify-phase6.mjs` already confirms `DRAFT_POSTS=100` unchanged and `NARRATIVE_DRAFTED_POSTS=59` matches the full-day count exactly.
- Mid-run defect found and fixed: see the markdown-image-hallucination constraint above (commit `751fe79`).

Additional work done this session, not originally in either plan:
- `color-scheme: light` added site-wide (both `main` and this worktree) — defensive hygiene after diagnosing (and ruling out as the real cause of) a dev-server-only dark-rendering artifact.
- Site homepage replaced with a 3-choice welcome page (This/Our Blog, Jackie's Story, Jackie's Song) — already merged and pushed to `main` directly, unrelated to phase 6, not part of this worktree.
</completed_work>

<remaining_work>

1. Write `06-06-SUMMARY.md` (template: `06-04-SUMMARY.md`). Must cover: the implementation, the two pilots, the grounding-rule fix, the review-tool build (a real scope addition — document what it does and why, referencing the superseded D-09), and the "approved through action" checkpoint resolution.
2. Write `06-07-SUMMARY.md`. Must cover: all 57 days generated across 8 batches, 0 failures, the two spot-checks (Keys finish + Canada return framing), and the markdown-image defect (found, fixed 3 ways, verified 0/59 remaining).
3. Do plan 06-07's Task 3 quickly: confirm a couple of transit-classified posts are untouched (`git diff` clean for those paths across this worktree's whole commit range vs `main`), then it's fully done.
4. Merge `worktree-wave5-06-06` into `main`. Expect at most a trivial conflict in `.planning/data/phase6-verify.json` (same pattern as the 06-05 merge) — resolve by regenerating (`node scripts/verify-phase6.mjs`) after merge, not by hand-editing the JSON.
5. Post-merge: `npm run build` + `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls` on `main`.
6. Update `STATE.md`/`ROADMAP.md`: phase 6 goes from 5/8 to 7/8 plans complete. Next up is plan 06-08 (Barbara's review queue / publication gate).
7. Remove the worktree (`git worktree remove --force .claude/worktrees/wave5-06-06`) and delete its branch, same as the 06-05 cleanup pattern.
8. Note but do NOT do yet: the actual human review pass through all 59 narratives via `narrative-viewer.mjs` (adding real memories, dropping unwanted days, eventually flipping `draft:false`) is plan 06-08's job, separate from this bookkeeping.
</remaining_work>

<decisions_made>
See `decisions` array in `.planning/HANDOFF.json` — not duplicated here to avoid drift between the two files.
</decisions_made>

<blockers>
None. This is a natural pause point (context budget), not a stuck point — everything through the end of 06-07's generation work is done and verified.
</blockers>

## Required Reading (in order)
1. This file's BLOCKING CONSTRAINTS section (above) — again, before anything else
2. `.planning/HANDOFF.json` — structured version of the same state, plus the full decision log
3. `.planning/phases/06-new-post-generation/06-04-SUMMARY.md` — template for the two SUMMARY.md files still needed
4. `scripts/narrative-viewer.mjs` and `scripts/11-draft-narratives.mjs` — read before touching either; both changed significantly this session

## Infrastructure State
- Worktree `.claude/worktrees/wave5-06-06` (branch `worktree-wave5-06-06`) — clean, 16 commits ahead of `main`.
- No servers running (the `narrative-viewer.mjs` instance used for testing was stopped; restart with `npm run narrative-viewer` from inside the worktree when ready for the actual review pass in plan 06-08).
- `.planning/data/narrative-notes.json` (in the worktree) currently has: `familyNotes: "Barbara's husband is named Mike."` and one per-day memory on 2024-04-13. This is real, intentional data — not test residue.

<context>
This session covered a lot of ground beyond the original 06-06 plan: the user's checkpoint feedback ("need real-time editing," "need a keep/discard checkbox," "some people are actually specific family members," "there will be memories you won't infer") was substantial enough to justify building a whole new tool rather than just tweaking the prompt, and building that tool naturally unblocked "draft the text for all of these days" (06-07) plus "rotate/delete/zoom photos" as a fast follow. The mental model throughout: treat checkpoint feedback as real signal to extend the plan, not just noise to route around; keep the generator's grounding discipline (family notes + explicit per-day facts only, never guessed names) even while making it more capable; and when a bulk AI-generation run finally gets a full build check, budget time for at least one real defect to show up (it did) rather than assuming spot-checks alone are sufficient — the build itself is a cheap, decisive check that catches classes of error spot-reading prose can miss entirely.
</context>

<next_action>
Start with: write `06-06-SUMMARY.md`, using `06-04-SUMMARY.md` as the template and this file's <completed_work> section as the source material. Then `06-07-SUMMARY.md`. Then proceed through <remaining_work> items 3-7 in order.
</next_action>
