---
context: phase
phase: 06-new-post-generation
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-09-19T01:13:27.658Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: 06-06-PLAN.md's Task 1 acceptance criteria assumes `2024-04-13-day-723-florida-keys.mdx` has **19** R2 image URLs and its automated `<verify>` block greps for exactly `^(19|20)$`. The actual, real, already-uploaded file has **17** image URLs (all `.jpg`, zero `.mov`), confirmed by direct read this session. This is stale planning-time data, not a bug to chase — treat 17 as ground truth. Structural mitigation: when implementing/running Task 1's automated verify, replace the literal `19`/`20` expectation with the real count (17) for 2024-04-13, or just verify the dry-run's printed count matches the file's actual Gallery array length generically rather than hardcoding 19. Do not "fix" the post to have 19 images — the file is correct, the plan's number is not.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

_None yet identified for this plan beyond the constraint above._

<current_state>
Wave 5, plan 06-06 (narrative generation implementation + 2 pilot drafts + human voice sign-off). Task 1 of 3 ("Implement --generate mode") is in progress: all read-first research is done, but zero code has been written yet. `scripts/11-draft-narratives.mjs` in the worktree is still exactly the plan-06-03 triage-only version (the `--generate` branch is still a stub that prints a pointer message and exits 0).

Working in an isolated worktree: `.claude/worktrees/wave5-06-06` (branch `worktree-wave5-06-06`, branched from main at commit `56ae654`). `.env` already copied in (has `ANTHROPIC_API_KEY` and the R2 vars). `node_modules` already installed. Worktree git status is clean — nothing to reconcile, just keep working.

Before starting 06-06 implementation this session, two land-trip-defect corrections were made and already committed to **main** (commit `56ae654`, present in this worktree since it branched after that commit):
1. `.planning/data/narrative-triage.json` — reclassified the 7 land-trip dates (2023-08-02 through 2023-08-08) from `full`/`transit` to `sparse` + `locked: true`, since they were classified using stale pre-fix photo counts (8-52 photos) and plan 06-05 later found they have zero real voyage photos. This matters directly for 06-06: `--generate` mode must only ever act on `full`-classified days per the plan's own selection rule 1, so these 7 will now correctly be skipped rather than sent to the API with no images.
2. Corrected a "6 posts" → "7 posts" counting error in `06-05-SUMMARY.md`, `STATE.md`, and the project memory file (`project_phase6_gps_mismatch_defect.md`) — the land-trip range is 2023-08-02 through 2023-08-08 inclusive, which is 7 dates, not 6.
</current_state>

<completed_work>

Completed Tasks:
- (none of 06-06's 3 tasks are complete yet)

Read-first research for Task 1 (all done, informs the implementation about to happen):
- `scripts/07-quality-lift.mjs` lines ~39-63 (`getClient()` lazy API-key guard — copy verbatim) and ~216-236 (`messages.create` call shape + text-block extraction) and ~405-450 (report JSON shape)
- `.planning/phases/06-new-post-generation/06-RESEARCH.md` § Pattern 5 (multimodal prompt construction, image-then-text ordering, `type: "url"` image source blocks) and § Pitfall 5 (verse-fabrication risk, mitigated by the existing human-review gate, not by tooling)
- `src/content/blog/great-loop/2024-02-23-boca-chica-vogelzaangs-days-254-258.mdx` — voice anchor, read in full. First-person, warm, conversational, specific names/places, light humor. Closing verse example: `Psalms 104:14-15 NIV` on the line right after the quoted verse text, no blank line between.
- `src/content/blog/great-loop/2022-04-22-the-adventure-begins.mdx` — the other closing-verse format anchor named in the plan (not yet re-read this session, but its format — `Psalms 19:1 NIV` — is already documented in 06-RESEARCH.md's Code Examples section and matches the boca-chica post's pattern).
- `src/content/blog/great-loop/2024-04-13-day-723-florida-keys.mdx` — target pilot post 1 (Keys-to-New-Bern range). Current body: frontmatter has `draft: true, r2Uploaded: true`, no `miles`/`hours` fields at all, `<VoyageStats  />` with no props. Gallery has **17** image URLs, all `.jpg`, zero `.mov` — see blocking constraint above.
- `src/content/blog/great-loop/2023-07-16-day-451-north-channel-on.mdx` — target pilot post 2 (Canada range). Current body: `miles: 0.2, hours: 0.1`, `<VoyageStats miles={0.2} hours={0.1} />`. Gallery has 10 URLs: 8 `.jpg` + 2 `.mov`.
</completed_work>

<remaining_work>

- **Task 1** (in progress): Implement `--generate` mode in `scripts/11-draft-narratives.mjs` per 06-06-PLAN.md's full Task 1 `<action>` block — CLI flags (`--generate`, `--date` repeatable, `--limit`, `--force`, `--dry-run`), selection rules (triage-driven, skip already-drafted unless `--force`, hard-refuse when `draft: false` even with `--force`, skip+report `file://` leftovers), prompt construction (system prompt with 2-3 dated style excerpts from `migrated: true` posts + the verse-format anchor, per-range framing, grounding instruction against fabrication, `EXCERPT:` output contract), the API call itself, body rewrite (byte-preserve Gallery + VoyageStats blocks, set `narrativeDrafted: true`, never touch `draft`/`title`/`date`/etc.), and the report file. Then run Task 1's automated verify (adjusted per the blocking constraint above) and confirm all acceptance criteria.
- **Task 2** (not started): Run real generation for `2024-04-13` and `2023-07-16` (after confirming both are still classified `full` in `narrative-triage.json` — they should be, since the land-trip reclassification only touched the 7 August dates). Verify Gallery/VoyageStats byte-preservation against `git show HEAD:<path>`, frontmatter flags, prose length, verse format, per-range framing language, `npm run build`.
- **Task 3** (not started, blocking human-verify checkpoint): Present both full drafts + preview URLs to the user, ask about voice/fabrication/verse accuracy/length, apply any requested prompt revisions and regenerate with `--force` if needed, record the verdict. Do NOT proceed to plan 06-07 (bulk generation) without an explicit approval here.
</remaining_work>

<decisions_made>

- Worked directly in the worktree without spawning a subagent — same pattern as plan 06-05, since deep context from the whole day's session (including the land-trip defect discovery) is directly relevant and a fresh subagent would need to re-derive it.
- Chose to fix the narrative-triage.json land-trip misclassification *before* starting 06-06 implementation, on main, rather than as part of the 06-06 plan itself — it's a direct, necessary consequence of the already-completed 06-05 plan's fix, and leaving it stale would have silently sent the bulk generator at photo-less posts later.
</decisions_made>

<blockers>
- None blocking further autonomous work right now. Task 3 (two tasks away) requires the user to read two AI-drafted posts and give a voice/fabrication/verse-accuracy verdict — that is an expected, planned human-verify checkpoint, not an unexpected blocker.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-new-post-generation/06-06-PLAN.md` — the full plan, especially Task 1's `<action>` block (it's long and detailed — follow it precisely rather than reconstructing from memory of this handoff's summary)
2. `.planning/phases/06-new-post-generation/06-CONTEXT.md` §§ D-06 through D-10 — the human decisions this plan implements (full/transit split, triage-before-generation, mandatory verse attempt, direct-MDX-editing review, Barbara-flips-draft-flag-herself)
3. `scripts/11-draft-narratives.mjs` (whole file, currently ~328 lines) — the triage half whose helpers/Maps this plan's Task 1 reuses
4. `src/content/blog/great-loop/2022-04-22-the-adventure-begins.mdx` — re-read directly (only referenced via 06-RESEARCH.md's quoted excerpt so far this session, not opened itself)

## Critical Anti-Patterns (do NOT repeat these)
- STALE-PLANNING-DATA: 06-06-PLAN.md's acceptance criteria hardcode a photo count (19) for a specific post that was computed at planning time and is now wrong (real count is 17) → verify against the live file's actual Gallery array length, not the plan's hardcoded number, whenever a plan's acceptance criteria cite a specific data-derived count for content that has since changed.
- MISCOUNTING-WHILE-SUMMARIZING: Earlier this session, "2023-08-04 was under separate investigation" mentally decoupled it from the other 6 land-trip dates while writing the 06-05 summary, producing an off-by-one "6 posts" claim when the real number was 7. → When writing a summary that counts a list of items, recount from the actual file list (`ls`/`grep -c`) rather than from memory of how the finding unfolded narratively.

## Infrastructure State
- Worktree `.claude/worktrees/wave5-06-06` (branch `worktree-wave5-06-06`) exists, is clean, has `.env` and `node_modules` already in place. No background processes running. No dev server currently running (Task 3's checkpoint will need `npm run dev:drafts`, not yet started).
- `ANTHROPIC_API_KEY` and R2 env vars both confirmed present in `.env`.

<context>
This is the very start of implementing plan 06-06 — nothing risky has happened yet, no API calls made, no files written in the worktree. The mental model going in: this is a fairly mechanical port of `07-quality-lift.mjs`'s Claude API calling pattern into a new multimodal (image + text) prompt, with the interesting/risky parts being (a) getting the per-range framing instruction right so Keys-to-New-Bern reads as the Loop's finish and Canada reads as a return excursion, (b) faithfully preserving the Gallery/VoyageStats blocks byte-for-byte on rewrite, and (c) never letting generation touch `draft` or overwrite a `draft: false` post. The human checkpoint (Task 3) is the real quality gate — Task 1/2 just need to get a plausible, structurally-correct draft in front of the user without wasting API spend on defects.
</context>

<next_action>
Start with: open `.planning/phases/06-new-post-generation/06-06-PLAN.md` Task 1's `<action>` section and implement `--generate` mode in `scripts/11-draft-narratives.mjs` inside `.claude/worktrees/wave5-06-06`, following it section by section (CLI args → selection rules → prompt construction → API call → body rewrite → report). Run the dry-run acceptance criteria as you go, adjusting the 19→17 image-count expectation per the blocking constraint above.
</next_action>
