---
phase: 06-new-post-generation
plan: 07
type: execute
wave: 6
depends_on: ["06-06"]
files_modified:
  - src/content/blog/great-loop/*.mdx
  - .planning/data/narrative-draft-report.json
autonomous: true
requirements: [POST-03, POST-05]

must_haves:
  truths:
    - "Every day the user classified full-narrative has AI-drafted prose in Barbara's voice ending with an attempted Bible verse"
    - "Every day the user classified transit still has exactly its stats and gallery, untouched by the generator"
    - "No post was published by the generator — every in-scope post is still draft:true awaiting Barbara"
    - "Every generated post kept its R2 gallery and voyage stats byte-for-byte"
  artifacts:
    - path: ".planning/data/narrative-draft-report.json"
      provides: "Full-run generation results: processed, skipped, failed, images sent, sampled-down days"
      contains: "summary"
  key_links:
    - from: ".planning/data/narrative-triage.json classification"
      to: "which posts receive narrative"
      via: "generate mode selects classification == full only"
      pattern: "classification"
    - from: "generated posts"
      to: "scripts/verify-phase6.mjs NARRATIVE_DRAFTED_POSTS"
      via: "narrativeDrafted frontmatter flag"
      pattern: "narrativeDrafted"
---

<objective>
Run the approved narrative generator across every remaining full-narrative day in both in-scope ranges, and prove the transit days were left alone.

Purpose: POST-05 requires the Loop's finishing segment to be fully documented with AI-drafted narrative, and the Canada side trip needs the same treatment for its eventful days. This is the bulk execution of the prompt a human approved on two pilots in plan 06-06.

Output: roughly 62 more AI-drafted posts, all still `draft: true`, ready for Barbara.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-new-post-generation/06-CONTEXT.md
@.planning/phases/06-new-post-generation/06-03-SUMMARY.md
@.planning/phases/06-new-post-generation/06-06-SUMMARY.md
@scripts/11-draft-narratives.mjs
@scripts/verify-phase6.mjs

<interfaces>
<!-- Baseline counts measured at planning time. The authoritative numbers are whatever
     .planning/data/narrative-triage.json holds after the user's plan 06-03 reclassifications;
     06-03-SUMMARY.md records the final split. Use the file, not these numbers, to drive the run —
     these are here so a material deviation is visible rather than silent. -->

Planning-time classification baseline (100 in-scope posts):
- full: 64  (keys-to-new-bern 20, canada-side-trip 44)
- transit: 36 (keys-to-new-bern 7, canada-side-trip 29)
- sparse: 0

Already generated by the plan 06-06 pilot: 2 full days (2024-04-13, 2023-07-16).
Remaining for this plan at baseline: 62 full days — 19 in keys-to-new-bern, 43 in canada-side-trip.

Generation contract (scripts/11-draft-narratives.mjs, built in plan 06-06):
- selects only days with classification "full" from .planning/data/narrative-triage.json
- skips posts already carrying narrativeDrafted: true unless --force
- refuses to write to any post with draft: false, even with --force
- never sets or clears fm.draft
- reuses each post's <Gallery> and <VoyageStats> blocks byte-for-byte
- --date is repeatable, --limit caps posts per invocation, --dry-run makes no API call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate narratives for the remaining Keys-to-New-Bern full days</name>
  <files>src/content/blog/great-loop/2024-04*.mdx, src/content/blog/great-loop/2024-05*.mdx, .planning/data/narrative-draft-report.json</files>
  <read_first>
    - .planning/data/narrative-triage.json (the authoritative classification list after the user's reclassifications)
    - .planning/phases/06-new-post-generation/06-06-SUMMARY.md (the approved prompt characteristics and the verse-accuracy verdict)
    - scripts/11-draft-narratives.mjs (the generate path and its guards)
  </read_first>
  <action>
Generate narratives for every `full`-classified day in the Keys-to-New-Bern range (2024-04-13..2024-05-17) that does not already have `narrativeDrafted: true` — 19 days at the planning-time baseline, whatever `.planning/data/narrative-triage.json` actually says after the user's plan 06-03 decisions.

Derive the date list from the triage file, not from a hardcoded list. If the derived count differs from 19, report the difference and the reason (user reclassification) before proceeding — do not silently run a different set.

Run in batches of about 8 dates per invocation with explicit repeated `--date` arguments, so a failure or an interruption costs at most one batch: `npm run draft-narratives -- --generate --date <d1> --date <d2> ...`.

After each batch, read `.planning/data/narrative-draft-report.json`. For any failed post, confirm the file is unmodified (`git diff` clean for that path), retry that single date once, and if it fails again record the date and the exact error and move on. Never hand-write narrative into an MDX file, and never set `narrativeDrafted` by hand.

This range is the main Loop finishing at New Bern on 2024-05-17. Spot-read three generated posts spread across the range and confirm none of them frames the days as a side trip or excursion — that framing belongs only to the 2023 Canada range.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const tri=JSON.parse(fs.readFileSync('.planning/data/narrative-triage.json','utf8'));const want=tri.days.filter(d=>d.range==='keys-to-new-bern'&&d.classification==='full');const bad=[];for(const d of want){const p='src/content/blog/great-loop/'+d.slug+'.mdx';const t=fs.readFileSync(p,'utf8');if(!/^narrativeDrafted: true$/m.test(t))bad.push(d.slug+':not-drafted');if(!/^draft: true$/m.test(t))bad.push(d.slug+':draft-flipped');if(/side trip|excursion/i.test(t))bad.push(d.slug+':side-trip-framing')}console.log('KEYS_FULL='+want.length);if(bad.length){console.error(JSON.stringify(bad));process.exit(1)}console.log('keys narratives ok')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Every `full`-classified day in the keys-to-new-bern range has `narrativeDrafted: true`
    - Every one of those posts still has `draft: true`
    - No post in this range contains the strings "side trip" or "excursion"
    - Each generated post's `<Gallery images={[...]} />` block is byte-identical to its pre-generation state (spot-check at least 3 posts against `git show HEAD:<path>`)
    - `.planning/data/narrative-draft-report.json` `summary.failed` is empty, or each failure is named with its error in the SUMMARY and its file confirmed `git diff` clean
    - If the derived date count differs from the planning-time baseline of 19, the difference and its cause are stated in the SUMMARY
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The Loop's finishing segment has an AI-drafted narrative for every day the user chose, framed as the main Loop running to New Bern.</done>
</task>

<task type="auto">
  <name>Task 2: Generate narratives for the remaining Canada side-trip full days</name>
  <files>src/content/blog/great-loop/2023-06*.mdx, src/content/blog/great-loop/2023-07*.mdx, src/content/blog/great-loop/2023-08*.mdx, .planning/data/narrative-draft-report.json</files>
  <read_first>
    - .planning/data/narrative-triage.json (authoritative classifications)
    - .planning/phases/06-new-post-generation/06-06-SUMMARY.md (approved prompt characteristics)
    - .planning/data/narrative-draft-report.json (cumulative results after Task 1)
  </read_first>
  <action>
Generate narratives for every `full`-classified day in the Canada side-trip range (2023-06-05..2023-08-31) that does not already have `narrativeDrafted: true` — 43 days at the planning-time baseline. This is the larger half of the generation work.

Same discipline as Task 1: derive the date list from the triage file, batch about 8 dates per invocation with explicit `--date` arguments, inspect the report after each batch, retry a failed date once, record and move on after a second failure.

This range is the 2023 return to Canadian waters — the North Channel and Ontario leg around Jul 14-22 2023 — and is explicitly NOT the already-published 2022 Georgian Bay/Canada leg. Spot-read three generated posts spread across the range and confirm the prose treats it as a return excursion rather than a first visit, and that it does not claim to be the 2022 trip.

Also spot-check for the failure mode the human flagged in plan 06-06: fabricated named people, restaurants, or marinas that the photos and Nebo data do not support. If you find fabrication in the spot-checks, stop and report rather than generating the rest of the batch.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const tri=JSON.parse(fs.readFileSync('.planning/data/narrative-triage.json','utf8'));const want=tri.days.filter(d=>d.range==='canada-side-trip'&&d.classification==='full');const bad=[];for(const d of want){const p='src/content/blog/great-loop/'+d.slug+'.mdx';const t=fs.readFileSync(p,'utf8');if(!/^narrativeDrafted: true$/m.test(t))bad.push(d.slug+':not-drafted');if(!/^draft: true$/m.test(t))bad.push(d.slug+':draft-flipped')}console.log('CANADA_FULL='+want.length);if(bad.length){console.error(JSON.stringify(bad));process.exit(1)}console.log('canada narratives ok')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Every `full`-classified day in the canada-side-trip range has `narrativeDrafted: true` and still has `draft: true`
    - Spot-checks of 3 posts spread across June, July, and August 2023 confirm return-excursion framing and no claim of being the 2022 Georgian Bay leg
    - Spot-checks found no fabricated named people, restaurants, or marinas unsupported by the photos or Nebo data; anything suspicious is reported rather than accepted
    - `<Gallery>` blocks byte-identical to pre-generation state in at least 3 spot-checked posts
    - `summary.failed` is empty, or each failure is named with its error and its file confirmed `git diff` clean
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Every eventful Canada side-trip day has an AI-drafted narrative framed as a return to Canadian waters.</done>
</task>

<task type="auto">
  <name>Task 3: Prove transit days were untouched and the phase state is consistent</name>
  <files>.planning/data/phase6-verify.json, .planning/data/narrative-draft-report.json</files>
  <read_first>
    - .planning/data/narrative-triage.json (the transit-day list that must be unmodified)
    - scripts/verify-phase6.mjs (counter definitions)
    - .planning/data/narrative-draft-report.json (final cumulative results)
  </read_first>
  <action>
Confirm the generator did exactly what the triage said and nothing more.

1. For every day classified `transit`, confirm its post has NO `narrativeDrafted` flag set to true and that its body still consists only of the two imports, the `<VoyageStats ... />` block, and the `<Gallery images={[...]} />` block — no prose was added. D-06 says these days get stats and gallery only.
2. Confirm `NARRATIVE_DRAFTED_POSTS` equals the number of `full`-classified days in the triage file.
3. Confirm `DRAFT_POSTS=100` — the generator published nothing. Barbara flips drafts in plan 06-08.
4. Confirm `FILE_URL_POSTS=0` still holds — narrative generation must not have disturbed any Gallery URL.
5. Record in the SUMMARY: total posts generated, total API calls, total images sent, days that were sampled down from more than 20 images, any failures, and any day whose classification differed from the planning-time baseline.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const tri=JSON.parse(fs.readFileSync('.planning/data/narrative-triage.json','utf8'));const full=tri.days.filter(d=>d.classification==='full');const transit=tri.days.filter(d=>d.classification==='transit');const bad=[];for(const d of transit){const t=fs.readFileSync('src/content/blog/great-loop/'+d.slug+'.mdx','utf8');if(/^narrativeDrafted: true$/m.test(t))bad.push(d.slug+':unexpected-narrative');const body=t.split(/\n---\n/)[1]||'';const stripped=body.replace(/import [^\n]*\n/g,'').replace(/<VoyageStats[^>]*\/>/g,'').replace(/<Gallery images=\{\[[\s\S]*?\]\}\s*\/>/g,'').trim();if(stripped.length>0)bad.push(d.slug+':unexpected-body-content')}console.log('FULL='+full.length+' TRANSIT='+transit.length);if(bad.length){console.error(JSON.stringify(bad));process.exit(1)}console.log('transit days untouched')" && node scripts/verify-phase6.mjs | grep -E "^DRAFT_POSTS=100$" && node scripts/verify-phase6.mjs | grep -E "^FILE_URL_POSTS=0$" && node scripts/verify-phase6.mjs --gate posts --gate no-file-urls && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - No `transit`-classified post has `narrativeDrafted: true`, and every transit post's body contains nothing beyond the two imports, the VoyageStats tag, and the Gallery block
    - `NARRATIVE_DRAFTED_POSTS` equals the count of `full`-classified days in `.planning/data/narrative-triage.json`
    - `DRAFT_POSTS=100` — nothing was published by the generator
    - `FILE_URL_POSTS=0` — no Gallery URL was disturbed
    - `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls` exits 0
    - The SUMMARY records total generated, total images sent, sampled-down days, failures, and classification deviations from the baseline
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Generation matched the approved triage exactly: full days drafted, transit days untouched, nothing published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script -> Anthropic API | ~62 requests carrying the API key and public photo URLs |
| model output -> blog content | Generated prose is written into files that will be published under Barbara's name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Spoofing | Fabricated events or misquoted scripture at scale across ~62 posts | mitigate | The prompt was human-approved on two pilots in plan 06-06 with fabrication and verse accuracy explicitly judged. Each task here spot-reads three generated posts for fabrication and stops the run if it finds any. The structural backstop is that nothing is published: `DRAFT_POSTS=100` is asserted after the run, and plan 06-08 puts Barbara on every post before publication |
| T-06-15 | Tampering | Bulk regeneration clobbering earlier work | mitigate | Runs without `--force`, so any post already carrying `narrativeDrafted: true` is skipped, and any post with `draft: false` is refused outright. Failures leave files byte-identical, verified with `git diff` before each single retry |
| T-06-13 | Information Disclosure | API key across ~62 batched requests | mitigate | Key sourced only from gitignored `.env` via `process.env` in the lazy `getClient()`; never logged, never written into `narrative-draft-report.json` |
| T-06-12 | Denial of Service | Runaway API spend | mitigate | The run is bounded by the `full`-classified set in the user-approved triage file, batched at about 8 posts per invocation with the report inspected between batches, capped at 20 images and 4000 max_tokens per request |
| T-06-17 | Tampering | Narrative insertion corrupting R2 Gallery URLs | mitigate | The generator reuses the existing `<Gallery>` and `<VoyageStats>` blocks as exact substrings rather than regenerating them; Task 3 asserts `FILE_URL_POSTS=0` and spot-checks byte-identity against `git show HEAD:<path>` |
</threat_model>

<verification>
1. Every `full`-classified day has `narrativeDrafted: true`; every `transit` day does not.
2. Transit-day bodies contain only imports, VoyageStats, and Gallery.
3. `DRAFT_POSTS=100`, `FILE_URL_POSTS=0`.
4. `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls` exits 0.
5. `npm run build` exits 0.
</verification>

<success_criteria>
- All full-narrative days in both ranges have AI-drafted prose with an attempted closing verse.
- Keys-to-New-Bern posts carry finishing-the-Loop framing; Canada posts carry return-excursion framing.
- Transit days are provably untouched.
- Nothing published; all 100 in-scope posts still `draft: true`.
- Gallery URLs and voyage stats preserved byte-for-byte.
</success_criteria>

<output>
Create `.planning/phases/06-new-post-generation/06-07-SUMMARY.md` when done. Record total generated, images sent, sampled-down days, failures, and any classification deviation from the planning-time baseline.
</output>
