---
phase: 06-new-post-generation
plan: 08
type: execute
wave: 7
depends_on: ["06-07"]
files_modified:
  - .planning/data/phase6-review-queue.md
  - src/content/blog/great-loop/*.mdx
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [POST-01, POST-02, POST-03, POST-05]

descoped_requirements:
  - id: POST-04
    reason: "Undocumented middle (Days 112-124) is out of scope per 06-CONTEXT.md D-05. Task 3 of this plan flags it in REQUIREMENTS.md traceability for movement to Out of Scope at the next /gsd:transition. No implementation work was done for it in this phase."
  - id: QLFT-05
    reason: "Effectively reversed per 06-CONTEXT.md D-00 — the 72 original Blogger posts are frozen and their Phase-4-added Gallery blocks were removed in commit fe69136. Flagged alongside POST-04 in Task 3."

must_haves:
  truths:
    - "Barbara has read, edited, and approved every in-scope post before it is publicly visible"
    - "Zero draft:true posts remain in the two in-scope date ranges, while the ~187 out-of-scope drafts stay untouched"
    - "The voyage index runs from Day 1 through the New Bern return on 2024-05-17 with no unexplained gap in the two in-scope ranges"
    - "POST-04 and QLFT-05 are visibly flagged as descoped rather than silently dropped"
    - "A production build emits all newly published posts and contains no draft content on any surface — post pages, blog index, homepage Recent Posts, voyage index, voyage map, or RSS feed"
  artifacts:
    - path: ".planning/data/phase6-review-queue.md"
      provides: "Ordered review checklist for all 100 in-scope posts with preview URL, classification, photo count, and Nebo route"
      min_lines: 110
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows flagging POST-04 and QLFT-05 as descoped-pending-transition, and adding the missing POST-05 and QLFT-05 rows"
      contains: "POST-05"
  key_links:
    - from: "Barbara's manual draft flip"
      to: "scripts/verify-phase6.mjs --gate no-drafts"
      via: "DRAFT_POSTS reaching 0 in the in-scope ranges only"
      pattern: "no-drafts"
    - from: "scripts/check-draft-leak.mjs"
      to: "dist/index.html, dist/blog/index.html, dist/voyages/great-loop/index.html, dist/rss.xml"
      via: "phase-closing assertion that no draft slug reaches any listing surface"
      pattern: "DRAFT_LEAKS=0"
    - from: ".planning/REQUIREMENTS.md traceability"
      to: "next /gsd:transition"
      via: "DESCOPED status markers naming 06-CONTEXT.md D-05 and D-00"
      pattern: "DESCOPED"
---

<amendment date="2026-09-23">
Scope reconciled before execution. Commit e1b44c4 (2026-09-22, owner request) removed 221 photo-only
"Day N" stubs site-wide, including 41 in-scope transit/sparse days; 2023-06-07 was dropped earlier via
narrative-viewer. `scripts/verify-phase6.mjs` now records all 41 as known gaps (KNOWN_GAPS=43), so the
in-scope set is the **58 narrativeDrafted posts**. Numbers throughout this plan that assumed 100 posts
should be read as follows: review queue = 58 checkboxes (derived from the 58 existing in-scope MDX files,
not the 100-day narrative-triage.json); published before this plan = 79; target build = **137** post
pages; out-of-scope drafts to leave untouched = 7 (not ~190). The explained-gap list in Task 3 must include
the 41 e1b44c4 dates.

Owner-confirmed land side trips (not boat days) inside the 58 — flag at the top of the queue for Barbara
to reframe or drop: 2023-08-14 (Quad Cities/Rock Island IL), 2023-08-30 (near Cincinnati),
2023-08-31 (near Fayetteville NC), 2024-04-21 (near Fayetteville NC — likely Raeford family visit).
</amendment>

<objective>
Put every in-scope post in front of Barbara, get them published, and close the phase with honest requirement accounting.

Purpose: POST-03 is the whole point of the phase — no AI narrative ships unreviewed. This plan makes the review tractable (an ordered queue with preview links), gates on Barbara completing it, then proves the two in-scope ranges are fully published while the ~187 explicitly out-of-scope drafts remain untouched.

Output: 100 published posts, a green `--gate no-drafts`, and REQUIREMENTS.md flagged for the POST-04 / QLFT-05 descope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-new-post-generation/06-CONTEXT.md
@.planning/phases/06-new-post-generation/06-06-SUMMARY.md
@.planning/phases/06-new-post-generation/06-07-SUMMARY.md
@scripts/verify-phase6.mjs

<interfaces>
<!-- State entering this plan, and the completion contract. -->

Entering state (asserted green at the end of plan 06-07):
- IN_SCOPE_DATES=101, KNOWN_GAPS=1 (2023-08-09), IN_SCOPE_POSTS=100, MISSING_POSTS=0
- FILE_URL_POSTS=0, R2_UPLOADED_POSTS=100, DRAFT_POSTS=100
- NARRATIVE_DRAFTED_POSTS = the count of full-classified days in narrative-triage.json (64 at baseline)
- Out-of-scope drafts elsewhere in src/content/blog/great-loop/: ~190, which must remain draft:true

Review workflow (06-CONTEXT.md D-09, D-10): Barbara edits MDX files directly — no new review UI — and
flips draft: true to draft: false herself when she is satisfied with a post. Nothing in this plan flips
that flag on her behalf.

Preview: `npm run dev:drafts` (added in plan 06-04) starts the dev server with drafts visible at
http://localhost:4321/blog/<slug>. A plain `npm run build` still excludes drafts — on every surface,
because plan 06-04 also added the draft predicate to `src/pages/index.astro`,
`src/pages/voyages/great-loop/index.astro`, and `src/pages/rss.xml.js`, which previously had none.

Leak gate: `node scripts/check-draft-leak.mjs` (also from plan 06-04) asserts `DRAFT_LEAKS=0` against
`dist/index.html`, `dist/blog/index.html`, `dist/voyages/great-loop/index.html`, and `dist/rss.xml`,
and prints per-surface link counts (`HOME_LINKS`, `BLOG_INDEX_LINKS`, `VOYAGE_INDEX_LINKS`, `RSS_ITEMS`).
A post-page count alone does NOT prove ROADMAP success criterion 4 — it passes even when every draft is
leaking through the homepage, voyage index, and feed.

Completion contract: `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls --gate no-drafts`
exits 0. DRAFT_POSTS counts only the two in-scope ranges, so the ~190 out-of-scope drafts cannot
false-positive this gate.

REQUIREMENTS.md traceability gaps found during planning (Task 3 fixes these):
- POST-05 has no row in the traceability table at all, despite being a Phase 6 requirement
- QLFT-05 has no row in the traceability table at all, despite being a v1 requirement
- The table's footer claims "Mapped to phases: 33 / Unmapped: 0", which is inconsistent with those two omissions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the review queue</name>
  <files>.planning/data/phase6-review-queue.md</files>
  <read_first>
    - .planning/data/narrative-triage.json (classification, photo counts, route names, locations per day)
    - scripts/verify-phase6.mjs (in-scope date derivation to reuse)
    - .planning/phases/06-new-post-generation/06-CONTEXT.md D-08, D-09, D-10 (what Barbara is being asked to do)
    - .planning/phases/06-new-post-generation/06-06-SUMMARY.md (the verse-accuracy verdict, which determines how carefully verses must be checked)
  </read_first>
  <action>
Generate `.planning/data/phase6-review-queue.md`, an ordered checklist Barbara works through. Write it with a small one-off Node command or by hand from the triage data — it does not need to be a permanent script, but it must be generated from the actual data rather than typed from memory.

Structure:
- A short header explaining the workflow: preview with `npm run dev:drafts`, edit the MDX file directly, flip `draft: true` to `draft: false` when satisfied, and re-run `npm run verify-phase6` any time to see remaining count.
- A "what to check" list, calibrated by what plan 06-06's human review actually found: is the voice right; are there invented people, restaurants, or marinas; is the Bible verse quoted accurately and does the citation match the text; do the photos belong to that day.
- Two sections, one per range, each with the range's dates and a one-line reminder of its framing — Keys-to-New-Bern is the main Loop finishing at New Bern on 2024-05-17, Canada is the 2023 return excursion to Canadian waters distinct from the published 2022 Georgian Bay leg.
- Within each section, one checklist line per post in date order: an unchecked markdown checkbox, the date, the local preview URL `http://localhost:4321/blog/<slug>`, the classification (`full` or `transit`), the photo count, and the Nebo route name when one exists.
- A closing note that the single in-scope date without a post, 2023-08-09, is a documented gap (GPS track but zero photos) and needs no action.

Order full-narrative days before transit days within each range, since the full ones need real reading while transit days only need a glance at photos and stats.
  </action>
  <verify>
    <automated>test -f .planning/data/phase6-review-queue.md && node -e "const fs=require('fs');const q=fs.readFileSync('.planning/data/phase6-review-queue.md','utf8');const tri=JSON.parse(fs.readFileSync('.planning/data/narrative-triage.json','utf8'));const boxes=(q.match(/^- \[ \] /gm)||[]).length;console.log('CHECKBOXES='+boxes+' TRIAGE_DAYS='+tri.days.length);if(boxes!==tri.days.length)process.exit(1);let missing=[];for(const d of tri.days){if(!q.includes(d.slug))missing.push(d.slug)}if(missing.length){console.error(JSON.stringify(missing.slice(0,5)));process.exit(1)}if(!q.includes('2023-08-09'))process.exit(1);console.log('review queue complete')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/data/phase6-review-queue.md` contains exactly one unchecked checkbox per in-scope post (100 at baseline), matching `narrative-triage.json` day count
    - Every post slug from the triage file appears in the queue
    - Each line carries the preview URL, classification, photo count, and Nebo route name where one exists
    - Both ranges are present as separate sections with their correct framing reminders (main Loop finish vs. Canada return excursion)
    - The queue names `2023-08-09` as a documented no-action gap
    - The "what to check" list explicitly includes verifying the Bible verse quote and citation, and checking for invented people/places
  </acceptance_criteria>
  <done>Barbara has an ordered, data-derived checklist with a working preview link for every post she needs to review.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Barbara reviews, edits, and publishes every in-scope post</name>
  <files>src/content/blog/great-loop/*.mdx, .planning/data/phase6-review-queue.md</files>
  <read_first>
    - .planning/data/phase6-review-queue.md (the checklist being worked)
    - .planning/phases/06-new-post-generation/06-CONTEXT.md D-03 (no AI narrative ships unreviewed), D-09, D-10
  </read_first>
  <what-built>
    All 100 in-scope posts now have R2-hosted photo galleries that render, voyage stats from Nebo data, and — for the days classified full-narrative — AI-drafted prose in Barbara's voice ending with an attempted Bible verse. Every one is still `draft: true`, so none of them is visible on the live site. `.planning/data/phase6-review-queue.md` is an ordered checklist with a preview link for each.

    This step is genuinely human-only: it is editorial judgment plus factual correction from memory, and D-10 reserves the `draft` flag flip for Barbara.
  </what-built>
  <how-to-verify>
    1. Start the preview server: `npm run dev:drafts`
    2. Open `.planning/data/phase6-review-queue.md` and work down the list.
    3. For each post: open its preview URL, read it, then edit the `.mdx` file directly in `src/content/blog/great-loop/` to correct facts, add personal memory and texture, and adjust voice.
       - Correct or replace the closing Bible verse. The AI's citation is an unverified attempt — check that the quoted text and the reference actually match.
       - Watch for invented specifics: named people, restaurants, marinas, or events that did not happen.
       - For transit days there is no prose to edit — just confirm the photos and stats look right.
    4. When a post is finished, change `draft: true` to `draft: false` in its frontmatter and tick its checkbox in the queue file.
    5. Any time you want to see progress, run `npm run verify-phase6` — `DRAFT_POSTS` counts only these two ranges, so it will read 0 when you are done. The other ~190 unrelated draft stubs elsewhere in the blog are deliberately out of scope and should stay as they are.
    6. Reply `done` when `npm run verify-phase6` reports `DRAFT_POSTS=0`. This can span multiple sessions — there is no need to finish in one sitting.
  </how-to-verify>
  <action>
    Present the review queue path, the preview command, and the current `DRAFT_POSTS` count. Do not edit any post's prose, and do not flip any `draft` flag — both are Barbara's alone under D-09 and D-10.

    If Barbara asks for help on a specific post, assist with that post only and leave its `draft` flag for her to flip.

    When she replies done, re-run the verification and, if any in-scope post is still `draft: true`, report exactly which ones by slug rather than treating the gate as passed.
  </action>
  <verify>
    <automated>node scripts/verify-phase6.mjs --gate no-drafts && node scripts/verify-phase6.mjs | grep -E "^DRAFT_POSTS=0$" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase6.mjs --gate no-drafts` prints `GATE_PASS no-drafts` and exits 0
    - `DRAFT_POSTS=0` for the two in-scope ranges
    - The count of `draft: true` posts OUTSIDE the two in-scope ranges is unchanged from before this plan (roughly 190) — the out-of-scope stubs were not touched, per D-05
    - No post's `draft` flag was flipped by Claude — every flip is attributable to Barbara's own edit
    - `npm run build` exits 0 and now emits 137 blog post pages (79 previously published + 58 newly published), counted as `find dist/blog -mindepth 2 -name index.html`
  </acceptance_criteria>
  <resume-signal>Type "done" when `npm run verify-phase6` reports `DRAFT_POSTS=0`, or ask for help with a specific post.</resume-signal>
  <done>Every in-scope post is human-reviewed and published; no AI narrative shipped unreviewed.</done>
</task>

<task type="auto">
  <name>Task 3: Final phase verification and requirement descope flags</name>
  <files>.planning/REQUIREMENTS.md, .planning/data/phase6-verify.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole file — the v1 requirement list and the Traceability table with its coverage footer)
    - .planning/phases/06-new-post-generation/06-CONTEXT.md § canonical_refs (the QLFT-05 note) and D-05 (the POST-04 descope)
    - .planning/ROADMAP.md § Phase 6 Success Criteria (the four criteria this task checks against)
    - scripts/verify-phase6.mjs (the gates being run)
  </read_first>
  <action>
Close the phase: prove all four ROADMAP success criteria, then record the two descoped requirements honestly.

1. Run `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls --gate no-drafts` and require exit 0 with all three `GATE_PASS` lines.

2. Check ROADMAP Phase 6 success criterion 2 — no unexplained date gaps. Within the two in-scope ranges, list every calendar date that has no published post and confirm each is explained: `2023-08-09` is the documented GPS-track-but-zero-photos gap, and any other date in the range has no entry in `src/data/daily-routes.json` at all (meaning no photo or GPS evidence exists for it). Record the full explained-gap list in the SUMMARY rather than asserting "no gaps".

3. Confirm a production build is clean and complete on EVERY surface, not just the post pages. This is ROADMAP success criterion 4 and a post-page count alone does not prove it.
   - `npm run build` exits 0.
   - `find dist/blog -mindepth 2 -name index.html` returns exactly 137 (79 previously published plus the 58 newly published). `-mindepth 2` is required: an unqualified `find dist/blog -name index.html` also counts `dist/blog/index.html`, the listing page, and returns 138.
   - `node scripts/check-draft-leak.mjs` (from plan 06-04) exits 0 with `DRAFT_LEAKS=0`, `RSS_ITEMS=137`, `VOYAGE_INDEX_LINKS=137`, and `BLOG_INDEX_LINKS=137`. This is the assertion that actually closes the criterion: before plan 06-04's fix, `dist/index.html`, `dist/voyages/great-loop/index.html`, and `dist/rss.xml` had no draft filter at all and shipped all 322 posts while the post-page count still read correct.
   - None of the ~190 out-of-scope draft stubs may appear on any of those four surfaces — that exposure is exactly what D-05 forbids. If `DRAFT_LEAKS` is non-zero, report the offending slugs and the surface each leaked onto and stop; do not proceed to step 4.

4. Edit `.planning/REQUIREMENTS.md`:
   - In the Traceability table, change POST-04's Status from `Pending` to `DESCOPED (Phase 6) — see .planning/phases/06-new-post-generation/06-CONTEXT.md D-05; move to Out of Scope at next /gsd:transition`. Do not delete the row and do not edit the v1 requirement bullet text.
   - Add a missing Traceability row for `QLFT-05 | Phase 3 | DESCOPED — reversed by 06-CONTEXT.md D-00 (72 original posts frozen, Phase-4 Gallery blocks removed in commit fe69136); move to Out of Scope at next /gsd:transition`. QLFT-05 currently has no row in that table at all.
   - Add a missing Traceability row for `POST-05 | Phase 6 | Complete`. POST-05 currently has no row in that table at all.
   - Update the Status of POST-01, POST-02, and POST-03 to `Complete`.
   - Correct the Coverage footer so the "Mapped to phases" and "Unmapped" numbers match the table after these additions, and add a one-line note that two v1 requirements are flagged DESCOPED pending the next transition.
   Do NOT move anything into the "Out of Scope" section in this plan — that is the transition command's job. This task only makes the descope visible and traceable so it cannot be silently forgotten.

5. Do not create tasks or content for POST-04. The "undocumented middle" (Days 112-124, Holland MI Aug 2022, and the rest of the Jan 2023 - Apr 2024 middle, roughly 187 draft stubs) stays exactly as it is per D-05.
  </action>
  <verify>
    <automated>node scripts/verify-phase6.mjs --gate posts --gate no-file-urls --gate no-drafts && npm run build && test $(find dist/blog -mindepth 2 -name 'index.html' | wc -l | tr -d ' ') -eq 137 && OUT=$(node scripts/check-draft-leak.mjs) && echo "$OUT" && echo "$OUT" | grep -qE '^DRAFT_LEAKS=0$' && echo "$OUT" | grep -qE '^RSS_ITEMS=137$' && echo "$OUT" | grep -qE '^VOYAGE_INDEX_LINKS=137$' && echo "$OUT" | grep -qE '^BLOG_INDEX_LINKS=137$' && grep -q 'POST-04' .planning/REQUIREMENTS.md && grep -c 'DESCOPED' .planning/REQUIREMENTS.md | grep -qE '^[2-9]' && grep -q 'POST-05' .planning/REQUIREMENTS.md && grep -q 'QLFT-05 | Phase 3' .planning/REQUIREMENTS.md && echo 'requirements flagged'</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls --gate no-drafts` exits 0 with three `GATE_PASS` lines
    - `npm run build` exits 0 and `find dist/blog -mindepth 2 -name index.html` returns exactly 137 post pages
    - `node scripts/check-draft-leak.mjs` exits 0 printing `DRAFT_LEAKS=0`, `RSS_ITEMS=137`, `VOYAGE_INDEX_LINKS=137`, and `BLOG_INDEX_LINKS=137`
    - `dist/index.html` Recent Posts, `dist/voyages/great-loop/index.html`, and `dist/rss.xml` contain zero links to any `draft: true` slug — including all ~190 out-of-scope stubs
    - Every in-scope calendar date without a published post is listed in the SUMMARY with its explanation (no photo/GPS evidence, or the documented 2023-08-09 gap)
    - `.planning/REQUIREMENTS.md` Traceability contains a POST-04 row whose Status begins with `DESCOPED` and names 06-CONTEXT.md D-05
    - `.planning/REQUIREMENTS.md` Traceability contains a new QLFT-05 row whose Status begins with `DESCOPED` and names D-00 and commit fe69136
    - `.planning/REQUIREMENTS.md` Traceability contains a new POST-05 row marked Complete, and POST-01/02/03 are marked Complete
    - The Coverage footer's numbers are arithmetically consistent with the table after the additions
    - The "Out of Scope" section of REQUIREMENTS.md is unmodified (moving items there is the transition command's job)
    - The count of `draft: true` posts outside the two in-scope ranges is unchanged — no out-of-scope stub was touched
  </acceptance_criteria>
  <done>All four ROADMAP Phase 6 success criteria are proven, and the two descoped requirements are flagged in writing for the next transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft content -> live public site | 100 posts cross into public visibility in this plan |
| generated prose -> content attributed to a real person | Published text carries Barbara's name and voice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Spoofing | Fabricated events or misquoted scripture reaching the public site | mitigate | Barbara reads and edits every post before flipping its `draft` flag; Claude is explicitly forbidden from flipping the flag on her behalf. The review queue names verse-citation checking and invented people/places as required checks. This is the phase's primary control and it is a blocking human gate, not an advisory one |
| T-06-10 | Information Disclosure | Out-of-scope draft stubs accidentally published alongside the in-scope ones, via post pages OR the homepage / voyage index / RSS feed | mitigate | `PREVIEW_DRAFTS` is never set in `netlify.toml` or CI. Two independent assertions: the production build emits exactly 137 post pages (not 322), and `scripts/check-draft-leak.mjs` proves no `draft: true` slug reaches `dist/index.html`, `dist/blog/index.html`, `dist/voyages/great-loop/index.html`, or `dist/rss.xml`. The second is load-bearing: the page count alone passed while all three listing surfaces were leaking every draft, which was the pre-06-04 state of the build |
| T-06-18 | Repudiation | Silently dropping POST-04 and QLFT-05 without a record | mitigate | Task 3 writes explicit `DESCOPED` status rows into REQUIREMENTS.md traceability naming the deciding decisions (D-05, D-00) and the commit that reversed QLFT-05, so the next `/gsd:transition` has an auditable trail rather than an unexplained absence |
| T-06-03 | Information Disclosure | Non-voyage personal photos becoming publicly visible on the live site | mitigate | Final human control: Barbara looks at every gallery during review and can remove any photo that does not belong before publishing. The upstream control was per-UUID export scoping in plans 06-02 and 06-05 |
</threat_model>

<verification>
1. `node scripts/verify-phase6.mjs --gate posts --gate no-file-urls --gate no-drafts` exits 0.
2. `npm run build` exits 0 and `find dist/blog -mindepth 2 -name index.html` returns exactly 137 post pages.
2b. `node scripts/check-draft-leak.mjs` exits 0 with `DRAFT_LEAKS=0`, `RSS_ITEMS=137`, `VOYAGE_INDEX_LINKS=137`, `BLOG_INDEX_LINKS=137`.
3. Out-of-scope draft count unchanged at roughly 190.
4. REQUIREMENTS.md traceability contains DESCOPED rows for POST-04 and QLFT-05, plus a POST-05 row.
5. Every in-scope date without a post is explained in the SUMMARY.
</verification>

<success_criteria>
- Zero `draft: true` posts remain in the two in-scope ranges; the ~190 out-of-scope drafts are untouched.
- The voyage index runs from Day 1 through the New Bern return on 2024-05-17 with every in-scope gap explained.
- Production build green at exactly 137 post pages, with `DRAFT_LEAKS=0` proving no draft reaches the homepage, blog index, voyage index, or RSS feed either.
- POST-01, POST-02, POST-03, POST-05 marked Complete; POST-04 and QLFT-05 flagged DESCOPED with their deciding references, ready for the next `/gsd:transition`.
</success_criteria>

<output>
Create `.planning/phases/06-new-post-generation/06-08-SUMMARY.md` when done. Record the explained-gap list, the final published post count, and the exact REQUIREMENTS.md edits made.
</output>
