---
phase: 01-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/layouts/Layout.astro
  - src/layouts/BlogPost.astro
  - src/components/BaseHead.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/404.astro
  - src/pages/blog/[...id].astro
  - src/pages/voyages/great-loop/index.astro
  - src/content/blog/great-loop/2024-01-01-test-scaffold.mdx
autonomous: false
requirements: [FOUND-04, SITE-01, SITE-03, SITE-04, SITE-05]
must_haves:
  truths:
    - "Visiting / shows a styled nautical homepage with hero, recent posts, and footer"
    - "Visiting /about, /voyages/great-loop/, and a /blog/<id> route returns real pages, not 404s"
    - "The dummy MDX post with all frontmatter fields renders at its URL with correct heading hierarchy"
    - "Nav uses Lora/Inter fonts, navy/off-white/brass palette, and is responsive (hamburger under 768px)"
    - "No login or auth gate on any page"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "Base page shell wrapping Header, slot, Footer with BaseHead"
      min_lines: 15
    - path: "src/components/Header.astro"
      provides: "Responsive nautical nav (Home / Great Loop / About)"
      contains: "great-loop"
    - path: "src/pages/blog/[...id].astro"
      provides: "Individual post dynamic route via getStaticPaths + getCollection"
      contains: "getStaticPaths"
    - path: "src/pages/voyages/great-loop/index.astro"
      provides: "Voyage index stub listing posts (map added Phase 5)"
      contains: "great-loop"
    - path: "src/content/blog/great-loop/2024-01-01-test-scaffold.mdx"
      provides: "Dummy post exercising every schema field"
      contains: "migrated:"
  key_links:
    - from: "src/pages/blog/[...id].astro"
      to: "blog collection"
      via: "getCollection('blog') in getStaticPaths"
      pattern: "getCollection\\('blog'\\)"
    - from: "src/pages/index.astro"
      to: "/voyages/great-loop/"
      via: "hero CTA + view-all link"
      pattern: "voyages/great-loop"
    - from: "src/layouts/Layout.astro"
      to: "src/components/Header.astro"
      via: "import + render"
      pattern: "Header"
---

<objective>
Build the base layout, nav/footer components, and all stub pages (homepage, about, voyage index, individual post route, 404), plus a dummy MDX post that exercises every schema field — styled per the UI-SPEC nautical contract.

Purpose: Delivers the visible, navigable, responsive site shell. Satisfies FOUND-04 (layout/typography), SITE-01 (public, no auth), SITE-03 (post pages), SITE-04 (about), and makes SITE-05 (multi-voyage routing) observable.
Output: A fully renderable site — every route returns a real page, the dummy post validates and renders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-CONTEXT.md
@.planning/phases/01-scaffolding/01-UI-SPEC.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-01-SUMMARY.md

<interfaces>
<!-- From plan 01-01 (already built). Use these directly. -->

Content schema (src/content.config.ts) — blog entry data fields:
  title: string, date: Date, voyage: string, location: string, excerpt: string, migrated: boolean
  lat?: number, lon?: number, coverPhoto?: string, anchorage?: string, marina?: string
  Entry identifier: post.id  (NOT post.slug — Astro 5+ Content Layer)

Render a post body: import { render } from 'astro:content'; const { Content } = await render(post);
Query posts: import { getCollection } from 'astro:content';

CSS theme variables available globally (src/styles/global.css @theme):
  --color-navy #1a2e4a, --color-offwhite #faf9f6, --color-brass #c9a84c,
  --color-ink #1c1c1e, --color-muted #6b7280, --color-danger #b91c1c
  --font-lora (serif body), --font-inter (sans headings/UI)
Tailwind 4 maps @theme color tokens to utilities: bg-navy, text-brass, border-navy, font-lora, font-inter, etc.

Fonts: rendered via <Font cssVariable="--font-lora" preload /> and <Font cssVariable="--font-inter" preload />
  imported from 'astro:assets' — place inside BaseHead.astro <head>.
</interfaces>

<route_contract>
<!-- Routes that MUST resolve (UI-SPEC Layout Contracts). -->
/                          → homepage (hero + recent posts + footer)
/about/                    → About Jackie B III
/voyages/great-loop/       → voyage index stub
/blog/<post.id>            → individual post (dynamic, from dummy post)
404                        → "Page not found — navigate home or browse the Great Loop."
</route_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build layout shell, nav, footer, and font/head plumbing</name>
  <files>src/components/BaseHead.astro, src/components/Header.astro, src/components/Footer.astro, src/layouts/Layout.astro, src/layouts/BlogPost.astro</files>
  <read_first>
    - .planning/phases/01-scaffolding/01-UI-SPEC.md (Layout Contracts: Navigation, Footer; Typography; Color; Accessibility; Interaction Contracts)
    - .planning/phases/01-scaffolding/01-01-SUMMARY.md (what plan 01 produced — font config, theme tokens)
    - src/styles/global.css (theme tokens to reference)
    - src/components/Header.astro (template ships one — read then replace with nautical nav)
    - src/layouts/Layout.astro (template may ship one — read before replacing)
  </read_first>
  <action>
    Create BaseHead.astro: a `<head>` partial taking title and description props. Import `{ Font }` from 'astro:assets' and render `<Font cssVariable="--font-lora" preload />` and `<Font cssVariable="--font-inter" preload />`. Import the global stylesheet (`import '../styles/global.css'`). Include charset, viewport, generator meta, canonical link, and per-page `<title>` and description meta.

    Create Header.astro per UI-SPEC Navigation contract: full-width `<header>` with navy background (bg-navy), 64px height desktop / 56px mobile. Site name "JACKIE B III" left in Inter 700, brass, 18px (links to /). Nav links right in order Home → Great Loop → About, pointing to `/`, `/voyages/great-loop/`, `/about/`, in Inter 700 white 18px. Wrap in `<nav aria-label="Main">`. Active link gets `aria-current="page"` with a 2px brass underline (style via `[aria-current="page"]`). Hover: brass underline (`hover:underline decoration-[#c9a84c]`). Under 768px: hide horizontal links, show a hamburger using a `<details>/<summary>` element (no JS) with `aria-label="Toggle navigation"`; the open panel stacks links navy background, each link min-h-[44px]. Accept the current path as a prop (e.g. `Astro.url.pathname`) to set aria-current.

    Create Footer.astro per UI-SPEC: full-width navy `<footer>`, copyright "© 2022–2024 Jackie B III Going Loopy" in Inter 400, 14px, white at 70% opacity. No social links.

    Create Layout.astro: the base page shell. Props: title, description. Renders `<html lang="en">` → `<head>`(BaseHead) → `<body>` containing Header (passing current pathname), a `<main>` with `<slot />`, and Footer. Body already styled by global.css.

    Create BlogPost.astro layout for individual posts per UI-SPEC Post Page Layout: wraps Layout, single column max-width 720px centered (max-w-[720px] mx-auto). Renders post title (Inter 700, 28px, ink) as `<h1>`, a meta row (date + location, Inter 400, 14px, muted), then a `<slot />` for the rendered MDX body (Lora 400, 18px, line-height 1.7). Accepts the blog entry data fields as props. Ensure prose `<a>` links use brass and `<h2>` uses Inter 700 20px (scope via a wrapper class so it doesn't affect nav).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 | grep -viE "^#" | grep -ciE "error|✗" | grep -qx 0 && grep -q "great-loop" src/components/Header.astro && grep -q "astro:assets" src/components/BaseHead.astro && echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/components/BaseHead.astro imports `{ Font }` from `astro:assets` and renders Font components for both `--font-lora` and `--font-inter`
    - src/components/Header.astro contains `<nav aria-label="Main"` and links to `/`, `/voyages/great-loop/`, `/about/`
    - src/components/Header.astro uses a `<details>` element for the mobile hamburger (assert: contains `<details`)
    - src/components/Footer.astro contains the copyright string `Jackie B III Going Loopy`
    - src/layouts/Layout.astro imports and renders Header and Footer and contains `<slot`
    - src/layouts/BlogPost.astro contains `max-w-[720px]` (or equivalent 720px max-width utility) and an `<h1`
    - `npm run build` completes with zero errors
  </acceptance_criteria>
  <done>Reusable layout shell with responsive navy nav (Home/Great Loop/About + hamburger), footer, font-loading head, and a 720px-centered post layout — all using the nautical theme tokens; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Build pages, dynamic post route, and the dummy validation post</name>
  <files>src/pages/index.astro, src/pages/about.astro, src/pages/404.astro, src/pages/blog/[...id].astro, src/pages/voyages/great-loop/index.astro, src/content/blog/great-loop/2024-01-01-test-scaffold.mdx</files>
  <read_first>
    - .planning/phases/01-scaffolding/01-UI-SPEC.md (Homepage Layout, Voyage Index Page Stub, About Page, Copywriting Contract)
    - .planning/phases/01-scaffolding/01-RESEARCH.md (Code Examples: Dummy Validation Post, Querying Blog Posts, Voyage Index Page Stub; Pattern 2 dynamic route)
    - src/layouts/Layout.astro and src/layouts/BlogPost.astro (created in Task 1 — props contract)
    - src/content.config.ts (schema field names for the dummy post frontmatter)
  </read_first>
  <action>
    Create the dummy post at `src/content/blog/great-loop/2024-01-01-test-scaffold.mdx` exercising EVERY schema field (this is the FOUND-02 / SITE-03 validation artifact). Frontmatter: title "Scaffold Test Post — delete before launch", date 2024-01-01, voyage "great-loop", location "Chesapeake Bay, MD", excerpt (one sentence), migrated false, lat 38.9, lon -76.4, coverPhoto "https://example.com/placeholder.jpg", anchorage "test anchorage", marina "test marina". Body: an `# H1` and an `## H2` plus a paragraph, so heading hierarchy renders. It lives in the great-loop subdirectory to prove SITE-05 routing.

    Create src/pages/blog/[...id].astro per RESEARCH Pattern 2: `export async function getStaticPaths()` mapping `await getCollection('blog')` to `{ params: { id: post.id }, props: { post } }`. In the component, `const { post } = Astro.props; const { Content } = await render(post);` then render `<BlogPost {...post.data}><Content /></BlogPost>`. Use `post.id` NOT `post.slug`.

    Create src/pages/index.astro homepage per UI-SPEC Homepage Layout, wrapped in Layout: (1) Hero — full-width navy, centered: display heading "Going Loopy" Inter 700 text-[36px] off-white; subhead "Jackie B III on the Great Loop, 2022–2024" Inter 400 18px muted; CTA link "Read the voyage" → /voyages/great-loop/ brass Inter 700 18px; padding 64px desktop / 48px mobile. (2) Recent Posts — centered max-w-[720px]: heading "Recent Posts" Inter 700 20px; query `getCollection('blog')` filtered to voyage === 'great-loop', sorted by date desc, sliced to 5; render each as a card (date label muted 14px + title link brass Inter 700 20px → /blog/<id> + excerpt Lora 18px + location muted 14px), separated by 1px navy rule; if empty show the empty-state copy "No posts yet. The voyage begins soon."; "View all posts" link → /voyages/great-loop/ brass Inter 700 18px. (3) Footer comes from Layout.

    Create src/pages/voyages/great-loop/index.astro per UI-SPEC Voyage Index Page Stub, wrapped in Layout: page title "Great Loop" Inter 700 28px; subhead "2022–2024 · 5,424 nm" Inter 400 14px muted; then `getCollection('blog')` filtered to voyage === 'great-loop' sorted by date ASC, rendered as the same card pattern linking to /blog/<post.id>. Add an HTML comment marking where the Phase 5 map will mount. Do NOT add a map (deferred to Phase 5).

    Create src/pages/about.astro per UI-SPEC About Page, wrapped in Layout: `<h1>` "About Jackie B III" Inter 700 28px; single column max-w-[720px]; 2–3 placeholder Lora paragraphs about the boat, crew, and Great Loop. Stub content only.

    Create src/pages/404.astro wrapped in Layout: heading and the copy "Page not found — navigate home or browse the Great Loop." with links home and to /voyages/great-loop/.

    No auth, no login, no forms anywhere (SITE-01).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 | grep -viE "^#" | grep -ciE "error|✗" | grep -qx 0 && grep -q "getStaticPaths" "src/pages/blog/[...id].astro" && grep -q "post.id" "src/pages/blog/[...id].astro" && grep -q "migrated:" src/content/blog/great-loop/2024-01-01-test-scaffold.mdx && test -f dist/about/index.html && test -f dist/voyages/great-loop/index.html && echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/content/blog/great-loop/2024-01-01-test-scaffold.mdx contains all 11 frontmatter fields (title, date, voyage, location, excerpt, migrated, lat, lon, coverPhoto, anchorage, marina) and an `# ` H1 and `## ` H2
    - src/pages/blog/[...id].astro contains `getStaticPaths`, `getCollection('blog')`, and uses `post.id` (NOT `post.slug`)
    - src/pages/index.astro contains "Going Loopy", "Read the voyage", and filters by `voyage === 'great-loop'`
    - src/pages/voyages/great-loop/index.astro contains "Great Loop" and "5,424 nm"
    - src/pages/about.astro contains `<h1` with "About Jackie B III"
    - src/pages/404.astro contains "Page not found"
    - `npm run build` completes with zero errors AND zero schema validation warnings
    - Build output contains dist/about/index.html, dist/voyages/great-loop/index.html, and a dist/blog/.../index.html for the dummy post id
  </acceptance_criteria>
  <done>Homepage, about, voyage index, 404, and the dynamic /blog/<id> route all render real pages; the dummy MDX post validates against the schema and renders with correct heading hierarchy; build green with zero schema warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Astro site shell built and styled: nautical-themed homepage (hero "Going Loopy", recent posts, footer), responsive navy nav with mobile hamburger, About page, Great Loop voyage index, dynamic post pages, and a 404. Fonts are Lora (serif body) + Inter (sans headings). Palette is navy #1a2e4a / off-white #faf9f6 / brass #c9a84c accents.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the printed localhost URL.
    2. Homepage (/): confirm the navy hero with "Going Loopy", a "Read the voyage" brass link, a "Recent Posts" section showing the Scaffold Test Post card, and a navy footer. Body text should be serif (Lora); nav/headings sans-serif (Inter).
    3. Resize the browser below 768px (or DevTools mobile): the horizontal nav collapses to a hamburger; tapping it reveals stacked Home / Great Loop / About links.
    4. Click into the Scaffold Test Post: confirm the post page is single-column, centered, with a 28px title, a date+location meta row, and visible H1/H2 heading hierarchy in the body.
    5. Visit /about/ and /voyages/great-loop/: both render real styled pages (not 404). The voyage index lists the test post.
    6. Visit a made-up URL (e.g. /nope): the 404 page shows "Page not found — navigate home or browse the Great Loop."
    7. Confirm NO login prompt or auth gate appears anywhere (SITE-01).
    8. Confirm colors match: navy header/footer, off-white page background, brass links.
  </how-to-verify>
  <resume-signal>Type "approved" if the layout, fonts, colors, responsiveness, and routes all look correct, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX content → rendered HTML | Post frontmatter and body are rendered into pages |
| Visitor → static site | Public unauthenticated read access (SITE-01 by design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | MDX body rendering (render() from astro:content) | accept | Content is authored in-repo (not user-submitted); Astro escapes MDX output by default. No untrusted input crosses this boundary in Phase 1 |
| T-01-04 | Information Disclosure | coverPhoto / external image URLs in dummy post | accept | Placeholder example.com URL only; real cloud photo URLs are public-by-design (MEDIA decisions) — no secrets |
| T-01-05 | Elevation of Privilege | Site auth surface | mitigate | No auth layer, no forms, no server endpoints exist — static SSG eliminates the privilege-escalation surface entirely (SITE-01) |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No NEW package installs in this plan (all deps installed in 01-01). N/A — no install tasks here |
</threat_model>

<verification>
- `npm run build` exits 0 with zero errors and zero schema warnings
- dist/ contains index.html, about/index.html, voyages/great-loop/index.html, 404 page, and a blog post page for the dummy post
- Human checkpoint confirms nautical styling, fonts, responsive nav, and all routes
</verification>

<success_criteria>
- Every route in the route contract resolves to a real page
- Dummy post validates all 11 schema fields and renders with H1/H2 hierarchy
- Site is responsive and uses Lora/Inter + navy/off-white/brass per UI-SPEC
- No auth gate anywhere (SITE-01)
- Multi-voyage routing visible (post served from great-loop subdirectory — SITE-05)
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding/01-02-SUMMARY.md` when done
</output>
