---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - tsconfig.json
  - .nvmrc
  - .gitignore
  - src/content.config.ts
  - src/styles/global.css
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths:
    - "npm run build completes with zero errors on a fresh Astro 7 project"
    - "The blog collection loads MDX from src/content/blog/great-loop/ (subdirectory-per-voyage)"
    - "All required and optional frontmatter fields are declared in the Zod schema"
    - "Nautical color tokens and Lora/Inter fonts are available as CSS variables"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config with MDX, sitemap, Tailwind Vite plugin, and Google Fonts (Lora + Inter)"
      contains: "@tailwindcss/vite"
    - path: "src/content.config.ts"
      provides: "blog + voyages content collections with Zod schemas"
      contains: "src/content/blog/great-loop"
    - path: "src/styles/global.css"
      provides: "Tailwind import + nautical @theme color tokens"
      contains: "@theme"
    - path: ".nvmrc"
      provides: "Node 22 pin for Netlify"
      contains: "22"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/blog/great-loop/"
      via: "glob loader base path"
      pattern: "base:.*great-loop"
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite plugins array"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Scaffold the Astro 7.x static site from the official blog template, add Tailwind CSS 4, configure Google Fonts (Lora + Inter), and replace the template's content schema with the multi-voyage blog + voyages collections.

Purpose: Establishes the buildable project foundation and the validated content contract every subsequent phase consumes. Phase 2 migrated posts land in the schema defined here.
Output: A buildable Astro 7 project with content.config.ts, global.css theme tokens, and configured fonts. No layout/pages yet (that is plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-CONTEXT.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-UI-SPEC.md

<version_note>
Live version check on 2026-06-27 (research dated 2026-03-23, past its 30-day validity):
- astro: 7.0.3 (research assumed 6.0.8) — same Content Layer API, same `src/content.config.ts`, same `post.id`, same `import { z } from 'astro/zod'`
- @astrojs/mdx: 7.0.0
- tailwindcss + @tailwindcss/vite: 4.3.1
- Node engines requirement unchanged: >=22.12.0 (installed Node is v24 — satisfies it)
- Astro Fonts API is now STABLE and TOP-LEVEL in defineConfig (`fonts: [...]`), NOT behind `experimental:`. Use the top-level `fonts` key. (Research hedged that it might be experimental in 6.0.8.)
Use Astro 7.x. Do NOT pin to 6.x.
</version_note>

<interfaces>
<!-- Content schema contract. Plan 02's pages and dummy post implement against these field names. -->

blog collection schema fields (from CONTEXT.md D-08 through D-11):
  Required: title (string), date (coerce.date), voyage (string slug e.g. "great-loop"),
            location (string), excerpt (string), migrated (boolean, default false)
  Optional: lat (number), lon (number), coverPhoto (string), anchorage (string), marina (string)

voyages collection schema fields:
  name (string), slug (string), startDate (coerce.date), endDate (coerce.date optional), description (string)

Import Zod via: import { z } from 'astro/zod'   (NOT from 'zod')
Glob loader base for blog: './src/content/blog/great-loop'
Glob loader base for voyages: './src/content/voyages'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 7 blog template and add Tailwind 4 + integrations</name>
  <files>package.json, package-lock.json, astro.config.mjs, tsconfig.json, .nvmrc, .gitignore</files>
  <read_first>
    - package.json (existing — contains @anthropic-ai/sdk and data-pipeline scripts that MUST be preserved/merged, not overwritten)
    - .planning/phases/01-scaffolding/01-RESEARCH.md (Installation section, Pattern 3, Pitfall 3)
    - .planning/phases/01-scaffolding/01-CONTEXT.md (D-01 Tailwind, D-04/D-05 fonts)
  </read_first>
  <action>
    Scaffold the official Astro blog template into the repo root WITHOUT clobbering the existing data-pipeline package.json. Because the repo root already has package.json (with @anthropic-ai/sdk dependency and the assess-photos/photo-viewer scripts) plus scripts/, .planning/, and node_modules/, scaffold into a temp directory and merge: run `npm create astro@latest temp-astro -- --template blog --no-install --no-git --skip-houston`, then move the generated astro.config.mjs, tsconfig.json, src/, public/, and the astro-relevant package.json fields into the repo root. Merge package.json: keep existing `dependencies.@anthropic-ai/sdk` and the existing `scripts.assess-photos` and `scripts.photo-viewer`; ADD astro's dependencies (astro ^7, @astrojs/mdx ^7, @astrojs/rss, @astrojs/sitemap, sharp) and astro's scripts (dev, start, build, preview, astro). Then run `npm install`.

    Add Tailwind 4 via `npx astro add tailwind --yes` — this installs `tailwindcss` ^4 and `@tailwindcss/vite` ^4 and wires the Vite plugin into astro.config.mjs. Verify package.json shows tailwindcss ^4.x (NOT ^3.x) and that NO tailwind.config.mjs file was created (a config file is a Tailwind 3 artifact per Pitfall 3). If `@astrojs/tailwind` appears in dependencies, remove it — it is deprecated and pulls Tailwind 3.

    Create `.nvmrc` containing exactly `22` (Netlify reads this; Astro 7 requires Node >=22.12.0 — Node 20 fails the build).

    Edit astro.config.mjs to add the Google Fonts config using the STABLE top-level `fonts` key (not `experimental.fonts`). Import `fontProviders` from 'astro/config'. Configure two providers via `fontProviders.google()`: Lora (cssVariable '--font-lora', weights ['400','700'], styles ['normal','italic']) for serif body, and Inter (cssVariable '--font-inter', weights ['400','700']) for sans headings/UI. Set `site` to a placeholder Netlify URL (e.g. 'https://jackieb3.netlify.app'). Keep the mdx() and sitemap() integrations from the template.

    Ensure .gitignore ignores dist/ and node_modules/ (the template's .gitignore covers this; merge if a root .gitignore already exists). Delete the temp-astro scaffold directory when done.
  </action>
  <verify>
    <automated>npx astro --version 2>&1 | grep -E "^(astro )?7\." && grep -q '"@tailwindcss/vite"' package.json && grep -q '"@astrojs/mdx"' package.json && test "$(cat .nvmrc)" = "22" && ! test -f tailwind.config.mjs && echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro --version` reports a 7.x version
    - package.json `dependencies` contains `astro` (^7), `@astrojs/mdx`, `@tailwindcss/vite` (^4), `tailwindcss` (^4)
    - package.json STILL contains `@anthropic-ai/sdk` and scripts `assess-photos` and `photo-viewer` (data pipeline preserved)
    - package.json `scripts` contains `dev`, `build`, `preview`
    - `.nvmrc` file content is exactly `22`
    - No `tailwind.config.mjs` file exists at repo root
    - `@astrojs/tailwind` does NOT appear anywhere in package.json
    - astro.config.mjs contains a top-level `fonts:` array (not `experimental: { fonts`) with `fontProviders.google()` for both Lora and Inter, and `cssVariable` values `--font-lora` and `--font-inter`
  </acceptance_criteria>
  <done>Astro 7 project scaffolded into repo root with Tailwind 4 Vite plugin, MDX, sitemap, Google Fonts (Lora + Inter) configured, Node pinned to 22, and the pre-existing data-pipeline package.json contents preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define content collection schema and nautical theme tokens</name>
  <files>src/content.config.ts, src/styles/global.css, src/content/blog/great-loop/.gitkeep, src/content/voyages/.gitkeep</files>
  <read_first>
    - .planning/phases/01-scaffolding/01-RESEARCH.md (Pattern 1 full schema, Pattern 3 CSS tokens, Pitfall 1, Pitfall 5)
    - .planning/phases/01-scaffolding/01-CONTEXT.md (D-08 through D-11 field optionality)
    - .planning/phases/01-scaffolding/01-UI-SPEC.md (CSS Custom Properties block — exact hex values and font variable names)
    - src/content.config.ts (the template ships its own — read it, then fully replace it per RESEARCH Open Question 3)
  </read_first>
  <behavior>
    - A dummy MDX post with all 11 fields present passes Zod validation (build succeeds)
    - A post missing `title` (a required field) fails Zod validation (build errors)
    - A post with only the 6 required fields and no optional fields passes validation
    - `getCollection('blog')` resolves entries from src/content/blog/great-loop/ only
  </behavior>
  <action>
    Fully REPLACE the blog-template's src/content.config.ts (which has the template's title/description/pubDate/heroImage schema — not our schema) with the project schema. The file MUST be at `src/content.config.ts` (project root level), NOT `src/content/config.ts` — the old Astro 4 path is silently ignored (Pitfall 1).

    Import `{ defineCollection }` from 'astro:content', `{ glob }` from 'astro/loaders', and `{ z }` from 'astro/zod' (NOT from 'zod' — Pitfall 5: Astro bundles Zod and a different version causes mismatch errors on Netlify).

    Define `blog` collection: `loader: glob({ pattern: '**/*.{md,mdx}', base: './src/content/blog/great-loop' })`. Schema fields per D-11 (required): title z.string(), date z.coerce.date(), voyage z.string(), location z.string(), excerpt z.string(), migrated z.boolean().default(false). Per D-08/D-09/D-10 (optional via .optional()): lat z.number(), lon z.number(), coverPhoto z.string(), anchorage z.string(), marina z.string(). Use `.optional()` not `.nullable()` (RESEARCH Open Question 2).

    Define `voyages` collection: `loader: glob({ pattern: '**/*.{md,mdx}', base: './src/content/voyages' })`. Schema: name z.string(), slug z.string(), startDate z.coerce.date(), endDate z.coerce.date().optional(), description z.string().

    Export `{ blog, voyages }` as `collections`.

    Create empty content directories with .gitkeep so the glob base paths exist at build time: src/content/blog/great-loop/.gitkeep and src/content/voyages/.gitkeep.

    Replace/create src/styles/global.css with `@import "tailwindcss";` followed by a `@theme {}` block declaring the exact tokens from UI-SPEC: --color-navy #1a2e4a, --color-offwhite #faf9f6, --color-brass #c9a84c, --color-ink #1c1c1e, --color-muted #6b7280, --color-danger #b91c1c, --font-lora ('Lora', Georgia, serif), --font-inter ('Inter', system-ui, sans-serif). Add a body rule setting background var(--color-offwhite), color var(--color-ink), font-family var(--font-lora), font-size 18px, line-height 1.7 (UI-SPEC base body contract). Do NOT create tailwind.config.mjs — all config is CSS-native in Tailwind 4.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 | grep -viE "^#" | grep -ciE "error|✗" | grep -qx 0 && grep -q "base: './src/content/blog/great-loop'" src/content.config.ts && grep -q "astro/zod" src/content.config.ts && grep -q "@theme" src/styles/global.css && echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts` exists (NOT `src/content/config.ts`)
    - src/content.config.ts imports z from `astro/zod` (assert: file contains `from 'astro/zod'`)
    - src/content.config.ts blog loader base is `./src/content/blog/great-loop` (assert: contains `base: './src/content/blog/great-loop'`)
    - src/content.config.ts declares all 6 required fields (title, date, voyage, location, excerpt, migrated) and all 5 optional fields (lat, lon, coverPhoto, anchorage, marina) with `.optional()`
    - src/content.config.ts exports `collections` containing both `blog` and `voyages`
    - src/styles/global.css contains `@import "tailwindcss"` and an `@theme` block with `--color-navy: #1a2e4a`, `--color-offwhite: #faf9f6`, `--color-brass: #c9a84c`
    - `npm run build` completes with zero errors (no real posts yet — empty collection is valid)
    - No `tailwind.config.mjs` exists
  </acceptance_criteria>
  <done>Content schema at src/content.config.ts validates the multi-voyage blog + voyages collections against the great-loop subdirectory; nautical theme tokens and fonts are available as CSS variables; npm run build is green with empty collections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code executes at install and build time |
| Google Fonts provider → build | Astro Fonts API fetches font files from Google at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (astro, @astrojs/mdx, @astrojs/sitemap, @astrojs/rss, sharp, tailwindcss, @tailwindcss/vite) | accept | All packages are first-party Astro/Tailwind org packages from the official `astro add` and blog template — no [ASSUMED]/[SUS] third-party packages introduced (no Package Legitimacy Audit required; no non-org installs). package-lock.json pins exact versions. |
| T-01-01 | Information Disclosure | Google Fonts at build time | mitigate | Astro Fonts API downloads fonts locally at build (self-hosted output) — no client-side request to Google's CDN, no visitor IP leak to Google |
| T-01-02 | Denial of Service | astro.config.mjs `site` field | accept | Static SSG output, public read-only site (SITE-01) — no server, no attack surface beyond static asset hosting |
</threat_model>

<verification>
- `npm run build` exits 0 with zero errors and zero schema warnings
- `npx astro --version` reports 7.x
- package.json retains pre-existing data-pipeline dependency and scripts
- No tailwind.config.mjs and no @astrojs/tailwind (Tailwind 4, not 3)
</verification>

<success_criteria>
- Astro 7 project builds clean from repo root
- Content schema at correct path with all 11 blog fields + voyages collection
- Subdirectory-per-voyage glob base (great-loop) wired (satisfies SITE-05 foundation)
- Nautical theme tokens + Lora/Inter fonts available
</success_criteria>

<output>
Create `.planning/phases/01-scaffolding/01-01-SUMMARY.md` when done
</output>
