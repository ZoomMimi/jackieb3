---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-09-23T14:04:56.112Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session — none discovered through failure that require an understanding check._

## Critical Anti-Patterns

_None new this session. See project memory (outside this repo) for prior-session anti-patterns already documented: `project_days65_72_chronology_cluster.md`, `feedback_quality_lift_script_scope.md`._

<current_state>
This was a live-review session, not phase/plan execution. The user browsed the running dev server and flagged two things in real time: a broken-looking dark theme, and a data bug on a specific post. Both were investigated and fixed. Working tree is clean, everything committed and pushed to `origin/main` through `f1a22a0`. No task is currently in progress.
</current_state>

<completed_work>

Completed this session (all committed and pushed):
- R2-uploaded the two 2022-06-22/23 gap-day stubs (`bbf9029`) — also fixed `scripts/10-upload-r2.mjs` so an explicit `--date` overrides the Phase-6 date-range guard instead of silently no-op'ing.
- Fixed "Georgian Bay Leg 3 Little Current, Baie Finn"'s date, 2022-08-04 → real 2022-07-29 (`516c695`), confirmed via an exact `nebo-logs.json` route-name text match ("Killarney to Little Current"). Also replaced its contaminated Charlevoix-MI coordinates with the real Little Current position.
- Diagnosed the user's "fonts/colors are broken, darker, harder to read" report as a stale Service Worker (cache `bhs77-v1-*`, from an unrelated reunion-website project that once shared port 4321) intercepting `localhost:4321` and layering an old dark theme on top of this site's real content. Unregistered it and cleared its caches via the browser devtools — no codebase change, no commit.
- Fixed a live-spotted date collision (`f1a22a0`): "A Week in the Big City" (a no-movement Baltimore stay) shared `date: 2022-05-17` with "Into Baltimore's Harbor" (the real travel day), so both pulled the same Nebo map/stats. Moved the stay-recap to 2022-05-18 and stripped its borrowed miles/hours. While investigating, also found "Into Baltimore's Harbor" itself had the *wrong* static miles/hours (27.7nm/3.5h — actually 2022-05-25's numbers) even though its dynamic max/avg-speed/weather were already correct; fixed to the real 05-17 log (21.6nm/3.1h). Also stripped the same bogus duplicate numbers from "A Wedding Trip to Colorado" (a flight, not a boating day — no Nebo entry exists for 05-19 at all).
</completed_work>

<remaining_work>

- Georgian Bay Leg 1 (07-27, confirmed) vs Leg 2 (currently 08-01, confirmed wrong) day-count conflict — the two posts' own narratives together describe more anchored days than the confirmed real-GPS gap between 07-27 and 07-29 (Leg 3's now-fixed departure date) allows. Map/log text evidence is exhausted; needs photo EXIF timestamps.
- "North Channel: Benjamins, Gore Bay" (currently 2022-08-06, confirmed wrong) narrows to 2022-07-30 or 2022-07-31 — no Nebo log exists for either day to break the tie. The three real-photo-GPS "Day 99/100/101 — North Channel ON" draft stubs are the next lead.
- Mighty Mississippi (2023-10-21) — confirmed wrong coordinate (~250mi off), real date/location not yet investigated via the map-image method.
- Cape May (Days 58-62) / Sandy Hook (Day 63) duplicate lat/lon + mileage — old item, untouched this session.
- Official roadmap: Phase 06 plan 06-08 (Barbara's review queue + publication gate) — has a PLAN.md but no SUMMARY, never started. 58 posts still `draft: true`, 41 in-scope dates missing entirely.
</remaining_work>

<decisions_made>

- `scripts/10-upload-r2.mjs`: an explicit `--date` now always overrides the Phase-6 RANGES guard, so one-off posts outside the two Phase-6 date ranges can still be uploaded by name (previously silently processed 0 posts).
- A no-movement multi-day stay recap post should carry no `miles`/`hours` frontmatter at all — `VoyageStats.astro` renders nothing when all its props are undefined, matching the pre-existing convention already used by "Guests of a Private Island Club".
- General mechanism now confirmed for "wrong map shown" reports: `BlogPost.astro` derives one `dateKey` from a post's frontmatter `date` and uses it to key *both* `neboMaps[dateKey]` and `neboDayStats[dateKey]`, with no per-post override. Two posts sharing a `date` will always share a map and dynamic stats. Any future "wrong/duplicate map" report should start by checking for a date collision with another real post.
</decisions_made>

<blockers>
None. Two remaining items (Georgian Bay Leg 1/2, Benjamins) are gated on needing photo EXIF timestamps rather than being stuck — not a blocker, just the next required evidence source.
</blockers>

## Required Reading (in order)
1. `project_days65_72_chronology_cluster.md` (in the user's Claude memory directory, outside this repo) — full evidence trail for the Georgian Bay Leg 1/2/3 + Benjamins cluster, including exactly what map/log evidence has already been checked. Do not re-derive this from scratch.

## Critical Anti-Patterns (do NOT repeat these)
- None new this session.

## Infrastructure State
- Astro dev server running: `astro dev --host` with `PREVIEW_DRAFTS=1`, pid 94248, `http://localhost:4321`. Left running for the user's continued live review — no need to restart on resume unless the user reports it's gone.
- If a future session hears "the site looks broken / dark / hard to read" on this machine again: check for a stale Service Worker registration on `localhost:4321` from a *different* local project before assuming a CSS regression (see completed work above — this cost real investigation time before the actual cause was found).

<context>
The user is doing ad-hoc live QA against the running dev server rather than working through a formal task list — expect more one-off bug reports in this style. The Georgian Bay/Benjamins cluster is the biggest remaining piece of real work and is well-documented; everything else is either a quick independent fix (Mississippi, Cape May/Sandy Hook) or the larger, separate 06-08 publication-gate effort.
</context>

<next_action>
Wait for the user's next live-review report, or if picking a direction proactively: pull photo EXIF timestamps (via `photo-index.json` / the Photos library) for the Georgian Bay Leg 1/2 window (2022-07-27 through 08-01) and the Benjamins window (07-30/07-31) to resolve items 5-6 with real evidence rather than guessing.
</next_action>
