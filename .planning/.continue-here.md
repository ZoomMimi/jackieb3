---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-09-22T18:23:39.000Z
---

<current_state>
Ad-hoc data-quality/content session on the Great Loop blog, not tied to a formal GSD phase — continuing directly from a prior session's handoff and running for many hours more. 16 commits made and pushed to `origin/main` this session, working tree clean. No uncommitted work, no blockers. Full detail in `.planning/HANDOFF.json`.
</current_state>

<completed_work>
Roughly chronological:
- 7 more real A-to-B location fixes from a more thorough site-wide pass (Baltimore/Middle River, Cape May/Sandy Hook, NY Harbor, Grafton IL, Sandestin, Clearwater, Key West)
- Found and fixed a **second, larger date-cascade bug**: Brewerton through Orillia (2022-07-02 through 07-19) — the posts' own `date` fields (not just the map images fixed in the prior session) were off by 1-2 days from the real Nebo GPS track. Verified all 11 posts against the actual Nebo map screenshots + narrative content, same rigorous method as before. Realigned `nebo-maps.json` keys afterward so the map images stayed correctly attached.
- Removed 5 individually-flagged + 221 bulk-removed redundant unpublished "Day N" draft stubs (checked all 279 first, kept the 58 with real Phase 6 narrative content untouched)
- Backfilled missing Distance/Underway stats on 9 posts; a few title/location corrections along the way ("Entering the Trent-Severn Waterway," "Entering the Chesapeake-Delaware Canal")
- **Found and fixed a site-wide OCR column-swap bug** in `.planning/data/nebo-logs.json`: map place-name text got OCR'd in between the Distance/Avg Speed/Max Speed column headers and their values, scrambling the parse. Audited all 171 trip-log entries, fixed 41 of 46 flagged (5 were genuine false positives, hand-verified). Two Phase 6 narrative posts had the wrong distance baked directly into their prose ("just 2 nautical miles" when it was really 62.8) — rewrote those sentences, not just the frontmatter.
- Retitled/corrected two more posts per the user's own first-hand trip memory: "Onto the Erie Canal" (New Baltimore NY to Waterford NY) and "Waving to Miss Liberty" (Sandy Hook NJ to Half Moon Bay Marina, Croton-on-Hudson NY — confirmed the real marina name via web search)
- Found a genuine **2-day content gap**: 2022-06-22 and 06-23 have real Nebo maps and GPS data but no blog post at all. Built two draft stubs from real local Photos-library photos (52 + 28 items, queried directly from the macOS Photos.sqlite database), verified they slot correctly into the prev/next chronology.
</completed_work>

<remaining_work>
1. **Georgian Bay Leg 1/2/3 + North Channel Benjamins cluster** (currently dated 2022-07-27, 08-01, 08-04, 08-06) — doubly confirmed wrong this session by an *independent* data source (real Nebo trip-log text, not just GPS reverse-geocoding): 08-04 and 08-06 are genuinely Michigan days, but those two posts' written content is Ontario. Real dates traced via GPS to roughly 07-25–07-31 but **not yet map-image-verified** the way the Jul 2-19 cascade was — that's the next concrete step, using the same proven method (read the real `.planning/data/nebo-maps/*.jpg` screenshots directly, cross-check against post content).
2. **Mighty Mississippi** (2023-10-21) — same class of bug, independently reconfirmed: real Nebo voyage that day is Paducah KY / Green Turtle Bay Marina, not the Grafton/Alton IL the post describes. Needs its real date figured out.
3. Cape May (Days 58-62) / Sandy Hook (Day 63) duplicate lat/lon + mileage — old item, untouched this session.
4. The two new June 22-23 draft stubs have local `file://` photo paths and need `scripts/10-upload-r2.mjs` run on them before their images will actually render.
</remaining_work>

<decisions_made>
- For a post spanning 2 real calendar days, use the arrival/first day as its single `date` field (established from the Peterborough and Bobcaygeon/Fenelon Falls fixes).
- When a post's own frontmatter `miles`/`hours` differs from a same-dated Nebo trip-log entry, don't blindly overwrite — they can both be legitimate (full daily GPX slice vs. Nebo's own start/stop voyage timer). Only trust the log's number when there's no other source or it's cross-validated.
- Never re-run `scripts/00-index-photos.mjs` scoped to a narrow date range against the live `.planning/data/photo-index.json` — it overwrites the whole shared file. Query `Photos.sqlite` directly with a standalone scoped script instead.
</decisions_made>

<blockers>
None. Everything committed and pushed.
</blockers>

## Infrastructure State
- Local dev server (`astro dev`, PREVIEW_DRAFTS=1) is running on `localhost:4322` — needed to view the unpublished draft stubs. Restart with `npx astro dev stop` then `npm run dev:drafts` if it goes stale.
- Production build (`npm run build`) verified clean at 86 pages after every change this session.

## Required Reading
1. `.claude/projects/.../memory/project_days65_72_chronology_cluster.md` — full detail on the Jul 2-19 fix (done) and the still-open Georgian Bay cluster (not done), including the exact GPS/content evidence gathered so far
2. `.claude/projects/.../memory/project_nebo_logs_ocr_bug.md` — the OCR bug, the audit method (and its limits — a generic regex approach was tried and rejected), and the Mighty Mississippi cross-reference

<context>
This grew almost entirely from the user browsing the live site and pointing out real bugs and corrections one at a time — several small asks turned into much bigger site-wide sweeps once a pattern was confirmed. The verification method that worked repeatedly: read the actual Nebo map screenshot image directly (`Read` tool handles images fine) or the raw Nebo trip-log text, cross-check against the post's own narrative content, and cross-validate distance/hours arithmetic against stored speed values — never trust a single stored number in isolation. Two near-misses were caught cleanly this session (a duplicate YAML key, and an accidental overwrite of the shared photo-index.json) with no lasting damage.
</context>

<next_action>
Start with: the Georgian Bay Leg 1/2/3 + North Channel Benjamins cluster (remaining_work item 1) — fetch and read `.planning/data/nebo-maps/2022-07-25.jpg` through `2022-08-06.jpg` (some may need fetching if not cached locally) and cross-check each against its candidate post's narrative content before changing any dates, same method as the Jul 2-19 fix. Ask the user which item to tackle first if picking up cold.
</next_action>
