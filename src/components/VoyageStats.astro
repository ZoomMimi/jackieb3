---
interface Props {
	miles?: number;
	hours?: number;
	stops?: number;
	startLocation?: string;
	endLocation?: string;
	maxKnots?: number;
	avgKnots?: number;
	weather?: string;
}

const { miles, hours, stops, startLocation, endLocation, maxKnots, avgKnots, weather } = Astro.props;

// Empty-state guard: render nothing when no props are provided
const hasData =
	miles !== undefined ||
	hours !== undefined ||
	stops !== undefined ||
	startLocation !== undefined ||
	endLocation !== undefined ||
	maxKnots !== undefined ||
	avgKnots !== undefined ||
	weather !== undefined;
---

{hasData && (
	<div class="voyage-stats">
		<div class="voyage-stats__rows">
			{miles !== undefined && (
				<div class="voyage-stats__row">
					<span class="voyage-stats__label">Distance</span>
					<span class="voyage-stats__value">{miles} nm</span>
				</div>
			)}
			{hours !== undefined && (
				<div class="voyage-stats__row">
					<span class="voyage-stats__label">Underway</span>
					<span class="voyage-stats__value">{Math.round(hours * 10) / 10} hrs</span>
				</div>
			)}
			{avgKnots !== undefined && (
				<div class="voyage-stats__row">
					<span class="voyage-stats__label">Avg Speed</span>
					<span class="voyage-stats__value">{avgKnots} kn</span>
				</div>
			)}
			{maxKnots !== undefined && (
				<div class="voyage-stats__row">
					<span class="voyage-stats__label">Max Speed</span>
					<span class="voyage-stats__value">{maxKnots} kn</span>
				</div>
			)}
			{weather !== undefined && (
				<div class="voyage-stats__row">
					<span class="voyage-stats__label">Weather</span>
					<span class="voyage-stats__value">{weather}</span>
				</div>
			)}
			{stops !== undefined && (
				<div class="voyage-stats__row">
					<span class="voyage-stats__label">Stops</span>
					<span class="voyage-stats__value">{stops}</span>
				</div>
			)}
		</div>
		{startLocation !== undefined && endLocation !== undefined && (
			<div class="voyage-stats__route">
				{startLocation} &rarr; {endLocation}
			</div>
		)}
	</div>
)}

<style>
	.voyage-stats {
		border-bottom: 2px solid var(--color-navy);
		margin-bottom: 1.5rem;
		padding-bottom: 1rem;
		font-family: var(--font-inter), system-ui, sans-serif;
	}

	.voyage-stats__rows {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem 2rem;
		margin-bottom: 0.5rem;
	}

	.voyage-stats__row {
		display: flex;
		flex-direction: column;
		gap: 0.1rem;
	}

	.voyage-stats__label {
		font-size: 11px;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: var(--color-muted);
	}

	.voyage-stats__value {
		font-size: 16px;
		font-weight: 600;
		color: var(--color-navy);
	}

	.voyage-stats__route {
		font-size: 13px;
		color: var(--color-muted);
		margin-top: 0.25rem;
	}
</style>
